# figures/

Drop figure files (`.pdf`, `.png`, `.jpg`, TikZ-generated PDFs) here.

`graphicspath{{figures/}}` is set in `preamble.tex`, so referencing
them from chapter files is just `\includegraphics{filename}` — no
folder prefix needed.
