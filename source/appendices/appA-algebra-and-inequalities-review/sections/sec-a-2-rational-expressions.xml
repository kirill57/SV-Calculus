<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-a-2-rational-expressions">
  <title>Rational expressions</title>

  <p>A rational expression is a quotient of polynomials, such as</p>

  <md>\frac{x^2-1}{x-1}.</md>

  <p>Rational expressions are common in limits, derivatives, partial fractions, and algebraic
simplification before integration.</p>

  <p>The first rule is about domain:</p>

  <md>
\text{A rational expression is undefined wherever its denominator is \(0\).}
</md>

  <subsection xml:id="appsubsec-canceling-common-factors">
    <title>Canceling common factors</title>

  <p>Consider</p>

  <md>\frac{x^2-1}{x-1}.</md>

  <p>The numerator factors:</p>

  <md>x^2-1=(x-1)(x+1).</md>

  <p>So</p>

  <md>\frac{x^2-1}{x-1}
=
\frac{(x-1)(x+1)}{x-1}.</md>

  <p>For <m>x\neq1</m>, we may cancel the common factor <m>x-1</m>:</p>

  <md>
\frac{x^2-1}{x-1}=x+1,
\qquad x\neq1.
</md>

  <p>The condition <m>x\neq1</m> stays.  The original expression was not defined at <m>x=1</m>.
Canceling a factor simplifies nearby values, but it does not restore the missing input.</p>

  <note xml:id="note-sec-a-2-rational-expressions-29">
    <title>Cancel factors, not terms</title>
      <p>This is legal:</p>
        <md>\frac{(x-1)(x+1)}{x-1}=x+1,
\qquad x\neq1.</md>
      <p>This is not legal:</p>
        <md>\frac{x^2-1}{x-1}\neq \frac{x^2}{x}=x.</md>
      <p>The numerator <m>x^2-1</m> is a difference, not a product.</p>
  </note>

  <example xml:id="ex-sec-a-2-rational-expressions-1">
    <title>Example</title>
    <statement>
  <p>Simplify</p>

  <md>\frac{x^2+5x+6}{x^2-9}.</md>

  <p>Factor numerator and denominator:</p>

  <md>x^2+5x+6=(x+2)(x+3),</md>

  <p>and</p>

  <md>x^2-9=(x-3)(x+3).</md>

  <p>Therefore</p>

  <md>\frac{x^2+5x+6}{x^2-9}
=
\frac{(x+2)(x+3)}{(x-3)(x+3)}.</md>

  <p>Cancel the common factor <m>x+3</m>, but keep the domain restrictions:</p>

  <md>
\frac{x^2+5x+6}{x^2-9}
=
\frac{x+2}{x-3},
\qquad x\neq -3,\;3.
</md>

  <p>The simplified expression still must remember that <m>x=-3</m> and <m>x=3</m> were forbidden
in the original denominator.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-combining-rational-expressions">
    <title>Combining rational expressions</title>

  <p>To add or subtract rational expressions, use a common denominator.</p>

  <example xml:id="ex-sec-a-2-rational-expressions-2">
    <title>Example</title>
    <statement>
  <p>Simplify</p>

  <md>\frac{2}{x}+\frac{3}{x+1}.</md>

  <p>The common denominator is <m>x(x+1)</m>.  Thus</p>

  <md>\frac{2}{x}
=
\frac{2(x+1)}{x(x+1)},</md>

  <p>and</p>

  <md>\frac{3}{x+1}
=
\frac{3x}{x(x+1)}.</md>

  <p>So</p>

  <md>\frac{2}{x}+\frac{3}{x+1}
=
\frac{2(x+1)+3x}{x(x+1)}.</md>

  <p>Therefore</p>

  <md>
\frac{2}{x}+\frac{3}{x+1}
=
\frac{5x+2}{x(x+1)},
\qquad x\neq0,-1.
</md>
    </statement>
  </example>

  <example xml:id="ex-sec-a-2-rational-expressions-3">
    <title>Example</title>
    <statement>
  <p>Simplify the complex fraction</p>

  <md>\frac{\frac1x-\frac1a}{x-a},
\qquad x\neq0,\ a\neq0,\ x\neq a.</md>

  <p>First simplify the numerator:</p>

  <md>\frac1x-\frac1a
=
\frac{a-x}{ax}
=
-\frac{x-a}{ax}.</md>

  <p>Then</p>

  <md>\frac{\frac1x-\frac1a}{x-a}
=
\frac{-\frac{x-a}{ax}}{x-a}.</md>

  <p>Since <m>x\neq a</m>, cancel <m>x-a</m>:</p>

  <md>
\frac{\frac1x-\frac1a}{x-a}
=
-\frac1{ax}.
</md>

  <p>This kind of expression appears when differentiating <m>1/x</m> from the definition.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-rationalizing">
    <title>Rationalizing</title>

  <p>Rationalizing means multiplying by a conjugate to remove a radical from a sum or
difference.</p>

  <p>The basic identity is</p>

  <md>(A-B)(A+B)=A^2-B^2.</md>

  <example xml:id="ex-sec-a-2-rational-expressions-4">
    <title>Example</title>
    <statement>
  <p>Simplify</p>

  <md>\frac{\sqrt{x+1}-1}{x},
\qquad x\neq0,\ x\ge -1.</md>

  <p>Multiply by the conjugate:</p>

  <md>\frac{\sqrt{x+1}-1}{x}
\cdot
\frac{\sqrt{x+1}+1}{\sqrt{x+1}+1}.</md>

  <p>The numerator becomes</p>

  <md>(\sqrt{x+1})^2-1^2=x+1-1=x.</md>

  <p>Thus</p>

  <md>\frac{\sqrt{x+1}-1}{x}
=
\frac{x}{x(\sqrt{x+1}+1)}.</md>

  <p>Since <m>x\neq0</m>,</p>

  <md>
\frac{\sqrt{x+1}-1}{x}
=
\frac{1}{\sqrt{x+1}+1}.
</md>

  <p>This is exactly the algebra that makes the limit</p>

  <md>\lim_{x\to0}\frac{\sqrt{x+1}-1}{x}</md>

  <p>easy.</p>
    </statement>
  </example>
  </subsection>

</section>
