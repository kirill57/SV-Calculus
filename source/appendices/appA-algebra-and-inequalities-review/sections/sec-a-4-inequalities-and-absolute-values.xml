<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-a-4-inequalities-and-absolute-values">
  <title>Inequalities and absolute values</title>

  <p>Calculus uses inequalities to control error.</p>

  <p>A limit proof says: make the input error small enough so that the output error is below
a chosen bound.  An approximation theorem says: the remainder is no larger than a
certain expression.  A convergence test says: compare one quantity with another.</p>

  <p>So inequalities are not only algebraic chores.  They are the grammar of control.</p>

  <subsection xml:id="appsubsec-rules-inequalities">
    <title>Rules for inequalities</title>

  <p>The safe rules are:</p>

  <table xml:id="tab-sec-a-4-inequalities-and-absolute-values-1">
    <tabular halign="left">
      <row header="yes" bottom="medium">
        <cell><strong>operation</strong></cell>
        <cell><strong>rule</strong></cell>
      </row>
      <row bottom="minor">
        <cell>Add the same number</cell>
        <cell>If <m>a&lt;b</m>, then <m>a+c&lt;b+c</m>.</cell>
      </row>
      <row bottom="minor">
        <cell>Subtract the same number</cell>
        <cell>If <m>a&lt;b</m>, then <m>a-c&lt;b-c</m>.</cell>
      </row>
      <row bottom="minor">
        <cell>Multiply by a positive number</cell>
        <cell>If <m>a&lt;b</m> and <m>c&gt;0</m>, then <m>ac&lt;bc</m>.</cell>
      </row>
      <row bottom="minor">
        <cell>Multiply by a negative number</cell>
        <cell>If <m>a&lt;b</m> and <m>c&lt;0</m>, then <m>ac&gt;bc</m>.</cell>
      </row>
      <row>
        <cell>Take reciprocals of positives</cell>
        <cell>If <m>0&lt;a&lt;b</m>, then <m>\dfrac1a&gt;\dfrac1b</m>.</cell>
      </row>
    </tabular>
  </table>

  <p>The most common mistake is forgetting to reverse the inequality when multiplying or
dividing by a negative number.</p>

  <example xml:id="ex-sec-a-4-inequalities-and-absolute-values-1">
    <title>Example</title>
    <statement>
  <p>Solve</p>

  <md>-3x+5&lt;11.</md>

  <p>Subtract <m>5</m>:</p>

  <md>-3x&lt;6.</md>

  <p>Divide by <m>-3</m>.  Because <m>-3</m> is negative, reverse the inequality:</p>

  <md>
x&gt;-2.
</md>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-sign-charts-inequalities">
    <title>Sign charts</title>

  <p>For polynomial and rational inequalities, factoring and sign charts are usually safer
than expanding.</p>

  <example xml:id="ex-sec-a-4-inequalities-and-absolute-values-2">
    <title>Example</title>
    <statement>
  <p>Solve</p>

  <md>x^2-x-6&gt;0.</md>

  <p>Factor:</p>

  <md>x^2-x-6=(x-3)(x+2).</md>

  <p>The critical numbers are</p>

  <md>x=-2
\qquad\text{and}\qquad
x=3.</md>

  <p>They split the real line into three intervals:</p>

  <md>(-\infty,-2),
\qquad
(-2,3),
\qquad
(3,\infty).</md>

  <p>Test one point in each interval.</p>

  <table xml:id="tab-sec-a-4-inequalities-and-absolute-values-2">
    <tabular halign="left">
      <row header="yes" bottom="medium">
        <cell><strong>interval</strong></cell>
        <cell><m>x+2</m></cell>
        <cell><m>x-3</m></cell>
        <cell><m>(x+2)(x-3)</m></cell>
      </row>
      <row bottom="minor">
        <cell><m>(-\infty,-2)</m></cell>
        <cell><m>-</m></cell>
        <cell><m>-</m></cell>
        <cell><m>+</m></cell>
      </row>
      <row bottom="minor">
        <cell><m>(-2,3)</m></cell>
        <cell><m>+</m></cell>
        <cell><m>-</m></cell>
        <cell><m>-</m></cell>
      </row>
      <row>
        <cell><m>(3,\infty)</m></cell>
        <cell><m>+</m></cell>
        <cell><m>+</m></cell>
        <cell><m>+</m></cell>
      </row>
    </tabular>
  </table>

  <p>We need the product to be positive, so</p>

  <md>
x&lt;-2
\quad\text{or}\quad
x&gt;3.
</md>

  <p>In interval notation,</p>

  <md>
(-\infty,-2)\cup(3,\infty).
</md>

  <p>Because the inequality is strict, the endpoints are not included.</p>
    </statement>
  </example>

  <example xml:id="ex-sec-a-4-inequalities-and-absolute-values-3">
    <title>Example</title>
    <statement>
  <p>Solve</p>

  <md>\frac{x+1}{x-2}\le0.</md>

  <p>The numerator is zero at <m>x=-1</m>.  The denominator is zero at <m>x=2</m>, where the
expression is undefined.</p>

  <p>These numbers split the line into</p>

  <md>(-\infty,-1),
\qquad
(-1,2),
\qquad
(2,\infty).</md>

  <table xml:id="tab-sec-a-4-inequalities-and-absolute-values-3">
    <tabular halign="left">
      <row header="yes" bottom="medium">
        <cell><strong>interval</strong></cell>
        <cell><m>x+1</m></cell>
        <cell><m>x-2</m></cell>
        <cell><m>\dfrac{x+1}{x-2}</m></cell>
      </row>
      <row bottom="minor">
        <cell><m>(-\infty,-1)</m></cell>
        <cell><m>-</m></cell>
        <cell><m>-</m></cell>
        <cell><m>+</m></cell>
      </row>
      <row bottom="minor">
        <cell><m>(-1,2)</m></cell>
        <cell><m>+</m></cell>
        <cell><m>-</m></cell>
        <cell><m>-</m></cell>
      </row>
      <row>
        <cell><m>(2,\infty)</m></cell>
        <cell><m>+</m></cell>
        <cell><m>+</m></cell>
        <cell><m>+</m></cell>
      </row>
    </tabular>
  </table>

  <p>We need the expression to be less than or equal to <m>0</m>.  It is negative on
<m>(-1,2)</m>, and it is zero at <m>x=-1</m>.  It is undefined at <m>x=2</m>.</p>

  <p>Therefore</p>

  <md>
[-1,2).
</md>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-absolute-value-distance-review">
    <title>Absolute value as distance</title>

  <p>The absolute value <m>|x|</m> is the distance from <m>x</m> to <m>0</m>.  More generally,</p>

  <md>
|x-a|
</md>

  <p>is the distance from <m>x</m> to <m>a</m>.</p>

  <p>This gives the most important translations:</p>

  <md>
|x-a|&lt;r
\quad\Longleftrightarrow\quad
a-r&lt;x&lt;a+r,

\qquad r&gt;0,</md>

  <p>and</p>

  <md>
|x-a|\le r
\quad\Longleftrightarrow\quad
a-r\le x\le a+r.
</md>

  <figure xml:id="fig-absolute-value-distance-appendix">
    <caption>Absolute value inequalities are distance statements.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}[xscale=1.1, every node/.style={font=\small}]
  \draw[&lt;-&gt;, thick] (-0.7,0) -- (6.7,0);

  \coordinate (L) at (1,0);
  \coordinate (A) at (3,0);
  \coordinate (R) at (5,0);

  \draw[very thick] (L) -- (R);
  \draw[fill=white, thick] (L) circle (2.5pt);
  \draw[fill=black] (A) circle (2.5pt);
  \draw[fill=white, thick] (R) circle (2.5pt);

  \node[below] at (L) {\(a-r\)};
  \node[below] at (A) {\(a\)};
  \node[below] at (R) {\(a+r\)};

  \draw[&lt;-&gt;] (1,0.55) -- (3,0.55);
  \draw[&lt;-&gt;] (3,0.55) -- (5,0.55);
  \node[above] at (2,0.55) {\(r\)};
  \node[above] at (4,0.55) {\(r\)};

  \node at (3,-0.85) {\(|x-a|&lt;r\) means \(x\) lies inside this open interval.};
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <example xml:id="ex-sec-a-4-inequalities-and-absolute-values-4">
    <title>Example</title>
    <statement>
  <p>Solve</p>

  <md>|x-4|&lt;0.2.</md>

  <p>This means that <m>x</m> is within <m>0.2</m> of <m>4</m>.  Therefore</p>

  <md>4-0.2&lt;x&lt;4+0.2.</md>

  <p>So</p>

  <md>
3.8&lt;x&lt;4.2.
</md>
    </statement>
  </example>

  <example xml:id="ex-sec-a-4-inequalities-and-absolute-values-5">
    <title>Example</title>
    <statement>
  <p>Solve</p>

  <md>|2x+1|\le5.</md>

  <p>Translate to a compound inequality:</p>

  <md>-5\le2x+1\le5.</md>

  <p>Subtract <m>1</m>:</p>

  <md>-6\le2x\le4.</md>

  <p>Divide by <m>2</m>:</p>

  <md>
-3\le x\le2.
</md>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-absolute-value-limits">
    <title>Absolute value and limits</title>

  <p>The notation in limits uses absolute value because limits are about distance.</p>

  <p>For example,</p>

  <md>|x-2|&lt;0.01</md>

  <p>means that <m>x</m> is within <m>0.01</m> of <m>2</m>.</p>

  <p>If</p>

  <md>f(x)=3x+1,</md>

  <p>then the distance from <m>f(x)</m> to <m>7</m> is</p>

  <md>|f(x)-7|
=
|3x+1-7|
=
|3x-6|
=
3|x-2|.</md>

  <p>So if we want</p>

  <md>|f(x)-7|&lt;0.03,</md>

  <p>it is enough to require</p>

  <md>3|x-2|&lt;0.03.</md>

  <p>That is,</p>

  <md>|x-2|&lt;0.01.</md>

  <p>This is the algebraic heart of a simple epsilon-delta proof.  The absolute value measures
the input error and the output error.</p>
  </subsection>

</section>
