<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-d-3-bolzano-s-theorem-and-bisection">
  <title>Bolzano’s Theorem and Bisection</title>

  <p>The Intermediate Value Theorem says that a continuous graph cannot jump over a
height.  A special and extremely useful case is the root case.</p>

  <p>If a continuous function changes sign, then it must be zero somewhere in between.</p>

  <subsection xml:id="appsubsec-bolzano-theorem">
    <title>Bolzano’s theorem</title>

  <theorem xml:id="thm-sec-d-3-bolzano-s-theorem-and-bisection-39">
    <title>Bolzano’s Theorem</title>
    <statement>
      <p>Suppose <m>f</m> is continuous on <m>[a,b]</m>.  If</p>
        <md>f(a)&lt;0
\qquad\text{and}\qquad
f(b)&gt;0,</md>
      <p>then there is at least one number <m>c\in(a,b)</m> such that</p>
        <md>f(c)=0.</md>
      <p>The same conclusion holds if the signs are reversed.</p>
    </statement>
  </theorem>

  <p>This is the Intermediate Value Theorem with the intermediate value <m>0</m>.</p>

  <figure xml:id="fig-bolzano-crossing">
    <caption>A continuous graph that goes from negative to positive must cross the <m>x</m>-axis.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.76\textwidth,
    height=0.45\textwidth,
    xmin=0, xmax=4,
    ymin=-2.2, ymax=2.2,
    axis lines=middle,
    xlabel={\(x\)},
    ylabel={\(f(x)\)},
    xtick={0.8,3.4},
    xticklabels={\(a\),\(b\)},
    ytick={-1,0,1},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[very thick, domain=0.55:3.6, samples=150] {0.55*(x-2.1)^3 + 0.35*(x-2.1)};
\addplot[only marks, mark=*] coordinates {(0.8,-1.29) (3.4,1.55)};
\draw[dashed] (axis cs:0.8,0) -- (axis cs:0.8,-1.29);
\draw[dashed] (axis cs:3.4,0) -- (axis cs:3.4,1.55);
\node[anchor=north east] at (axis cs:2.1,0) {\(c\)};
\node[anchor=west] at (axis cs:2.45,1.25) {continuous graph};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>
  </subsection>

  <subsection xml:id="appsubsec-bolzano-proof-bisection">
    <title>Proof by bisection</title>

  <p>Assume</p>

  <md>f(a)&lt;0
\qquad\text{and}\qquad
f(b)&gt;0.</md>

  <p>Set</p>

  <md>L_1=a,
\qquad
R_1=b.</md>

  <p>The interval</p>

  <md>[L_1,R_1]</md>

  <p>has a sign change.</p>

  <p>Now bisect the interval.  Let</p>

  <md>M_1=\frac{L_1+R_1}{2}.</md>

  <p>There are three possibilities.</p>

  <p>If</p>

  <md>f(M_1)=0,</md>

  <p>then we have found a root.</p>

  <p>If</p>

  <md>f(M_1)&gt;0,</md>

  <p>then the sign change occurs on</p>

  <md>[L_1,M_1],</md>

  <p>so we choose</p>

  <md>L_2=L_1,
\qquad
R_2=M_1.</md>

  <p>If</p>

  <md>f(M_1)&lt;0,</md>

  <p>then the sign change occurs on</p>

  <md>[M_1,R_1],</md>

  <p>so we choose</p>

  <md>L_2=M_1,
\qquad
R_2=R_1.</md>

  <p>Repeat this process.</p>

  <p>Unless we find a root exactly at a midpoint, we create nested intervals</p>

  <md>[L_1,R_1]\supset [L_2,R_2]\supset [L_3,R_3]\supset\cdots</md>

  <p>such that</p>

  <md>f(L_n)\le0
\qquad\text{and}\qquad
f(R_n)\ge0</md>

  <p>for every <m>n</m>.</p>

  <p>Each bisection cuts the length in half:</p>

  <md>R_n-L_n=\frac{b-a}{2^{n-1}}.</md>

  <p>So</p>

  <md>R_n-L_n\to0.</md>

  <p>By the Nested Interval Principle, the intervals trap one number <m>c</m>.  Thus</p>

  <md>L_n\to c
\qquad\text{and}\qquad
R_n\to c.</md>

  <p>Since <m>f</m> is continuous,</p>

  <md>f(L_n)\to f(c)
\qquad\text{and}\qquad
f(R_n)\to f(c).</md>

  <p>But</p>

  <md>f(L_n)\le0</md>

  <p>for every <m>n</m>, so the limit satisfies</p>

  <md>f(c)\le0.</md>

  <p>Also,</p>

  <md>f(R_n)\ge0</md>

  <p>for every <m>n</m>, so the limit satisfies</p>

  <md>f(c)\ge0.</md>

  <p>Therefore</p>

  <md>f(c)=0.</md>

  <p>That proves Bolzano’s Theorem.</p>

  <note xml:id="note-sec-d-3-bolzano-s-theorem-and-bisection-40">
    <title>The proof is also an algorithm</title>
      <p>The same bisection process that proves the root exists also gives numerical
approximations to the root.</p>
  </note>
  </subsection>

  <subsection xml:id="appsubsec-bisection-error">
    <title>Bisection error</title>

  <p>After <m>N</m> completed bisections, the interval containing the root has length</p>

  <md>
\frac{b-a}{2^N}.
</md>

  <p>If we report the midpoint of the final interval as our approximation, then the true root
is at most half an interval length away.  Therefore the midpoint error is at most</p>

  <md>
\frac{b-a}{2^{N+1}}.
</md>

  <example xml:id="ex-sec-d-3-bolzano-s-theorem-and-bisection-1">
    <title>Example</title>
    <statement>
  <p>Use bisection to locate a root of</p>

  <md>p(x)=x^3-x-1</md>

  <p>in the interval <m>[1,2]</m>.</p>

  <p>First check the signs:</p>

  <md>p(1)=1-1-1=-1&lt;0,</md>

  <p>and</p>

  <md>p(2)=8-2-1=5&gt;0.</md>

  <p>So there is a root in <m>(1,2)</m>.</p>

  <table xml:id="tab-sec-d-3-bolzano-s-theorem-and-bisection-2">
    <tabular halign="left">
      <row header="yes" bottom="medium">
        <cell><strong>step</strong></cell>
        <cell><m>L</m></cell>
        <cell><m>R</m></cell>
        <cell><m>M=\dfrac{L+R}{2}</m></cell>
        <cell><m>p(M)</m></cell>
        <cell><strong>next interval</strong></cell>
      </row>
      <row bottom="minor">
        <cell><m>0</m></cell>
        <cell><m>1</m></cell>
        <cell><m>2</m></cell>
        <cell><m>1.5</m></cell>
        <cell><m>0.875</m></cell>
        <cell><m>[1,1.5]</m></cell>
      </row>
      <row bottom="minor">
        <cell><m>1</m></cell>
        <cell><m>1</m></cell>
        <cell><m>1.5</m></cell>
        <cell><m>1.25</m></cell>
        <cell><m>-0.296875</m></cell>
        <cell><m>[1.25,1.5]</m></cell>
      </row>
      <row bottom="minor">
        <cell><m>2</m></cell>
        <cell><m>1.25</m></cell>
        <cell><m>1.5</m></cell>
        <cell><m>1.375</m></cell>
        <cell><m>0.224609375</m></cell>
        <cell><m>[1.25,1.375]</m></cell>
      </row>
      <row>
        <cell><m>3</m></cell>
        <cell><m>1.25</m></cell>
        <cell><m>1.375</m></cell>
        <cell><m>1.3125</m></cell>
        <cell><m>-0.051513672</m></cell>
        <cell><m>[1.3125,1.375]</m></cell>
      </row>
    </tabular>
  </table>

  <p>After four completed bisections, the root lies in</p>

  <md>[1.3125,1.375].</md>

  <p>The interval length is</p>

  <md>\frac1{2^4}=\frac1{16}=0.0625.</md>

  <p>If we use the midpoint</p>

  <md>\frac{1.3125+1.375}{2}=1.34375,</md>

  <p>then the error is at most</p>

  <md>\frac{0.0625}{2}=0.03125.</md>

  <p>More bisections improve the guarantee.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-bolzano-continuity-matters">
    <title>Why continuity matters</title>

  <p>Bolzano’s Theorem fails without continuity.</p>

  <p>Define</p>

  <md>
    <mrow>J(x)=
\begin{cases}
-1, \amp x&lt;0,</mrow>
    <mrow>1, \amp x\ge0.
\end{cases}</mrow>
  </md>

  <p>Then</p>

  <md>J(-1)=-1&lt;0
\qquad\text{and}\qquad
J(1)=1&gt;0.</md>

  <p>But there is no <m>c\in[-1,1]</m> with</p>

  <md>J(c)=0.</md>

  <p>The function jumps over <m>0</m>.</p>

  <figure xml:id="fig-bolzano-fails-jump">
    <caption>A jump discontinuity can change sign without crossing <m>0</m>.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.68\textwidth,
    height=0.42\textwidth,
    xmin=-1.4, xmax=1.4,
    ymin=-1.5, ymax=1.5,
    axis lines=middle,
    xlabel={\(x\)},
    ylabel={\(J(x)\)},
    xtick={-1,0,1},
    ytick={-1,0,1},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[very thick, domain=-1.35:-0.04, samples=2] {-1};
\addplot[very thick, domain=0:1.35, samples=2] {1};
\addplot[only marks, mark=o, mark size=3pt, thick] coordinates {(0,-1)};
\addplot[only marks, mark=*, mark size=2.5pt] coordinates {(0,1)};
\draw[dashed] (axis cs:-1.35,0) -- (axis cs:1.35,0);
\node[anchor=west] at (axis cs:0.15,0.15) {skipped \(0\)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <p>The theorem is a no-jump theorem.</p>
  </subsection>

</section>
