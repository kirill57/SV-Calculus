<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-e-2-numerical-precision-and-roundoff">
  <title>Numerical precision and roundoff</title>

  <p>A real number can have infinitely many decimal places.  A machine stores only finitely
many digits.</p>

  <p>That difference is small in many calculations and large in some.</p>

  <p>Calculus cares about small errors, so we must know how numerical errors enter.</p>

  <subsection xml:id="appsubsec-exact-and-floating-point">
    <title>Exact arithmetic and floating-point arithmetic</title>

  <p>In exact mathematics,</p>

  <md>\frac{1}{3}</md>

  <p>is exactly one third.</p>

  <p>In decimal floating-point arithmetic, it must be stored approximately:</p>

  <md>0.333333333333\ldots</md>

  <p>with only finitely many digits.</p>

  <p>Similarly,</p>

  <md>\sqrt2</md>

  <p>is an exact number in mathematics, but a decimal approximation in a calculator.</p>

  <p>A computer typically stores a number in scientific notation, roughly like</p>

  <md>\pm d_1.d_2d_3\cdots d_k\times 10^m,</md>

  <p>where only finitely many digits are kept.  The exact storage system depends on the
machine and software, but the main idea is universal:</p>

  <md>
\text{Most real numbers are rounded when stored.}
</md>

  <note xml:id="note-sec-e-2-numerical-precision-and-roundoff-21">
    <title>Roundoff error</title>
      <p><em>Roundoff error</em> is the error introduced when a real number is replaced by a
nearby machine-representable number.</p>
  </note>

  <p>This does not make computation unreliable.  It means computation must be interpreted
with error in mind.</p>
  </subsection>

  <subsection xml:id="appsubsec-cancellation">
    <title>Subtraction can magnify relative error</title>

  <p>The most dangerous basic operation is often subtraction of nearly equal numbers.</p>

  <p>Suppose a calculator stores the two numbers</p>

  <md>1.0000001
\qquad\text{and}\qquad
1.0000000</md>

  <p>with limited precision.  Their difference is</p>

  <md>0.0000001.</md>

  <p>If each input has small rounding error, the difference may have a large relative error,
because the true answer is very small.</p>

  <p>This problem is called <em>cancellation</em>.</p>

  <p>Calculus produces cancellation often.  The difference quotient</p>

  <md>\frac{f(x+h)-f(x)}{h}</md>

  <p>subtracts two nearby function values when <m>h</m> is small.  If <m>h</m> is too small on a
computer, roundoff error can overwhelm the improvement we expected from taking a
smaller interval.</p>

  <example xml:id="ex-sec-e-2-numerical-precision-and-roundoff-1">
    <title>a numerically unstable expression</title>
    <statement>
  <p>Consider</p>

  <md>Q(x)=\frac{\sqrt{1+x}-1}{x}</md>

  <p>for <m>x</m> near <m>0</m>.</p>

  <p>Mathematically, rationalize:</p>

  <md>\frac{\sqrt{1+x}-1}{x}
\cdot
\frac{\sqrt{1+x}+1}{\sqrt{1+x}+1}
=
\frac{1}{\sqrt{1+x}+1},
\qquad x\neq0.</md>

  <p>So for <m>x\neq0</m>,</p>

  <md>Q(x)=\frac{1}{\sqrt{1+x}+1}.</md>

  <p>As <m>x\to0</m>,</p>

  <md>Q(x)\to \frac12.</md>

  <p>The original form subtracts two nearby numbers:</p>

  <md>\sqrt{1+x}
\quad\text{and}\quad
1.</md>

  <p>The rationalized form avoids that subtraction.  Near <m>x=0</m>, it is usually more stable
numerically.</p>

  <md>
\frac{\sqrt{1+x}-1}{x}
\quad\text{and}\quad
\frac{1}{\sqrt{1+x}+1}
</md>

  <p>are algebraically equivalent for <m>x\neq0</m>, but not equally safe for numerical
evaluation.</p>

  <note xml:id="note-sec-e-2-numerical-precision-and-roundoff-22">
    <title>Numerical algebra habit</title>
      <p>Algebraic simplification is not only for exact answers.  It can also make numerical
answers more stable.</p>
  </note>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-roundoff-vs-truncation">
    <title>Roundoff versus truncation</title>

  <p>Many numerical methods have two different kinds of error.</p>

  <table xml:id="tab-sec-e-2-numerical-precision-and-roundoff-0">
    <tabular halign="left">
      <row header="yes" bottom="medium">
        <cell><strong>error type</strong></cell>
        <cell><strong>meaning</strong></cell>
      </row>
      <row bottom="minor">
        <cell>Truncation error</cell>
        <cell>The error from replacing an infinite or limiting process by a finite one.</cell>
      </row>
      <row>
        <cell>Roundoff error</cell>
        <cell>The error from storing and operating with finitely many digits.</cell>
      </row>
    </tabular>
  </table>

  <example xml:id="ex-sec-e-2-numerical-precision-and-roundoff-2">
    <title>derivative approximation</title>
    <statement>
  <p>To approximate <m>f’(a)</m>, we might use</p>

  <md>\frac{f(a+h)-f(a)}{h}.</md>

  <p>If <m>h</m> is large, the secant slope may be a poor approximation to the tangent slope.
That is truncation error.</p>

  <p>If <m>h</m> is extremely small, then <m>f(a+h)</m> and <m>f(a)</m> may be almost equal as stored
numbers.  Their subtraction may lose important digits.  That is roundoff error.</p>

  <p>So making <m>h</m> smaller is not always better on a machine.</p>

  <warning xml:id="warn-sec-e-2-numerical-precision-and-roundoff-23">
    <title>Computing warning</title>
      <p>In exact mathematics,</p>
        <md>h\to0</md>
      <p>is the right idea.</p>
      <p>In floating-point computation, taking <m>h</m> too small can make the numerical result
worse.</p>
  </warning>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-residuals-step-sizes">
    <title>Residuals and step sizes</title>

  <p>When an algorithm produces an approximate answer, do not ask only:</p>

  <md>\text{Did the program stop?}</md>

  <p>Ask:</p>

  <md>\text{Why did it stop?}</md>

  <p>For root-finding, two common checks are:</p>

  <md>|f(x_n)| \quad \text{and} \quad |x_n-x_{n-1}|.</md>

  <p>The number <m>|f(x_n)|</m> is a <em>residual</em>.  It tells how close the function value is to
zero.</p>

  <p>The number <m>|x_n-x_{n-1}|</m> is a step size.  It tells how much the approximate root
changed in the last step.</p>

  <p>Both can matter.</p>

  <p>A small step size alone does not prove that <m>f(x_n)</m> is small.  An algorithm might be
stuck.</p>

  <p>A small residual alone does not always prove the root is known accurately.  If the graph
is very flat or very steep, residual size and input error can behave differently.</p>

  <note xml:id="note-sec-e-2-numerical-precision-and-roundoff-24">
    <title>Numerical stopping habit</title>
      <p>For iterative methods, record at least:</p>
        <md>x_n,\qquad f(x_n),\qquad |x_n-x_{n-1}|.</md>
      <p>A good computation leaves a trail.</p>
  </note>
  </subsection>

</section>
