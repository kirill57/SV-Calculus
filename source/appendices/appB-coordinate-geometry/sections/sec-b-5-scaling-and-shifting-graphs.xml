<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-b-5-scaling-and-shifting-graphs">
  <title>Scaling and shifting graphs</title>

  <p>Many graphs in calculus come from simpler graphs by shifting, stretching, and
reflecting.</p>

  <p>If we understand the parent graph, transformations let us sketch the new graph
quickly.  More importantly, they tell us how the formula changes the geometry.</p>

  <subsection xml:id="appsubsec-vertical-horizontal-shifts">
    <title>Vertical and horizontal shifts</title>

  <p>Let <m>y=f(x)</m>.</p>

  <p>The graph of</p>

  <md>y=f(x)+k</md>

  <p>is the graph of <m>y=f(x)</m> shifted vertically by <m>k</m>.</p>

  <p>If <m>k&gt;0</m>, the graph moves up.  If <m>k&lt;0</m>, it moves down.</p>

  <p>The graph of</p>

  <md>y=f(x-h)</md>

  <p>is the graph of <m>y=f(x)</m> shifted horizontally by <m>h</m>.</p>

  <p>If <m>h&gt;0</m>, the graph moves right.  If <m>h&lt;0</m>, it moves left.</p>

  <table xml:id="tab-sec-b-5-scaling-and-shifting-graphs-2">
    <tabular halign="left">
      <row header="yes" bottom="medium">
        <cell><strong>new function</strong></cell>
        <cell><strong>effect on graph of <m>y=f(x)</m></strong></cell>
      </row>
      <row bottom="minor">
        <cell><m>f(x)+k</m></cell>
        <cell>shift up by <m>k</m> if <m>k&gt;0</m>, down if <m>k&lt;0</m></cell>
      </row>
      <row>
        <cell><m>f(x-h)</m></cell>
        <cell>shift right by <m>h</m> if <m>h&gt;0</m>, left if <m>h&lt;0</m></cell>
      </row>
    </tabular>
  </table>

  <example xml:id="ex-sec-b-5-scaling-and-shifting-graphs-1">
    <title>Example</title>
    <statement>
  <p>Start with</p>

  <md>f(x)=x^2.</md>

  <p>Then</p>

  <md>g(x)=(x-3)^2-1</md>

  <p>is the graph of <m>x^2</m> shifted right <m>3</m> and down <m>1</m>.  Its vertex is</p>

  <md>(3,-1).</md>

  <figure xml:id="fig-appendix-parabola-shift">
    <caption>Replacing <m>x</m> by <m>x-3</m> shifts right <m>3</m>; subtracting <m>1</m> shifts down <m>1</m>.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.75\textwidth,
    height=0.47\textwidth,
    xmin=-3, xmax=7,
    ymin=-2, ymax=8,
    axis lines=middle,
    xlabel={\(x\)},
    ylabel={\(y\)},
    xtick={0,3},
    ytick={-1,0,4},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
    legend style={draw=none, fill=none, at={(0.5,-0.20)}, anchor=north, legend columns=2, font=\small}
]
\addplot[thick, dashed, domain=-2.5:2.5, samples=120] {x^2};
\addlegendentry{\(y=x^2\)}
\addplot[very thick, domain=0.5:5.5, samples=120] {(x-3)^2-1};
\addlegendentry{\(y=(x-3)^2-1\)}
\addplot[only marks, mark=*] coordinates {(0,0) (3,-1)};
\node[anchor=north west] at (axis cs:3,-1) {\((3,-1)\)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-vertical-scaling-reflection">
    <title>Vertical scaling and reflection</title>

  <p>The graph of</p>

  <md>y=af(x)</md>

  <p>is a vertical scaling of the graph of <m>y=f(x)</m>.</p>

  <p>If</p>

  <md>|a|&gt;1,</md>

  <p>the graph stretches vertically.</p>

  <p>If</p>

  <md>0&lt;|a|&lt;1,</md>

  <p>the graph compresses vertically.</p>

  <p>If</p>

  <md>a&lt;0,</md>

  <p>the graph is also reflected across the <m>x</m>-axis.</p>

  <example xml:id="ex-sec-b-5-scaling-and-shifting-graphs-2">
    <title>Example</title>
    <statement>
  <p>Start with</p>

  <md>f(x)=\sqrt{x}.</md>

  <p>Then</p>

  <md>g(x)=-2\sqrt{x}</md>

  <p>is reflected across the <m>x</m>-axis and stretched vertically by <m>2</m>.  Its domain is still</p>

  <md>[0,\infty),</md>

  <p>but its range is</p>

  <md>(-\infty,0].</md>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-horizontal-scaling-reflection">
    <title>Horizontal scaling and reflection</title>

  <p>The graph of</p>

  <md>y=f(cx)</md>

  <p>is a horizontal scaling of the graph of <m>y=f(x)</m>.</p>

  <p>If</p>

  <md>|c|&gt;1,</md>

  <p>the graph compresses horizontally by a factor of <m>|c|</m>.</p>

  <p>If</p>

  <md>0&lt;|c|&lt;1,</md>

  <p>the graph stretches horizontally by a factor of <m>1/|c|</m>.</p>

  <p>If</p>

  <md>c&lt;0,</md>

  <p>the graph is also reflected across the <m>y</m>-axis.</p>

  <p>This direction can feel backwards.  The reason is that the input is being changed
before <m>f</m> acts.</p>

  <p>For example,</p>

  <md>f(2x)</md>

  <p>reaches the old input <m>1</m> when</p>

  <md>2x=1,</md>

  <p>so</p>

  <md>x=\frac12.</md>

  <p>Everything happens in half the horizontal distance.  The graph is compressed.</p>

  <table xml:id="tab-sec-b-5-scaling-and-shifting-graphs-3">
    <tabular halign="left">
      <row header="yes" bottom="medium">
        <cell><strong>new function</strong></cell>
        <cell><strong>effect on graph of <m>y=f(x)</m></strong></cell>
      </row>
      <row bottom="minor">
        <cell><m>af(x)</m>, <m>a&gt;1</m></cell>
        <cell>vertical stretch by <m>a</m></cell>
      </row>
      <row bottom="minor">
        <cell><m>af(x)</m>, <m>0&lt;a&lt;1</m></cell>
        <cell>vertical compression by <m>a</m></cell>
      </row>
      <row bottom="minor">
        <cell><m>-f(x)</m></cell>
        <cell>reflection across the <m>x</m>-axis</cell>
      </row>
      <row bottom="minor">
        <cell><m>f(cx)</m>, <m>c&gt;1</m></cell>
        <cell>horizontal compression by <m>c</m></cell>
      </row>
      <row bottom="minor">
        <cell><m>f(cx)</m>, <m>0&lt;c&lt;1</m></cell>
        <cell>horizontal stretch by <m>1/c</m></cell>
      </row>
      <row>
        <cell><m>f(-x)</m></cell>
        <cell>reflection across the <m>y</m>-axis</cell>
      </row>
    </tabular>
  </table>

  <example xml:id="ex-sec-b-5-scaling-and-shifting-graphs-3">
    <title>Example</title>
    <statement>
  <p>Compare</p>

  <md>y=\sin x</md>

  <p>and</p>

  <md>y=\sin(2x).</md>

  <p>The graph of <m>\sin(2x)</m> completes a full oscillation when</p>

  <md>2x=2\pi,</md>

  <p>so</p>

  <md>x=\pi.</md>

  <p>Thus its period is</p>

  <md>\pi,</md>

  <p>half the period of <m>\sin x</m>.  The graph is horizontally compressed by <m>2</m>.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-transformations-domains">
    <title>Transformations and domains</title>

  <p>Transformations also affect domains and ranges.</p>

  <example xml:id="ex-sec-b-5-scaling-and-shifting-graphs-4">
    <title>Example</title>
    <statement>
  <p>Find the domain and range of</p>

  <md>g(x)=\sqrt{x-2}+1.</md>

  <p>The square root requires</p>

  <md>x-2\ge0.</md>

  <p>So</p>

  <md>x\ge2.</md>

  <p>Thus the domain is</p>

  <md>[2,\infty).</md>

  <p>Since</p>

  <md>\sqrt{x-2}\ge0,</md>

  <p>we have</p>

  <md>\sqrt{x-2}+1\ge1.</md>

  <p>Thus the range is</p>

  <md>[1,\infty).</md>

  <p>The graph of <m>y=\sqrt{x}</m> has been shifted right <m>2</m> and up <m>1</m>.</p>

  <figure xml:id="fig-appendix-sqrt-shift">
    <caption>The starting point of the square-root graph moves from <m>(0,0)</m> to <m>(2,1)</m>.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.70\textwidth,
    height=0.44\textwidth,
    xmin=-0.5, xmax=8,
    ymin=-0.5, ymax=4,
    axis lines=middle,
    xlabel={\(x\)},
    ylabel={\(y\)},
    xtick={0,2,4,6,8},
    ytick={0,1,2,3},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
    legend style={draw=none, fill=none, at={(0.5,-0.20)}, anchor=north, legend columns=2, font=\small}
]
\addplot[thick, dashed, domain=0:8, samples=150] {sqrt(x)};
\addlegendentry{\(y=\sqrt{x}\)}
\addplot[very thick, domain=2:8, samples=150] {sqrt(x-2)+1};
\addlegendentry{\(y=\sqrt{x-2}+1\)}
\addplot[only marks, mark=*] coordinates {(0,0) (2,1)};
\node[anchor=west] at (axis cs:2,1) {\((2,1)\)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-transforming-equations">
    <title>Transforming equations</title>

  <p>Transformations are not limited to functions.  They also apply to equations.</p>

  <p>For example, the unit circle</p>

  <md>x^2+y^2=1</md>

  <p>becomes an ellipse if we stretch horizontally by <m>3</m> and vertically by <m>2</m>.</p>

  <p>A point on the stretched curve has the form</p>

  <md>x=3u,
\qquad
y=2v,</md>

  <p>where</p>

  <md>u^2+v^2=1.</md>

  <p>So</p>

  <md>u=\frac{x}{3},
\qquad
v=\frac{y}{2}.</md>

  <p>Substitute into the unit circle equation:</p>

  <md>\left(\frac{x}{3}\right)^2+\left(\frac{y}{2}\right)^2=1.</md>

  <p>Thus the stretched curve is</p>

  <md>
\frac{x^2}{9}+\frac{y^2}{4}=1.
</md>

  <p>If we then shift the ellipse up by <m>1</m>, replace <m>y</m> by <m>y-1</m>:</p>

  <md>
\frac{x^2}{9}+\frac{(y-1)^2}{4}=1.
</md>

  <p>This is an ellipse centered at</p>

  <md>(0,1)</md>

  <p>with horizontal semi-axis <m>3</m> and vertical semi-axis <m>2</m>.</p>

  <warning xml:id="warn-sec-b-5-scaling-and-shifting-graphs-44">
    <title>Transformation warning</title>
      <p>For graph transformations, the input changes in the opposite-looking direction.
The expression <m>x-h</m> shifts right by <m>h</m>, not left.</p>
      <p>A reliable check is to track a familiar point.  If the old graph starts at <m>(0,0)</m>,
then <m>y=\sqrt{x-2}+1</m> starts at <m>(2,1)</m>.</p>
  </warning>
  </subsection>

</section>
