<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-b-2-circles">
  <title>Circles</title>

  <p>A circle is a distance condition.</p>

  <p>Choose a center</p>

  <md>(h,k)</md>

  <p>and a radius <m>r&gt;0</m>.  A point <m>(x,y)</m> lies on the circle if its distance from
<m>(h,k)</m> is exactly <m>r</m>.</p>

  <p>That sentence becomes an equation.</p>

  <subsection xml:id="appsubsec-equation-circle">
    <title>Equation of a circle</title>

  <p>The distance from <m>(x,y)</m> to <m>(h,k)</m> is</p>

  <md>\sqrt{(x-h)^2+(y-k)^2}.</md>

  <p>So the circle condition is</p>

  <md>\sqrt{(x-h)^2+(y-k)^2}=r.</md>

  <p>Squaring both sides gives</p>

  <md>
(x-h)^2+(y-k)^2=r^2.
</md>

  <p>This is the standard equation of a circle with center <m>(h,k)</m> and radius <m>r</m>.</p>

  <p>If the center is the origin, the equation becomes</p>

  <md>
x^2+y^2=r^2.
</md>

  <example xml:id="ex-sec-b-2-circles-1">
    <title>Example</title>
    <statement>
  <p>Find an equation of the circle with center</p>

  <md>(2,-3)</md>

  <p>and radius</p>

  <md>5.</md>

  <p>Use the standard form:</p>

  <md>(x-2)^2+(y-(-3))^2=5^2.</md>

  <p>Thus</p>

  <md>
(x-2)^2+(y+3)^2=25.
</md>

  <figure xml:id="fig-appendix-circle-center-radius">
    <caption>The circle <m>(x-2)^2+(y+3)^2=25</m> has center <m>(2,-3)</m> and radius <m>5</m>.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.58\textwidth,
    height=0.58\textwidth,
    xmin=-4, xmax=8,
    ymin=-9, ymax=3,
    axis lines=middle,
    xlabel={\(x\)},
    ylabel={\(y\)},
    xtick={-3,0,2,7},
    ytick={-8,-3,0,2},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
    axis equal image,
]
\addplot[very thick, domain=0:360, samples=200] ({2+5*cos(x)}, {-3+5*sin(x)});
\addplot[only marks, mark=*] coordinates {(2,-3)};
\node[anchor=south west] at (axis cs:2,-3) {\((2,-3)\)};
\node[anchor=west] at (axis cs:4.0,-0.2) {\(r=5\)};
\draw[thick] (axis cs:2,-3) -- (axis cs:7,-3);
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-circle-completing-square">
    <title>Completing the square</title>

  <p>A circle is not always written in standard form.  Completing the square reveals the
center and radius.</p>

  <example xml:id="ex-sec-b-2-circles-2">
    <title>Example</title>
    <statement>
  <p>Show that</p>

  <md>x^2+y^2-6x+4y-12=0</md>

  <p>is a circle, and find its center and radius.</p>

  <p>Group the <m>x</m>-terms and <m>y</m>-terms:</p>

  <md>x^2-6x+y^2+4y=12.</md>

  <p>Complete the square in each variable:</p>

  <md>x^2-6x=(x-3)^2-9,</md>

  <p>and</p>

  <md>y^2+4y=(y+2)^2-4.</md>

  <p>Substitute:</p>

  <md>(x-3)^2-9+(y+2)^2-4=12.</md>

  <p>Thus</p>

  <md>(x-3)^2+(y+2)^2=25.</md>

  <p>So the center is</p>

  <md>(3,-2)</md>

  <p>and the radius is</p>

  <md>5.</md>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-circle-inequalities">
    <title>Disks and circle inequalities</title>

  <p>A circle is the boundary.  A disk is the filled-in region.</p>

  <p>The equation</p>

  <md>(x-h)^2+(y-k)^2=r^2</md>

  <p>means distance exactly <m>r</m> from <m>(h,k)</m>.</p>

  <p>The inequality</p>

  <md>(x-h)^2+(y-k)^2&lt;r^2</md>

  <p>means distance less than <m>r</m>.  This is the inside of the circle, without the boundary.</p>

  <p>The inequality</p>

  <md>(x-h)^2+(y-k)^2\le r^2</md>

  <p>means the inside together with the boundary.</p>

  <table xml:id="tab-sec-b-2-circles-1">
    <tabular halign="left">
      <row header="yes" bottom="medium">
        <cell><strong>condition</strong></cell>
        <cell><strong>region</strong></cell>
      </row>
      <row bottom="minor">
        <cell><m>(x-h)^2+(y-k)^2=r^2</m></cell>
        <cell>circle</cell>
      </row>
      <row bottom="minor">
        <cell><m>(x-h)^2+(y-k)^2&lt;r^2</m></cell>
        <cell>inside, boundary excluded</cell>
      </row>
      <row bottom="minor">
        <cell><m>(x-h)^2+(y-k)^2\le r^2</m></cell>
        <cell>inside, boundary included</cell>
      </row>
      <row>
        <cell><m>(x-h)^2+(y-k)^2&gt;r^2</m></cell>
        <cell>outside, boundary excluded</cell>
      </row>
    </tabular>
  </table>

  <example xml:id="ex-sec-b-2-circles-3">
    <title>Example</title>
    <statement>
  <p>Describe the region</p>

  <md>(x-1)^2+(y+3)^2\le 9.</md>

  <p>The center is</p>

  <md>(1,-3),</md>

  <p>and the radius is</p>

  <md>3.</md>

  <p>Because the inequality is <m>\le</m>, the region is the closed disk: all points whose
distance from <m>(1,-3)</m> is at most <m>3</m>.</p>
    </statement>
  </example>
  </subsection>

</section>
