<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-b-4-distance-and-midpoint-formulas">
  <title>Distance and midpoint formulas</title>

  <p>Distance turns coordinate geometry into measurement.  The distance formula is just
the Pythagorean Theorem written with coordinates.</p>

  <subsection xml:id="appsubsec-distance-between-points">
    <title>Distance between two points</title>

  <p>Let</p>

  <md>P_1=(x_1,y_1)
\qquad\text{and}\qquad
P_2=(x_2,y_2).</md>

  <p>The horizontal change from <m>P_1</m> to <m>P_2</m> is</p>

  <md>x_2-x_1.</md>

  <p>The vertical change is</p>

  <md>y_2-y_1.</md>

  <p>These are the legs of a right triangle.  Therefore the distance between the points is</p>

  <md>
d(P_1,P_2)
=
\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}.
</md>

  <figure xml:id="fig-appendix-distance-formula">
    <caption>The distance formula is the Pythagorean Theorem.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.68\textwidth,
    height=0.45\textwidth,
    xmin=0, xmax=6,
    ymin=0, ymax=5,
    axis lines=left,
    xlabel={\(x\)},
    ylabel={\(y\)},
    xtick={1,5},
    ytick={1,4},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[only marks, mark=*] coordinates {(1,1) (5,4) (5,1)};
\draw[very thick] (axis cs:1,1) -- (axis cs:5,4);
\draw[dashed, thick] (axis cs:1,1) -- (axis cs:5,1);
\draw[dashed, thick] (axis cs:5,1) -- (axis cs:5,4);
\node[anchor=east] at (axis cs:1,1) {\(P_1\)};
\node[anchor=west] at (axis cs:5,4) {\(P_2\)};
\node[anchor=north] at (axis cs:3,1) {\(x_2-x_1\)};
\node[anchor=west] at (axis cs:5,2.5) {\(y_2-y_1\)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <example xml:id="ex-sec-b-4-distance-and-midpoint-formulas-1">
    <title>Example</title>
    <statement>
  <p>Find the distance between</p>

  <md>(-2,3)
\qquad\text{and}\qquad
(4,-5).</md>

  <p>Use the formula:</p>

  <md>d=\sqrt{(4-(-2))^2+(-5-3)^2}.</md>

  <p>So</p>

  <md>d=\sqrt{6^2+(-8)^2}
=
\sqrt{36+64}
=
\sqrt{100}
=
10.</md>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-midpoint">
    <title>Midpoint of a segment</title>

  <p>The midpoint is halfway in the <m>x</m>-direction and halfway in the <m>y</m>-direction.</p>

  <p>If</p>

  <md>P_1=(x_1,y_1)
\qquad\text{and}\qquad
P_2=(x_2,y_2),</md>

  <p>then the midpoint is</p>

  <md>
M=
\left(
\frac{x_1+x_2}{2},
\frac{y_1+y_2}{2}
\right).
</md>

  <example xml:id="ex-sec-b-4-distance-and-midpoint-formulas-2">
    <title>Example</title>
    <statement>
  <p>Find the midpoint of the segment joining</p>

  <md>(-2,3)
\qquad\text{and}\qquad
(4,-5).</md>

  <p>Average the <m>x</m>-coordinates:</p>

  <md>\frac{-2+4}{2}=1.</md>

  <p>Average the <m>y</m>-coordinates:</p>

  <md>\frac{3+(-5)}{2}=-1.</md>

  <p>So the midpoint is</p>

  <md>
(1,-1).
</md>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="appsubsec-locus-perpendicular-bisector">
    <title>A locus example: perpendicular bisectors</title>

  <p>A <em>locus</em> is a set of points satisfying a condition.</p>

  <p>For example, the perpendicular bisector of a segment is the set of all points
equidistant from the segment’s endpoints.</p>

  <example xml:id="ex-sec-b-4-distance-and-midpoint-formulas-3">
    <title>Example</title>
    <statement>
  <p>Find the set of points <m>(x,y)</m> equidistant from</p>

  <md>A=(0,0)</md>

  <p>and</p>

  <md>B=(4,2).</md>

  <p>Equidistant means</p>

  <md>d((x,y),A)=d((x,y),B).</md>

  <p>Using squared distances avoids square roots:</p>

  <md>x^2+y^2=(x-4)^2+(y-2)^2.</md>

  <p>Expand the right side:</p>

  <md>x^2+y^2=x^2-8x+16+y^2-4y+4.</md>

  <p>Cancel <m>x^2+y^2</m> from both sides:</p>

  <md>0=-8x-4y+20.</md>

  <p>Thus</p>

  <md>8x+4y=20.</md>

  <p>Divide by <m>4</m>:</p>

  <md>
2x+y=5.
</md>

  <p>This line is the perpendicular bisector of the segment from <m>A</m> to <m>B</m>.</p>

  <note xml:id="note-sec-b-4-distance-and-midpoint-formulas-22">
    <title>Why squared distances are allowed</title>
      <p>Distance is nonnegative.  If two distances are equal, then their squares are equal.
Conversely, if two nonnegative distances have equal squares, then the distances are
equal.</p>
  </note>
    </statement>
  </example>
  </subsection>

</section>
