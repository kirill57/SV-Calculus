<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-6-2-local-and-absolute-extrema">
  <title>Local and absolute extrema</title>

  <p>The sign chart for <m>x^3-3x</m> showed a graph that rises, then falls, then rises.
At the point where rising changes to falling, the graph has a high point nearby.
At the point where falling changes to rising, the graph has a low point nearby.</p>

  <p>Those points are called extrema.</p>

  <subsection xml:id="subsec-local-max-local-min">
    <title>Local maximum and local minimum</title>

  <p>A high point nearby is not necessarily the highest point everywhere.  A mountain
can have many peaks.  A small peak is still a peak if the graph is lower on both
sides nearby.</p>

  <note xml:id="note-sec-6-2-local-and-absolute-extrema-55">
    <title>Local extrema</title>
      <p>A function <m>f</m> has a <em>local maximum</em> at <m>c</m> if</p>
        <md>f(c)\ge f(x)</md>
      <p>for all <m>x</m> sufficiently close to <m>c</m>.</p>
      <p>A function <m>f</m> has a <em>local minimum</em> at <m>c</m> if</p>
        <md>f(c)\le f(x)</md>
      <p>for all <m>x</m> sufficiently close to <m>c</m>.</p>
  </note>

  <p>The phrase “sufficiently close” means that we only compare <m>f(c)</m> with nearby
values of the function.  Local extrema are neighborhood ideas.</p>

  <p>Now compare this with the global idea.</p>

  <note xml:id="note-sec-6-2-local-and-absolute-extrema-56">
    <title>Absolute extrema</title>
      <p>A function <m>f</m> has an <em>absolute maximum</em> at <m>c</m> on a set <m>D</m> if</p>
        <md>f(c)\ge f(x)</md>
      <p>for every <m>x\in D</m>.</p>
      <p>A function <m>f</m> has an <em>absolute minimum</em> at <m>c</m> on a set <m>D</m> if</p>
        <md>f(c)\le f(x)</md>
      <p>for every <m>x\in D</m>.</p>
  </note>

  <p>Absolute extrema compare against the whole domain under discussion.  Local extrema
compare only nearby.</p>


  <exercise xml:id="checkpoint-6-2-local-versus-absolute">
        <title>Why is a local maximum not automatically an absolute maximum?</title>
        <statement>
          <p>
            A local maximum only asks that nearby values be no larger. An absolute maximum asks that every value on the domain under discussion be no larger. In your own words, why can a nearby peak lose to an endpoint, and what is the difference between a neighborhood comparison and a whole-domain comparison?
          </p>
        </statement>
        <hint>
          <p>
            A mountain can have many peaks. The highest point on a trail may sit at the end of the trail rather than at a nearby summit.
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-6-2-local-and-absolute-extrema-1">
    <title>Example</title>
    <statement>
  <p>Let</p>

  <md>f(x)=x^3-6x^2+9x+2,
\qquad \frac12\le x\le 5.</md>

  <p>Then</p>

  <md>f'(x)=3x^2-12x+9=3(x-1)(x-3).</md>

  <p>The derivative changes from positive to negative at <m>x=1</m>, so <m>x=1</m> is a local
maximum.  The derivative changes from negative to positive at <m>x=3</m>, so <m>x=3</m>
is a local minimum.</p>

  <p>But the absolute maximum on the interval occurs at the endpoint <m>x=5</m>.  A
local maximum does not have to be the absolute maximum.</p>

  <figure xml:id="fig-local-vs-absolute-extrema">
    <caption>A local maximum may not be the absolute maximum.  Endpoints must be checked.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.82\textwidth,
    height=0.50\textwidth,
    xmin=0.3, xmax=5.2,
    ymin=0, ymax=24,
    axis lines=left,
    xlabel={\(x\)},
    ylabel={\(f(x)\)},
    xtick={0.5,1,3,5},
    xticklabels={\(\frac12\),\(1\),\(3\),\(5\)},
    ytick={0,5,10,15,20},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=0.5:5, samples=200] {x^3-6*x^2+9*x+2};
\addplot[only marks, mark=*] coordinates {(0.5,5.125) (1,6) (3,2) (5,22)};
\node[anchor=south] at (axis cs:1,6) {local max};
\node[anchor=north] at (axis cs:3,2) {local min};
\node[anchor=west] at (axis cs:5,22) {absolute max};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-fermats-theorem-extrema">
    <title>Fermat’s theorem</title>

  <p>At a smooth local maximum, the tangent line is horizontal.  The graph rises into
the point and falls away from it.  The left-side slopes and right-side slopes force
the tangent slope to be <m>0</m>.</p>

  <p>At a smooth local minimum, the same thing happens upside down.</p>

  <theorem xml:id="thm-sec-6-2-local-and-absolute-extrema-57">
    <title>Fermat’s theorem</title>
    <statement>
      <p>Suppose <m>f</m> has a local maximum or local minimum at an interior point <m>c</m>.  If
<m>f</m> is differentiable at <m>c</m>, then</p>
        <md>f'(c)=0.</md>
    </statement>
  </theorem>

  <paragraphs>
    <title>Proof</title>

  <p>We prove the local maximum case.  The local minimum case is similar.</p>

  <p>Assume <m>f</m> has a local maximum at <m>c</m>.  Then for all <m>x</m> close enough to <m>c</m>,</p>

  <md>f(x)\le f(c).</md>

  <p>For <m>h&gt;0</m> small enough,</p>

  <md>f(c+h)-f(c)\le 0.</md>

  <p>Dividing by the positive number <m>h</m>, we get</p>

  <md>\frac{f(c+h)-f(c)}{h}\le 0.</md>

  <p>So the right-hand difference quotients are less than or equal to <m>0</m>.</p>

  <p>For <m>h&lt;0</m> small enough, the numerator still satisfies</p>

  <md>f(c+h)-f(c)\le 0,</md>

  <p>but now the denominator <m>h</m> is negative.  Dividing by a negative number reverses
the inequality:</p>

  <md>\frac{f(c+h)-f(c)}{h}\ge 0.</md>

  <p>So the left-hand difference quotients are greater than or equal to <m>0</m>.</p>

  <p>If <m>f'(c)</m> exists, then the left-hand and right-hand limits of these quotients must
agree.  The only number that can be both</p>

  <md>\le 0</md>

  <p>and</p>

  <md>\ge 0</md>

  <p>is <m>0</m>.  Therefore</p>

  <md>f'(c)=0.</md>
  </paragraphs>

  <exercise xml:id="checkpoint-6-2-fermat-necessary">
        <title>Why does Fermat’s theorem give a necessary condition, not a sufficient one?</title>
        <statement>
          <p>
            If a differentiable interior point is a local extremum, then <m>f'(c)=0</m>. In your own words, why does that force a horizontal tangent, and why does the converse fail: why can <m>f'(c)=0</m> occur without a local max or min?
          </p>
        </statement>
        <hint>
          <p>
            The theorem says every smooth interior extremum is critical. It does not say every critical number is an extremum. Endpoints and corners sit outside the hypotheses as well.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-fermat-hypotheses-matter">
    <title>Why Fermat’s hypotheses matter</title>

  <p>Fermat’s theorem is short, but every condition is doing work.</p>

  <paragraphs>
    <title>Endpoint warning</title>

  <p>Let</p>

  <md>f(x)=x</md>

  <p>on the interval</p>

  <md>[0,1].</md>

  <p>The function has an absolute minimum at <m>x=0</m> and an absolute maximum at <m>x=1</m>.
But</p>

  <md>f'(x)=1</md>

  <p>everywhere.</p>

  <p>There is no contradiction.  Fermat’s theorem applies to interior points, not endpoints.
At an endpoint, the graph can be highest or lowest simply because the interval stops.</p>
  </paragraphs>

  <paragraphs>
    <title>Corner warning</title>

  <p>Let</p>

  <md>f(x)=|x|.</md>

  <p>This function has a local minimum at <m>x=0</m>.  But</p>

  <md>f'(0)</md>

  <p>does not exist.  Again, Fermat’s theorem is not contradicted.  The theorem says that
if the local extremum occurs at a differentiable interior point, then the derivative
is <m>0</m>.</p>

  <p>Corners can have extrema without horizontal tangents.</p>
  </paragraphs>

  <paragraphs>
    <title>Critical number warning</title>

  <p>The converse of Fermat’s theorem is false.</p>

  <p>Fermat’s theorem says:</p>

  <md>\text{differentiable local extremum}
\quad\Longrightarrow\quad
f'(c)=0.</md>

  <p>It does not say:</p>

  <md>f'(c)=0
\quad\Longrightarrow\quad
\text{local extremum}.</md>

  <p>For example,</p>

  <md>f(x)=x^3</md>

  <p>has</p>

  <md>f'(x)=3x^2,</md>

  <p>so</p>

  <md>f'(0)=0.</md>

  <p>But <m>x^3</m> is increasing through <m>0</m>.  It has neither a local maximum nor a local
minimum there.</p>

  <md>
\text{Every differentiable interior extremum is critical, but not every critical number is an extremum.}
</md>
  </paragraphs>
  </subsection>

  <subsection xml:id="subsec-first-derivative-test">
    <title>The first derivative test</title>

  <p>A critical number becomes more meaningful when we look at the sign of <m>f'</m> on
both sides.</p>

  <note xml:id="note-sec-6-2-local-and-absolute-extrema-58">
    <title>First derivative test</title>
      <p>Suppose <m>c</m> is a critical number of <m>f</m>, and <m>f</m> is continuous at <m>c</m>.</p>
        <md>
    <mrow>\begin{array}{c|c|c}
\text{sign of } f' \text{ before } c \amp \text{sign of } f' \text{ after } c \amp \text{conclusion}</mrow>
    <mrow>\hline
+ \amp - \amp \text{local maximum at } c</mrow>
    <mrow>- \amp + \amp \text{local minimum at } c</mrow>
    <mrow>+ \amp + \amp \text{no local extremum forced}</mrow>
    <mrow>- \amp - \amp \text{no local extremum forced}
\end{array}</mrow>
  </md>
  </note>

  <p>The reason is simple.  If the function increases before <m>c</m> and decreases after
<m>c</m>, then <m>c</m> is a high point nearby.  If it decreases before <m>c</m> and increases
after <m>c</m>, then <m>c</m> is a low point nearby.</p>

  <p>This test depends on the first derivative sign rule, whose full proof comes from the
Mean Value Theorem.  The picture is already clear; the next theorem will make it
secure.</p>


  <exercise xml:id="checkpoint-6-2-first-derivative-test">
        <title>Why does the first derivative test look for a sign change?</title>
        <statement>
          <p>
            If <m>f'</m> changes from positive to negative at a critical number, the graph has a local maximum there. In your own words, why is the change of sign the essential evidence, rather than the mere fact that <m>f'(c)=0</m>?
          </p>
        </statement>
        <hint>
          <p>
            Rising into a point and falling away from it makes a nearby high point. A horizontal tangent that does not change the direction of travel does not.
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-6-2-local-and-absolute-extrema-6">
    <title>Example</title>
    <statement>
  <p>For</p>

  <md>f(x)=x^3-3x,</md>

  <p>we found</p>

  <md>f'(x)=3(x-1)(x+1).</md>

  <p>The sign chart is</p>

  <md>
    <mrow>\begin{array}{c|ccc}
x \amp (-\infty,-1) \amp (-1,1) \amp (1,\infty)</mrow>
    <mrow>\hline
f'(x) \amp + \amp - \amp +
\end{array}</mrow>
  </md>

  <p>So <m>f</m> has a local maximum at</p>

  <md>x=-1</md>

  <p>and a local minimum at</p>

  <md>x=1.</md>

  <p>The values are</p>

  <md>f(-1)=2,
\qquad
f(1)=-2.</md>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-closed-interval-method">
    <title>Absolute extrema and the closed interval method</title>

  <p>Local extrema are nearby comparisons.  Absolute extrema are whole-domain
comparisons.  To find them, we need a guarantee that they exist.</p>

  <p>The guarantee comes from continuity on a closed, bounded interval.</p>

  <theorem xml:id="thm-sec-6-2-local-and-absolute-extrema-59">
    <title>Extreme Value Theorem</title>
    <statement>
      <p>If <m>f</m> is continuous on a closed interval <m>[a,b]</m>, then <m>f</m> has an absolute
maximum and an absolute minimum on <m>[a,b]</m>.</p>
    </statement>
  </theorem>

  <p>This theorem is a deep fact about the real line.  It rests on the idea that the real
line has no gaps.  For now, its meaning is more important than its proof:</p>

  <md>\text{a continuous graph on a closed finite interval cannot escape its highest and lowest values.}</md>

  <p>The hypotheses matter.</p>

  <paragraphs>
    <title>Open interval warning</title>

  <p>The function</p>

  <md>f(x)=x</md>

  <p>on</p>

  <md>(0,1)</md>

  <p>has no absolute maximum and no absolute minimum.  Its values get close to <m>1</m>,
but never equal <m>1</m>.  They get close to <m>0</m>, but never equal <m>0</m>.</p>

  <p>The interval is not closed.</p>
  </paragraphs>

  <paragraphs>
    <title>Unbounded interval warning</title>

  <p>The function</p>

  <md>f(x)=x</md>

  <p>on</p>

  <md>[0,\infty)</md>

  <p>has an absolute minimum at <m>0</m>, but no absolute maximum.</p>

  <p>The interval is not bounded.</p>
  </paragraphs>

  <paragraphs>
    <title>Continuity warning</title>

  <p>Define</p>

  <md>
    <mrow>f(x)=
\begin{cases}
x, \amp 0\le x&lt;1,</mrow>
    <mrow>0, \amp x=1.
\end{cases}</mrow>
  </md>

  <p>on <m>[0,1]</m>.  This function has no absolute maximum.  The values approach <m>1</m>,
but the function never reaches <m>1</m>.  At the endpoint, it jumps down to <m>0</m>.</p>

  <p>The interval is closed and bounded, but the function is not continuous.</p>
  </paragraphs>
  </subsection>

  <subsection xml:id="subsec-closed-interval-method-procedure">
    <title>The closed interval method</title>

  <p>Fermat’s theorem tells us where an interior extremum can occur.  The Extreme Value
Theorem tells us that absolute extrema exist on a closed interval.  Together they
give a practical method.</p>

  <note xml:id="note-sec-6-2-local-and-absolute-extrema-60">
    <title>Closed interval method</title>
      <p>To find the absolute maximum and minimum of a continuous function <m>f</m> on
<m>[a,b]</m>:</p>
        <md>
    <mrow>\begin{array}{ll}
\text{1.} \amp \text{Find all critical numbers of } f \text{ in } (a,b).</mrow>
    <mrow>\text{2.} \amp \text{Evaluate } f \text{ at those critical numbers.}</mrow>
    <mrow>\text{3.} \amp \text{Evaluate } f(a) \text{ and } f(b).</mrow>
    <mrow>\text{4.} \amp \text{Compare the values.  The largest is the absolute maximum value.}</mrow>
    <mrow>\amp \text{The smallest is the absolute minimum value.}
\end{array}</mrow>
  </md>
  </note>

  <p>Endpoints are included because Fermat’s theorem does not apply there.  An endpoint
can be the best or worst value simply because the graph stops.</p>


  <exercise xml:id="checkpoint-6-2-closed-interval-endpoints">
        <title>Why must the closed interval method check endpoints?</title>
        <statement>
          <p>
            A continuous function on <m>[a,b]</m> attains its absolute maximum and minimum. In your own words, why do those extreme values have to occur at a critical number or at an endpoint, and why can an open or unbounded interval fail to have an absolute extremum at all?
          </p>
        </statement>
        <hint>
          <p>
            Fermat’s theorem only constrains interior differentiable extrema. An endpoint can be best or worst simply because the interval stops.
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-6-2-local-and-absolute-extrema-10">
    <title>Example</title>
    <statement>
  <p>Find the absolute maximum and minimum of</p>

  <md>f(x)=x^3-6x^2+9x+2</md>

  <p>on</p>

  <md>\left[\frac12,5\right].</md>

  <p>First compute the derivative:</p>

  <md>f'(x)=3x^2-12x+9.</md>

  <p>Factor:</p>

  <md>f'(x)=3(x-1)(x-3).</md>

  <p>So the critical numbers inside the interval are</p>

  <md>x=1
\qquad\text{and}\qquad
x=3.</md>

  <p>Now evaluate <m>f</m> at the critical numbers and endpoints.</p>

  <md>
    <mrow>\begin{array}{c|c}
x \amp f(x)</mrow>
    <mrow>\hline
\frac12 \amp \frac{41}{8}</mrow>
    <mrow>
1 \amp 6</mrow>
    <mrow>
3 \amp 2</mrow>
    <mrow>
5 \amp 22
\end{array}</mrow>
  </md>

  <p>The smallest value is</p>

  <md>2,</md>

  <p>which occurs at</p>

  <md>x=3.</md>

  <p>The largest value is</p>

  <md>22,</md>

  <p>which occurs at</p>

  <md>x=5.</md>

  <p>Therefore <m>f</m> has absolute minimum value <m>2</m> at <m>x=3</m>, and absolute maximum
value <m>22</m> at <m>x=5</m>.</p>

  <p>Notice what happened.  The local maximum at <m>x=1</m> is not the absolute maximum.
The endpoint <m>x=5</m> wins.</p>

  <md>
\text{On a closed interval, always check endpoints.}
</md>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-extrema-hook-mvt">
    <title>Where this points</title>

  <p>We have used the derivative to read the shape of a function:</p>

  <md>f'&gt;0 \quad\Rightarrow\quad \text{increasing},</md>

  <md>f'&lt;0 \quad\Rightarrow\quad \text{decreasing},</md>

  <p>and</p>

  <md>\text{smooth local extremum} \quad\Rightarrow\quad f'=0.</md>

  <p>Fermat’s theorem was proved directly from the derivative definition.  But the first
derivative sign rule still needs its real proof.</p>

  <p>The missing idea is surprisingly simple.  If a car’s average velocity over a trip is
<m>50</m> miles per hour, then at some instant its speed should have been <m>50</m> miles
per hour.  If a smooth graph starts and ends at the same height, then somewhere in
between it should have a horizontal tangent.</p>

  <p>That idea is Rolle’s Theorem, and just beyond it is the Mean Value Theorem.</p>
  </subsection>



</section>
