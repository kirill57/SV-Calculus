<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-5-1-the-algebra-of-derivatives">
  <title>The algebra of derivatives</title>

  <p>Some ways of building functions are gentle.  Multiplying by a constant, adding two functions, and subtracting two functions do not create any hidden interaction.  Their derivatives behave exactly as we hope.</p>

  <p>This is the first algebra of derivatives.</p>

  <subsection xml:id="subsec-constant-multiple-rule-ch5">
    <title>Scaling a changing quantity</title>

  <p>Suppose a particle has position</p>

  <md>s(t)=t^2</md>

  <p>meters after <m>t</m> seconds.  Its velocity is</p>

  <md>s'(t)=2t</md>

  <p>meters/second.</p>

  <p>If we measure the same position in centimeters, the position function becomes</p>

  <md>S(t)=100s(t)=100t^2.</md>

  <p>Then</p>

  <md>S'(t)=200t.</md>

  <p>This is exactly</p>

  <md>100s'(t)=100(2t).</md>

  <p>Changing meters to centimeters multiplies every position by <m>100</m>.  It also multiplies every velocity by <m>100</m>.  Scaling the output scales the rate of change by the same factor.</p>

  <note xml:id="note-sec-5-1-the-algebra-of-derivatives-58">
    <title>Constant multiple rule</title>
      <p>If <m>f</m> is differentiable at <m>x</m>, and <m>c</m> is a constant, then</p>
        <md>
\frac{d}{dx}\bigl(c f(x)\bigr)=c f'(x).
</md>
  </note>

  <paragraphs>
    <title>Proof</title>

  <p>By the definition of derivative,</p>

  <md>\frac{d}{dx}\bigl(c f(x)\bigr)
=
\lim_{h\to0}\frac{c f(x+h)-c f(x)}{h}.</md>

  <p>Factor out <m>c</m>:</p>

  <md>\lim_{h\to0}c\frac{f(x+h)-f(x)}{h}
=
c\lim_{h\to0}\frac{f(x+h)-f(x)}{h}.</md>

  <p>Therefore</p>

  <md>\frac{d}{dx}\bigl(c f(x)\bigr)=c f'(x).</md>
  </paragraphs>


  <exercise xml:id="checkpoint-5-1-scaling-scales-the-rate">
        <title>Why does scaling an output scale its rate by the same factor?</title>
        <statement>
          <p>
            Measuring the same motion in centimeters instead of meters multiplies every position by <m>100</m> and every velocity by <m>100</m>. In your own words, why must the constant multiple rule look like that, and why is this the same idea as pulling a constant out of a difference quotient?
          </p>
        </statement>
        <hint>
          <p>
            If every output is multiplied by <m>c</m>, every change in output is multiplied by <m>c</m> as well. The input step <m>h</m> is unchanged.
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-5-1-the-algebra-of-derivatives-2">
    <title>Example</title>
    <statement>
  <p>Since</p>

  <md>\frac{d}{dx}x^5=5x^4,</md>

  <p>we have</p>

  <md>\frac{d}{dx}(7x^5)=7\cdot5x^4=35x^4.</md>

  <p>The coefficient <m>7</m> simply comes along for the ride.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-constants-disappear-ch5">
    <title>Constants disappear</title>

  <p>Adding a constant shifts a graph up or down.  It does not change slope.</p>

  <p>For example, compare</p>

  <md>f(x)=x^2</md>

  <p>and</p>

  <md>g(x)=x^2+5.</md>

  <p>The graph of <m>g</m> is the graph of <m>f</m> shifted upward by <m>5</m>.  The two graphs have the same tangent slopes at corresponding <m>x</m>-values.</p>

  <p>Algebraically,</p>

  <md>g'(x)
=
\frac{d}{dx}(x^2+5)
=
2x+0
=
2x.</md>

  <p>So</p>

  <md>
\frac{d}{dx}\bigl(f(x)+C\bigr)=f'(x)
</md>

  <p>for any constant <m>C</m>.</p>

  <p>This is not a new rule.  It is the sum rule together with</p>

  <md>\frac{d}{dx}(C)=0.</md>

  <p>But it is worth remembering as a sentence:</p>

  <md>
\text{The derivative records change, so it forgets vertical shifts.}
</md>

  <exercise xml:id="checkpoint-5-1-constants-disappear">
        <title>Why do added constants disappear under differentiation?</title>
        <statement>
          <p>
            The graphs of <m>x^2</m> and <m>x^2+5</m> have the same tangent slopes. In your own words, why does the derivative record change and therefore forget vertical shifts?
          </p>
        </statement>
        <hint>
          <p>
            A vertical shift does not change rise over run. The sum rule together with <m>(C)'=0</m> is the algebraic form of that sentence.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-sum-rule-ch5">
    <title>Adding changing quantities</title>

  <p>Suppose two quantities change at the same time:</p>

  <md>A(x)=x^2,
\qquad
B(x)=x^3.</md>

  <p>Their total is</p>

  <md>T(x)=A(x)+B(x)=x^2+x^3.</md>

  <p>At <m>x=2</m>,</p>

  <md>A'(2)=4,
\qquad
B'(2)=12.</md>

  <p>So we expect the total to be changing at rate</p>

  <md>4+12=16.</md>

  <p>Check directly:</p>

  <md>T'(x)
=
\lim_{h\to0}\frac{(x+h)^2+(x+h)^3-(x^2+x^3)}{h}.</md>

  <p>Split the numerator:</p>

  <md>T'(x)
=
\lim_{h\to0}
\left[
\frac{(x+h)^2-x^2}{h}
+
\frac{(x+h)^3-x^3}{h}
\right].</md>

  <p>The two pieces approach</p>

  <md>2x
\qquad\text{and}\qquad
3x^2.</md>

  <p>So</p>

  <md>T'(x)=2x+3x^2.</md>

  <p>At <m>x=2</m>,</p>

  <md>T'(2)=4+12=16.</md>

  <p>The rate of change of a sum is the sum of the rates of change.</p>

  <note xml:id="note-sec-5-1-the-algebra-of-derivatives-59">
    <title>Sum and difference rules</title>
      <p>If <m>f</m> and <m>g</m> are differentiable at <m>x</m>, then</p>
        <md>
\frac{d}{dx}\bigl(f(x)+g(x)\bigr)=f'(x)+g'(x)
</md>
      <p>and</p>
        <md>
\frac{d}{dx}\bigl(f(x)-g(x)\bigr)=f'(x)-g'(x).
</md>
  </note>

  <paragraphs>
    <title>Proof of the sum rule</title>

  <p>Start from the difference quotient:</p>

  <md>\frac{(f+g)(x+h)-(f+g)(x)}{h}.</md>

  <p>This equals</p>

  <md>\frac{f(x+h)+g(x+h)-f(x)-g(x)}{h}.</md>

  <p>Regroup:</p>

  <md>\frac{f(x+h)-f(x)}{h}
+
\frac{g(x+h)-g(x)}{h}.</md>

  <p>Taking the limit gives</p>

  <md>(f+g)'(x)=f'(x)+g'(x).</md>

  <p>The difference rule follows by applying the sum rule to</p>

  <md>f+(-1)g.</md>
  </paragraphs>
  </subsection>

  <subsection xml:id="subsec-linearity-of-derivative">
    <title>Linearity of the derivative</title>

  <p>The constant multiple rule and the sum rule combine into one compact formula.</p>

  <note xml:id="note-sec-5-1-the-algebra-of-derivatives-60">
    <title>Linearity of differentiation</title>
      <p>If <m>f</m> and <m>g</m> are differentiable at <m>x</m>, and <m>a,b</m> are constants, then</p>
        <md>
\frac{d}{dx}\bigl(a f(x)+b g(x)\bigr)
=
a f'(x)+b g'(x).
</md>
  </note>

  <p>This property is called <em>linearity</em>.  The word means that differentiation respects addition and constant scaling.</p>

  <p>There is also a local-linear reason for the rule.  If <m>f</m> and <m>g</m> are differentiable at <m>x</m>, then for small <m>h</m>,</p>

  <md>f(x+h)=f(x)+f'(x)h+\text{small error},</md>

  <p>and</p>

  <md>g(x+h)=g(x)+g'(x)h+\text{small error}.</md>

  <p>Then</p>

  <md>a f(x+h)+b g(x+h)</md>

  <p>is approximately</p>

  <md>a\bigl(f(x)+f'(x)h\bigr)
+
b\bigl(g(x)+g'(x)h\bigr).</md>

  <p>Regroup:</p>

  <md>a f(x)+b g(x)
+
\bigl(a f'(x)+b g'(x)\bigr)h.</md>

  <p>So the coefficient of <m>h</m>, which is the derivative, is</p>

  <md>a f'(x)+b g'(x).</md>

  <p>The rule is not merely algebraic convenience.  It says that local linear approximations add the way ordinary lines add.</p>

  <exercise xml:id="checkpoint-5-1-linearity-adds-lines">
        <title>Why is differentiation linear?</title>
        <statement>
          <p>
            The rate of change of a sum is the sum of the rates, and local linear approximations add the way ordinary lines add. In your own words, why do those two statements belong together, and why do they still not handle a product?
          </p>
        </statement>
        <hint>
          <p>
            If two quantities change independently, their total change is the sum of the two changes. A product has an interaction: each factor helps change the product.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-polynomial-derivatives-ch5">
    <title>Derivatives of polynomials</title>

  <p>A polynomial is built from powers of <m>x</m>, constants, constant multiples, and sums.  The rules above therefore give the derivative of every polynomial.</p>

  <p>If</p>

  <md>p(x)=a_nx^n+a_{n-1}x^{n-1}+\cdots+a_2x^2+a_1x+a_0,</md>

  <p>then</p>

  <md>
p'(x)
=
na_nx^{n-1}+(n-1)a_{n-1}x^{n-2}+\cdots+2a_2x+a_1.
</md>

  <p>The constant term <m>a_0</m> disappears.</p>

  <example xml:id="ex-sec-5-1-the-algebra-of-derivatives-4">
    <title>Example</title>
    <statement>
  <p>Differentiate</p>

  <md>p(x)=4x^7-3x^4+2x-8.</md>

  <p>Term by term,</p>

  <md>p'(x)=4\cdot7x^6-3\cdot4x^3+2.</md>

  <p>So</p>

  <md>
p'(x)=28x^6-12x^3+2.
</md>
    </statement>
  </example>

  <example xml:id="ex-sec-5-1-the-algebra-of-derivatives-5">
    <title>a tangent line</title>
    <statement>
  <p>Let</p>

  <md>p(x)=x^3-3x+1.</md>

  <p>Find the tangent line at <m>x=2</m>.</p>

  <p>First find the point:</p>

  <md>p(2)=8-6+1=3.</md>

  <p>So the point is</p>

  <md>(2,3).</md>

  <p>Now find the derivative:</p>

  <md>p'(x)=3x^2-3.</md>

  <p>At <m>x=2</m>,</p>

  <md>p'(2)=3(2)^2-3=12-3=9.</md>

  <p>The tangent line has slope <m>9</m> and passes through <m>(2,3)</m>.  Therefore</p>

  <md>y-3=9(x-2),</md>

  <p>or</p>

  <md>
y=9x-15.
</md>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-linearity-not-enough">
    <title>Why these rules are not enough</title>

  <p>Linearity handles expressions such as</p>

  <md>4x^7-3x^4+2x-8.</md>

  <p>It does not handle products, quotients, or compositions without new ideas.</p>

  <p>For example,</p>

  <md>F(x)=(x^2+1)(x^3-5)</md>

  <p>is a product.  We could expand it:</p>

  <md>F(x)=x^5+x^3-5x^2-5,</md>

  <p>and then differentiate:</p>

  <md>F'(x)=5x^4+3x^2-10x.</md>

  <p>That works here because the product expands into a polynomial.  But this strategy will not be pleasant for every product, and it will not help at all for products such as</p>

  <md>x^2\sqrt{x+1}</md>

  <p>or, later,</p>

  <md>x^2\sin x.</md>

  <p>A product changes in a different way from a sum.  If one side of a rectangle changes, the area changes.  If both sides change, the area changes for two reasons at once.</p>

  <p>That is where the product rule comes from.</p>

  <exercise xml:id="checkpoint-5-1-why-not-enough">
        <title>Why are sum and scaling not enough?</title>
        <statement>
          <p>
            Linearity differentiates every polynomial, but not every useful function is a polynomial. In your own words, why does expanding a product fail as a general strategy, and what new kind of change does a product introduce?
          </p>
        </statement>
        <hint>
          <p>
            Expansion is unpleasant or impossible for many products. If both sides of a rectangle change, the area changes for two reasons at once.
          </p>
        </hint>
      </exercise>

  </subsection>



</section>
