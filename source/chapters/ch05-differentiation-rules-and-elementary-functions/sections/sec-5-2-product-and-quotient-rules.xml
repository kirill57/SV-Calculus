<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-5-2-product-and-quotient-rules">
  <title>Product and quotient rules</title>

  <p>Sums are easy because changes add.  Products are more interesting because the factors interact.</p>

  <p>If a rectangle has width <m>w</m> and height <m>h</m>, then its area is</p>

  <md>A=wh.</md>

  <p>If both <m>w</m> and <m>h</m> change, the area changes because the width changes and because the height changes.  The product rule keeps track of both effects.</p>

  <subsection xml:id="subsec-why-product-rule-two-terms">
    <title>Why the product rule has two terms</title>

  <p>Let</p>

  <md>w(t)=t
\qquad\text{and}\qquad
H(t)=t^2.</md>

  <p>Think of <m>w(t)</m> as the width of a rectangle and <m>H(t)</m> as its height.  Then the area is</p>

  <md>A(t)=w(t)H(t)=t\cdot t^2=t^3.</md>

  <p>So</p>

  <md>A'(t)=3t^2.</md>

  <p>Now compare this with the rates of the two factors:</p>

  <md>w'(t)=1,
\qquad
H'(t)=2t.</md>

  <p>If we incorrectly multiplied the derivatives, we would get</p>

  <md>w'(t)H'(t)=1\cdot2t=2t,</md>

  <p>which is not <m>3t^2</m>.</p>

  <p>The missing idea is that each factor helps change the product:</p>

  <md>w'(t)H(t)+w(t)H'(t)
=
1\cdot t^2+t\cdot2t
=
3t^2.</md>

  <p>That matches <m>A'(t)</m>.</p>

  <p>The derivative of a product is not the product of the derivatives.  It is a sum of two effects.</p>

  <md>
\text{change in product}
=
\text{change in first factor}
+
\text{change in second factor}.
</md>

  <p>More precisely, each change is weighted by the other factor.</p>

  <exercise xml:id="checkpoint-5-2-two-terms-not-product">
        <title>Why does the product rule have two terms?</title>
        <statement>
          <p>
            The derivative of a product is not the product of the derivatives. In your own words, why must each factor contribute its own change, weighted by the other factor, and what do the two strips in the area picture represent?
          </p>
        </statement>
        <hint>
          <p>
            If only the width changes, the area still changes. If only the height changes, the area still changes. Both can happen at once.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-product-rule-area">
    <title>Product rule from changing area</title>

  <p>For a moment, suppose <m>f(x)</m> and <m>g(x)</m> are positive, so that we can picture</p>

  <md>f(x)g(x)</md>

  <p>as the area of a rectangle.</p>

  <p>When <m>x</m> changes to <m>x+h</m>, the side lengths change by</p>

  <md>\Delta f=f(x+h)-f(x),</md>

  <p>and</p>

  <md>\Delta g=g(x+h)-g(x).</md>

  <p>The new rectangle has side lengths</p>

  <md>f(x)+\Delta f
\qquad\text{and}\qquad
g(x)+\Delta g.</md>

  <p>Its new area is</p>

  <md>\bigl(f(x)+\Delta f\bigr)\bigl(g(x)+\Delta g\bigr).</md>

  <p>The change in area is</p>

  <md>\bigl(f+\Delta f\bigr)\bigl(g+\Delta g\bigr)-fg.</md>

  <p>Expanding gives</p>

  <md>fg+g\Delta f+f\Delta g+\Delta f\,\Delta g-fg.</md>

  <p>So</p>

  <md>\Delta(fg)=g\Delta f+f\Delta g+\Delta f\,\Delta g.</md>

  <figure xml:id="fig-product-rule-area">
    <caption>When both factors change, a product changes by two main strips and a small corner.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}[scale=1.05, every node/.style={font=\small}]
  % Dimensions
  \def\f{3.0}
  \def\g{2.0}
  \def\df{1.0}
  \def\dg{0.75}

  % Original rectangle
  \draw[fill=gray!12, thick] (0,0) rectangle (\f,\g);
  \node at (1.5,1.0) {\(fg\)};

  % Right strip
  \draw[fill=gray!25, thick] (\f,0) rectangle ({\f+\df},\g);
  \node[rotate=90] at ({\f+0.5*\df},1.0) {\(g\,\Delta f\)};

  % Top strip
  \draw[fill=gray!25, thick] (0,\g) rectangle (\f,{\g+\dg});
  \node at (1.5,{\g+0.38}) {\(f\,\Delta g\)};

  % Corner
  \draw[fill=gray!40, thick] (\f,\g) rectangle ({\f+\df},{\g+\dg});
  \node at ({\f+0.5*\df},{\g+0.38}) {\(\Delta f\,\Delta g\)};

  % Labels
  \draw[&lt;-&gt;] (0,-0.35) -- (\f,-0.35);
  \node[below] at (1.5,-0.35) {\(f\)};

  \draw[&lt;-&gt;] (\f,-0.35) -- ({\f+\df},-0.35);
  \node[below] at ({\f+0.5*\df},-0.35) {\(\Delta f\)};

  \draw[&lt;-&gt;] (-0.35,0) -- (-0.35,\g);
  \node[left] at (-0.35,1.0) {\(g\)};

  \draw[&lt;-&gt;] (-0.35,\g) -- (-0.35,{\g+\dg});
  \node[left] at (-0.35,{\g+0.5*\dg}) {\(\Delta g\)};
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <p>Now divide by <m>h</m>:</p>

  <md>\frac{\Delta(fg)}{h}
=
g\frac{\Delta f}{h}
+
f\frac{\Delta g}{h}
+
\Delta f\frac{\Delta g}{h}.</md>

  <p>As <m>h\to0</m>,</p>

  <md>\frac{\Delta f}{h}\to f'(x),
\qquad
\frac{\Delta g}{h}\to g'(x).</md>

  <p>Also <m>\Delta f\to0</m>, because differentiability implies continuity.  Therefore the last term</p>

  <md>\Delta f\frac{\Delta g}{h}</md>

  <p>approaches <m>0\cdot g'(x)=0</m>.</p>

  <p>The two main strips survive.  The tiny corner disappears in the limiting rate.</p>

  <p>This gives the product rule.</p>

  <note xml:id="note-sec-5-2-product-and-quotient-rules-111">
    <title>Product rule</title>
      <p>If <m>f</m> and <m>g</m> are differentiable at <m>x</m>, then</p>
        <md>
\frac{d}{dx}\bigl(f(x)g(x)\bigr)
=
f'(x)g(x)+f(x)g'(x).
</md>
      <p>In prime notation,</p>
        <md>
(fg)'=f'g+fg'.
</md>
  </note>

  <paragraphs>
    <title>Proof</title>

  <p>Let</p>

  <md>\Delta f=f(x+h)-f(x),
\qquad
\Delta g=g(x+h)-g(x).</md>

  <p>Then</p>

  <md>f(x+h)=f(x)+\Delta f</md>

  <p>and</p>

  <md>g(x+h)=g(x)+\Delta g.</md>

  <p>So</p>

  <md>f(x+h)g(x+h)-f(x)g(x)
=
g(x)\Delta f+f(x)\Delta g+\Delta f\,\Delta g.</md>

  <p>Divide by <m>h</m>:</p>

  <md>\frac{f(x+h)g(x+h)-f(x)g(x)}{h}
=
g(x)\frac{\Delta f}{h}
+
f(x)\frac{\Delta g}{h}
+
\Delta f\frac{\Delta g}{h}.</md>

  <p>As <m>h\to0</m>,</p>

  <md>\frac{\Delta f}{h}\to f'(x),</md>

  <md>\frac{\Delta g}{h}\to g'(x),</md>

  <p>and</p>

  <md>\Delta f\to0.</md>

  <p>Therefore</p>

  <md>(fg)'(x)=g(x)f'(x)+f(x)g'(x).</md>

  <p>That is the product rule.</p>
  </paragraphs>

  <paragraphs>
    <title>Differential form of the same idea</title>

  <p>The exact change is</p>

  <md>\Delta(fg)=g\,\Delta f+f\,\Delta g+\Delta f\,\Delta g.</md>

  <p>The differential keeps only the linear part:</p>

  <md>
d(fg)=g\,df+f\,dg.
</md>

  <p>The corner term</p>

  <md>\Delta f\,\Delta g</md>

  <p>is a product of two small changes.  It is smaller than first order and is not part of the linear approximation.</p>
  </paragraphs>

  <exercise xml:id="checkpoint-5-2-corner-vanishes">
        <title>Why does the tiny corner disappear in the limit?</title>
        <statement>
          <p>
            The exact change in a product includes the extra term <m>\Delta f\,\Delta g</m>. In your own words, why does that corner not survive in the derivative, and why does the argument need differentiability of the factors?
          </p>
        </statement>
        <hint>
          <p>
            A product of two small changes is smaller than first order. Differentiability implies continuity, so <m>\Delta f\to 0</m> while the remaining difference quotient stays finite.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-using-product-rule">
    <title>Using the product rule</title>

  <example xml:id="ex-sec-5-2-product-and-quotient-rules-3">
    <title>Example</title>
    <statement>
  <p>Differentiate</p>

  <md>F(x)=(x^2+1)(x^3-5).</md>

  <p>Let</p>

  <md>f(x)=x^2+1
\qquad\text{and}\qquad
g(x)=x^3-5.</md>

  <p>Then</p>

  <md>f'(x)=2x,
\qquad
g'(x)=3x^2.</md>

  <p>By the product rule,</p>

  <md>F'(x)=2x(x^3-5)+(x^2+1)(3x^2).</md>

  <p>Simplify:</p>

  <md>F'(x)=2x^4-10x+3x^4+3x^2.</md>

  <p>Thus</p>

  <md>
F'(x)=5x^4+3x^2-10x.
</md>

  <p>This matches what we would get by expanding first:</p>

  <md>(x^2+1)(x^3-5)=x^5+x^3-5x^2-5.</md>

  <p>But the product rule works even when expansion is not practical.</p>
    </statement>
  </example>

  <example xml:id="ex-sec-5-2-product-and-quotient-rules-4">
    <title>Example</title>
    <statement>
  <p>Differentiate</p>

  <md>P(x)=x^4(x^2-3x+2).</md>

  <p>Here</p>

  <md>f(x)=x^4,
\qquad
g(x)=x^2-3x+2.</md>

  <p>Then</p>

  <md>f'(x)=4x^3,
\qquad
g'(x)=2x-3.</md>

  <p>So</p>

  <md>P'(x)=4x^3(x^2-3x+2)+x^4(2x-3).</md>

  <p>This is already a correct derivative.  If desired, simplify:</p>

  <md>P'(x)=4x^5-12x^4+8x^3+2x^5-3x^4,</md>

  <p>so</p>

  <md>
P'(x)=6x^5-15x^4+8x^3.
</md>
    </statement>
  </example>

  <paragraphs>
    <title>Warning: <m>(fg)'\neq f'g'</m></title>

  <p>Take</p>

  <md>f(x)=x,
\qquad
g(x)=x.</md>

  <p>Then</p>

  <md>f(x)g(x)=x^2,</md>

  <p>so</p>

  <md>(fg)'=2x.</md>

  <p>But</p>

  <md>f'(x)g'(x)=1\cdot1=1.</md>

  <p>These are not the same function.</p>

  <p>The product rule says</p>

  <md>(fg)'=f'g+fg',</md>

  <p>not</p>

  <md>(fg)'=f'g'.</md>
  </paragraphs>

  <paragraphs>
    <title>A hypothesis warning</title>

  <p>The product rule says that if both factors are differentiable, then the product is differentiable.  The converse is not true.</p>

  <p>Let</p>

  <md>F(x)=x|x|.</md>

  <p>The factor <m>|x|</m> is not differentiable at <m>0</m>, so the product rule cannot be used at <m>0</m>.  But <m>F</m> itself is differentiable at <m>0</m>.  Indeed,</p>

  <md>F'(0)
=
\lim_{h\to0}\frac{h|h|-0}{h}
=
\lim_{h\to0}|h|
=
0.</md>

  <p>So a product may have a derivative even when one factor does not.  The rule gives a safe sufficient condition, not a complete test for every possible product.</p>
  </paragraphs>
  </subsection>

  <subsection xml:id="subsec-reciprocal-rule">
    <title>The reciprocal rule</title>

  <p>Quotients begin with reciprocals.  If</p>

  <md>u(x)=\frac1{g(x)},</md>

  <p>then <m>u</m> changes in the opposite direction from <m>g</m>.  When the denominator increases, the reciprocal decreases.</p>

  <p>The derivative formula is:</p>

  <md>
\frac{d}{dx}\left(\frac1{g(x)}\right)
=
-\frac{g'(x)}{(g(x))^2},
</md>

  <p>where <m>g(x)\neq0</m>.</p>

  <paragraphs>
    <title>Proof</title>

  <p>Let</p>

  <md>u(x)=\frac1{g(x)}.</md>

  <p>Then</p>

  <md>u'(x)
=
\lim_{h\to0}
\frac{\frac1{g(x+h)}-\frac1{g(x)}}{h}.</md>

  <p>Combine the fractions:</p>

  <md>\frac{\frac1{g(x+h)}-\frac1{g(x)}}{h}
=
\frac{g(x)-g(x+h)}{h\,g(x+h)g(x)}.</md>

  <p>Rewrite the numerator:</p>

  <md>g(x)-g(x+h)=-(g(x+h)-g(x)).</md>

  <p>So</p>

  <md>\frac{\frac1{g(x+h)}-\frac1{g(x)}}{h}
=
-\frac{g(x+h)-g(x)}{h\,g(x+h)g(x)}.</md>

  <p>Now let <m>h\to0</m>.  Since <m>g</m> is differentiable, it is continuous.  Therefore</p>

  <md>g(x+h)\to g(x).</md>

  <p>Also,</p>

  <md>\frac{g(x+h)-g(x)}{h}\to g'(x).</md>

  <p>Thus</p>

  <md>u'(x)
=
-\frac{g'(x)}{g(x)^2}.</md>
  </paragraphs>

  <example xml:id="ex-sec-5-2-product-and-quotient-rules-8">
    <title>Example</title>
    <statement>
  <p>Differentiate</p>

  <md>R(x)=\frac1{x^2+1}.</md>

  <p>Let</p>

  <md>g(x)=x^2+1.</md>

  <p>Then</p>

  <md>g'(x)=2x.</md>

  <p>By the reciprocal rule,</p>

  <md>R'(x)
=
-\frac{2x}{(x^2+1)^2}.</md>

  <p>So</p>

  <md>
\frac{d}{dx}\left(\frac1{x^2+1}\right)
=
-\frac{2x}{(x^2+1)^2}.
</md>
    </statement>
  </example>

  <paragraphs>
    <title>Negative powers</title>

  <p>The reciprocal rule also extends the power rule to negative integer powers.</p>

  <p>For <m>n</m> a positive integer,</p>

  <md>x^{-n}=\frac1{x^n},
\qquad x\neq0.</md>

  <p>Using the reciprocal rule with</p>

  <md>g(x)=x^n,</md>

  <p>we get</p>

  <md>\frac{d}{dx}x^{-n}
=
-\frac{nx^{n-1}}{(x^n)^2}.</md>

  <p>Since</p>

  <md>(x^n)^2=x^{2n},</md>

  <p>this becomes</p>

  <md>-nx^{n-1-2n}
=
-nx^{-n-1}.</md>

  <p>So</p>

  <md>
\frac{d}{dx}x^{-n}=-n x^{-n-1}
\qquad (x\neq0).
</md>

  <p>This is the same pattern as the power rule:</p>

  <md>\frac{d}{dx}x^r=rx^{r-1}.</md>

  <p>We have now proved it for positive and negative integer powers.  Fractional powers will come later.</p>
  </paragraphs>
  </subsection>

  <subsection xml:id="subsec-quotient-rule">
    <title>The quotient rule</title>

  <p>A quotient is a product with a reciprocal:</p>

  <md>\frac{f(x)}{g(x)}
=
f(x)\cdot\frac1{g(x)}.</md>

  <p>So the quotient rule follows from the product rule and the reciprocal rule.</p>

  <note xml:id="note-sec-5-2-product-and-quotient-rules-112">
    <title>Quotient rule</title>
      <p>If <m>f</m> and <m>g</m> are differentiable at <m>x</m>, and <m>g(x)\neq0</m>, then</p>
        <md>
\frac{d}{dx}\left(\frac{f(x)}{g(x)}\right)
=
\frac{g(x)f'(x)-f(x)g'(x)}{(g(x))^2}.
</md>
  </note>

  <paragraphs>
    <title>Derivation</title>

  <p>Write</p>

  <md>\frac{f}{g}=f\cdot\frac1g.</md>

  <p>Then</p>

  <md>\left(\frac{f}{g}\right)'
=
f'\cdot\frac1g
+
f\cdot\left(\frac1g\right)'.</md>

  <p>Using the reciprocal rule,</p>

  <md>\left(\frac1g\right)'=-\frac{g'}{g^2}.</md>

  <p>Therefore</p>

  <md>\left(\frac{f}{g}\right)'
=
\frac{f'}{g}
-
\frac{fg'}{g^2}.</md>

  <p>Put both terms over the common denominator <m>g^2</m>:</p>

  <md>\left(\frac{f}{g}\right)'
=
\frac{gf'-fg'}{g^2}.</md>
  </paragraphs>


  <exercise xml:id="checkpoint-5-2-reciprocal-then-quotient">
        <title>Why is a quotient a product with a reciprocal?</title>
        <statement>
          <p>
            When a denominator increases, the reciprocal decreases, and the quotient rule follows by combining that fact with the product rule. In your own words, why does the minus sign appear, and why must we still require <m>g(x)\neq 0</m>?
          </p>
        </statement>
        <hint>
          <p>
            A larger denominator makes each output smaller. The reciprocal is undefined where the original function is zero.
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-5-2-product-and-quotient-rules-11">
    <title>Example</title>
    <statement>
  <p>Differentiate</p>

  <md>Q(x)=\frac{x^2+1}{x-3}.</md>

  <p>Let</p>

  <md>f(x)=x^2+1,
\qquad
g(x)=x-3.</md>

  <p>Then</p>

  <md>f'(x)=2x,
\qquad
g'(x)=1.</md>

  <p>The quotient rule gives</p>

  <md>Q'(x)
=
\frac{(x-3)(2x)-(x^2+1)(1)}{(x-3)^2}.</md>

  <p>Simplify the numerator:</p>

  <md>(x-3)(2x)-(x^2+1)
=
2x^2-6x-x^2-1
=
x^2-6x-1.</md>

  <p>Thus</p>

  <md>
Q'(x)=\frac{x^2-6x-1}{(x-3)^2}
</md>

  <p>for</p>

  <md>x\neq3.</md>
    </statement>
  </example>

  <example xml:id="ex-sec-5-2-product-and-quotient-rules-12">
    <title>Example</title>
    <statement>
  <p>Differentiate</p>

  <md>R(x)=\frac{x^3}{x^2+1}.</md>

  <p>Let</p>

  <md>f(x)=x^3,
\qquad
g(x)=x^2+1.</md>

  <p>Then</p>

  <md>f'(x)=3x^2,
\qquad
g'(x)=2x.</md>

  <p>So</p>

  <md>R'(x)
=
\frac{(x^2+1)(3x^2)-x^3(2x)}{(x^2+1)^2}.</md>

  <p>Simplify:</p>

  <md>R'(x)
=
\frac{3x^4+3x^2-2x^4}{(x^2+1)^2}
=
\frac{x^4+3x^2}{(x^2+1)^2}.</md>

  <p>Therefore</p>

  <md>
R'(x)=\frac{x^4+3x^2}{(x^2+1)^2}.
</md>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-denominator-erases-point">
    <title>When a denominator erases a point</title>

  <p>The quotient rule requires</p>

  <md>g(x)\neq0.</md>

  <p>This matters.</p>

  <p>Consider</p>

  <md>H(x)=\frac{x^2-1}{x-1}.</md>

  <p>For <m>x\neq1</m>, we can factor:</p>

  <md>H(x)=\frac{(x-1)(x+1)}{x-1}=x+1.</md>

  <p>So for <m>x\neq1</m>,</p>

  <md>H'(x)=1.</md>

  <p>But the original function <m>H</m> is not defined at <m>x=1</m>.  Therefore <m>H'(1)</m> does not exist.  A function cannot have a derivative at a point where it is not defined.</p>

  <p>If we fill the hole by defining a new function</p>

  <md>
    <mrow>\widetilde H(x)=
\begin{cases}
\dfrac{x^2-1}{x-1}, \amp x\neq1,</mrow>
    <mrow>
2, \amp x=1,
\end{cases}</mrow>
  </md>

  <p>then</p>

  <md>\widetilde H(x)=x+1</md>

  <p>for every <m>x</m>, including <m>x=1</m>.  In that new case,</p>

  <md>\widetilde H'(1)=1.</md>


  <exercise xml:id="checkpoint-5-2-hole-not-automatically-filled">
        <title>Why does simplifying a quotient not define the missing point?</title>
        <statement>
          <p>
            For <m>x\neq 1</m>, the quotient <m>\dfrac{x^2-1}{x-1}</m> agrees with <m>x+1</m>, but the original function is not defined at <m>x=1</m>. In your own words, why can there be no derivative at a point where the function itself does not exist?
          </p>
        </statement>
        <hint>
          <p>
            Algebra can reveal nearby behavior. Differentiability still requires the function to have a value at the point.
          </p>
        </hint>
      </exercise>

  <warning xml:id="warn-sec-5-2-product-and-quotient-rules-113">
    <title>A quotient rule warning</title>
      <p>Simplifying a quotient can reveal nearby behavior, but it does not automatically define the missing point.  Differentiability at a point requires the function to exist at that point.</p>
  </warning>
  </subsection>

  <subsection xml:id="subsec-rules-so-far">
    <title>The rules so far</title>

  <p>We now have a larger toolbox:</p>

  <md>\frac{d}{dx}(c)=0,</md>

  <md>\frac{d}{dx}x^n=nx^{n-1}
\qquad(n=1,2,3,\ldots),</md>

  <md>\frac{d}{dx}\bigl(a f+b g\bigr)=a f'+b g',</md>

  <md>\frac{d}{dx}(fg)=f'g+fg',</md>

  <md>\frac{d}{dx}\left(\frac1g\right)=-\frac{g'}{g^2},</md>

  <p>and</p>

  <md>\frac{d}{dx}\left(\frac{f}{g}\right)=\frac{gf'-fg'}{g^2}.</md>

  <p>These rules handle many expressions built by adding, scaling, multiplying, and dividing.</p>

  <p>But one common construction is still missing.  Many functions are nested:</p>

  <md>(x^2+1)^5,
\qquad
\sqrt{x^3+1},
\qquad
\frac1{(2x-7)^4}.</md>

  <p>The outside function changes because the inside function changes first.  The next rule measures change through a chain.</p>
  </subsection>



</section>
