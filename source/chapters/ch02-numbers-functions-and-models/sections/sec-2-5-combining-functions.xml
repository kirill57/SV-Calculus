<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-combining-functions">
  <title>Combining functions</title>

  <p>Transformations moved a graph without changing its basic identity. A parabola could shift, stretch, or reflect and still be built from the same old rule.</p>

  <p>There is another way to make new functions. We can combine outputs. We can multiply two changing quantities. We can divide one quantity by another. Most importantly, we can feed the output of one function into the input of another.</p>

  <p>These operations are not bookkeeping. Later, each one will force a derivative rule.</p>

  <subsection xml:id="subsec-sums-products-quotients">
    <title>Sums, products, and quotients</title>

    <p>Suppose a small delivery company charges for a trip in two parts. The driver cost is</p>

    <md>D(m)=25+0.40m,</md>

    <p>where <m>m</m> is the number of miles. The fuel cost is</p>

    <md>F(m)=0.18m.</md>

    <p>Both functions have the same input: miles. The total cost is therefore</p>

    <md>C(m)=D(m)+F(m) = 25+0.40m+0.18m = 25+0.58m.</md>

    <p>This is a new function made from two old ones. The same idea works in general.</p>

    <definition xml:id="def-arithmetic-of-functions">
      <title>Arithmetic of functions</title>
      <statement>
        <p>If <m>f</m> and <m>g</m> are functions, then we define:</p>
        <md>
          <mrow>(f+g)(x)\amp=f(x)+g(x),</mrow>
          <mrow>(f-g)(x)\amp=f(x)-g(x),</mrow>
          <mrow>(fg)(x)\amp=f(x)g(x),</mrow>
        </md>
        <p>and, when <m>g(x)\neq 0</m>,</p>
        <md>\left(\frac{f}{g}\right)(x)=\frac{f(x)}{g(x)}.</md>
      </statement>
    </definition>

    <p>The input <m>x</m> must be allowed for both functions. For the quotient, we also need the denominator not to be zero.</p>

    <example xml:id="ex-domain-of-sum-and-product">
      <title>Domain of sum and product</title>
      <statement>
        <p>Let <m>f(x)=\sqrt{x}</m> and <m>g(x)=\frac{1}{x-2}</m>.</p>
        <p>The domain of <m>f</m> is <m>[0,\infty)</m>, and the domain of <m>g</m> is <m>(-\infty,2)\cup(2,\infty)</m>.</p>
        <p>Therefore the domain of <m>f+g</m> is the overlap:</p>
        <md>[0,\infty)\cap\left((-\infty,2)\cup(2,\infty)\right) = [0,2)\cup(2,\infty).</md>
        <p>On that domain, <m>(f+g)(x)=\sqrt{x}+\frac{1}{x-2}</m>. The same domain is used for the product <m>(fg)(x)=\frac{\sqrt{x}}{x-2}</m>.</p>
        <p>A formula may look harmless, but the old restrictions remain.</p>
      </statement>
    </example>

    <example xml:id="ex-multiplying-changing-quantities">
      <title>Multiplying two changing quantities</title>
      <statement>
        <p>A rectangle has length <m>\ell(t)=3+t</m> and width <m>w(t)=5-2t</m>, where <m>t</m> is time in seconds. Its area is</p>
        <md>A(t)=\ell(t)w(t) = (3+t)(5-2t) = 15-t-2t^2.</md>
        <p>But the physical domain is not all real numbers. Length and width should be nonnegative: <m>3+t\ge 0</m> and <m>5-2t\ge 0</m>.</p>
        <p>So <m>t\ge -3</m> and <m>t\le \frac{5}{2}</m>. The physical domain is <m>\left[-3,\frac{5}{2}\right]</m>.</p>
        <p>Algebra alone produced the formula. The meaning of the variables produced the domain.</p>
      </statement>
    </example>

    <warning xml:id="warn-cancellation-domain">
      <title>Cancellation and hidden holes</title>
      <p>Consider</p>
      <md>h(x)=\frac{x^2-1}{x-1}.</md>
      <p>Because <m>x^2-1=(x-1)(x+1)</m>, we can simplify: <m>h(x)=x+1</m> for <m>x\neq 1</m>.</p>
      <p>The condition <m>x\neq 1</m> cannot be erased. The original formula was not defined at <m>x=1</m>. So <m>h</m> is not the same function as <m>p(x)=x+1</m> with domain all real numbers.</p>
      <p>They have the same values for every <m>x\neq 1</m>, but they do not have the same domain.</p>
    </warning>

    <remark xml:id="rmk-simplifying-warning">
      <title>Moral</title>
      <p>Simplifying a formula can hide a missing input. A function is not only a formula; it is a formula together with its allowed inputs.</p>
    </remark>

    <p>This small warning will return when we study limits. Sometimes a function has a hole at one point, but its nearby values still point clearly toward a missing height.</p>
  </subsection>

  <subsection xml:id="subsec-composition">
    <title>Composition</title>

    <p>Addition, multiplication, and division combine outputs that come from the same input. Composition is different. In composition, the output of one function becomes the input of another.</p>

    <p>Imagine a hiker walking along a straight trail. At time <m>t</m>, her position is <m>s(t)=3t</m> miles from the trailhead. The temperature at position <m>x</m> miles from the trailhead is <m>T(x)=70-2x</m> degrees Fahrenheit.</p>

    <p>What is the temperature around the hiker at time <m>t</m>? First time gives position: <m>t\mapsto s(t)=3t</m>. Then position gives temperature: <m>x\mapsto T(x)=70-2x</m>.</p>

    <p>So time gives temperature by two steps:</p>

    <md>T(s(t))=T(3t)=70-2(3t)=70-6t.</md>

    <p>This is a composition.</p>

    <figure xml:id="fig-composition-machine">
      <caption>In <m>f(g(x))</m>, the function <m>g</m> acts first. Its output becomes the input of <m>f</m>.</caption>
      <image>
        <latex-image>
          \begin{tikzpicture}[every node/.style={font=\small}]
            \node (x) at (0,0) {\(x\)};
            \node[draw, rounded corners, minimum width=1.2cm, minimum height=0.7cm] (g) at (2,0) {\(g\)};
            \node (gx) at (4,0) {\(g(x)\)};
            \node[draw, rounded corners, minimum width=1.2cm, minimum height=0.7cm] (f) at (6,0) {\(f\)};
            \node (fgx) at (8.25,0) {\(f(g(x))\)};

            \draw[-&gt;, thick] (x) -- (g);
            \draw[-&gt;, thick] (g) -- (gx);
            \draw[-&gt;, thick] (gx) -- (f);
            \draw[-&gt;, thick] (f) -- (fgx);

            \node at (4,-1.0) {first use \(g\), then use \(f\)};
          \end{tikzpicture}
        </latex-image>
      </image>
    </figure>

    <definition xml:id="def-composition">
      <title>Composition</title>
      <statement>
        <p>The <term>composition</term> of <m>f</m> with <m>g</m> is the function</p>
        <md>(f\circ g)(x)=f(g(x)).</md>
        <p>The function <m>g</m> is applied first. The function <m>f</m> is applied second.</p>
      </statement>
    </definition>

    <p>The symbol <m>f\circ g</m> is read <q><m>f</m> composed with <m>g</m></q>. The order matters. Usually, <m>f\circ g \neq g\circ f</m>.</p>

    <example xml:id="ex-composition-order-matters">
      <title>Order matters in composition</title>
      <statement>
        <p>Let <m>f(u)=\sqrt{u}</m> and <m>g(x)=4-x^2</m>.</p>
        <p>Then <m>(f\circ g)(x)=f(g(x))=\sqrt{4-x^2}</m>. For this to make sense, we need <m>4-x^2\ge 0</m>, so <m>-2\le x\le 2</m>. Thus <m>\operatorname{domain}(f\circ g)=[-2,2]</m>.</p>
        <p>Now reverse the order: <m>(g\circ f)(x)=g(f(x))=4-(\sqrt{x})^2 = 4-x</m>, but only for inputs where <m>\sqrt{x}</m> is defined. Therefore <m>\operatorname{domain}(g\circ f)=[0,\infty)</m>.</p>
        <p>So <m>f\circ g</m> and <m>g\circ f</m> are different functions.</p>
      </statement>
    </example>

    <principle xml:id="principle-domain-of-composition">
      <title>Domain of a composition</title>
      <statement>
        <p>For <m>f(g(x))</m> to be defined, two things must happen:</p>
        <ul>
          <li><p><m>x</m> must be allowed as an input of <m>g</m>, and</p></li>
          <li><p><m>g(x)</m> must be allowed as an input of <m>f</m>.</p></li>
        </ul>
        <p>In symbols,</p>
        <md>\operatorname{domain}(f\circ g) = \{x\in \operatorname{domain}(g): g(x)\in \operatorname{domain}(f)\}.</md>
      </statement>
    </principle>

    <example xml:id="ex-composition-in-transformation">
      <title>Composition inside a transformation</title>
      <statement>
        <p>The function <m>h(x)=(x-2)^2</m> can be read as a composition.</p>
        <p>Let <m>g(x)=x-2</m> and <m>f(u)=u^2</m>. Then <m>h(x)=f(g(x))</m>. First subtract <m>2</m>, then square.</p>
        <p>This is why horizontal shifts can feel backward. The inside function changes the input before the outer function sees it.</p>
      </statement>
    </example>
  </subsection>

  <subsection xml:id="subsec-inverse-functions">
    <title>Inverse functions</title>

    <p>Some functions can be undone.</p>

    <p>Temperature conversion gives a simple example. If <m>C</m> is temperature in degrees Celsius, then the corresponding Fahrenheit temperature is</p>

    <md>F(C)=\frac{9}{5}C+32.</md>

    <p>For example, <m>F(20)=\frac{9}{5}(20)+32=68</m>. If we know the Fahrenheit temperature and want Celsius, we solve <m>F=\frac{9}{5}C+32</m> for <m>C</m>:</p>

    <md>C=\frac{5}{9}(F-32).</md>

    <p>So the function that undoes <m>F(C)</m> is <m>C(F)=\frac{5}{9}(F-32)</m>. Check: <m>C(F(20)) = \frac{5}{9}(68-32) = 20</m>. The two conversions undo each other.</p>

    <definition xml:id="def-inverse-function">
      <title>Inverse function</title>
      <statement>
        <p>Suppose <m>f</m> is a function with domain <m>D</m> and range <m>R_f</m>. An <term>inverse function</term> for <m>f</m> is a function</p>
        <md>f^{-1}:R_f\to D</md>
        <p>such that <m>f^{-1}(f(x))=x</m> for every <m>x\in D</m>, and <m>f(f^{-1}(y))=y</m> for every <m>y\in R_f</m>.</p>
      </statement>
    </definition>

    <p>The notation <m>f^{-1}</m> is read <q><m>f</m> inverse</q>. It does not mean reciprocal. In general, <m>f^{-1}(x)\neq \frac{1}{f(x)}</m>.</p>

    <example xml:id="ex-linear-inverse">
      <title>Finding a linear inverse</title>
      <statement>
        <p>Let <m>f(x)=2x-7</m>. To find the inverse, write <m>y=2x-7</m> and solve for <m>x</m>:</p>
        <md>x=\frac{y+7}{2} \qquad\Longrightarrow\qquad f^{-1}(x)=\frac{x+7}{2}.</md>
        <p>Check both directions:</p>
        <md>f^{-1}(f(x)) = \frac{(2x-7)+7}{2} = x, \qquad f(f^{-1}(x)) = 2\left(\frac{x+7}{2}\right)-7 = x.</md>
      </statement>
    </example>
  </subsection>

  <subsection xml:id="subsec-when-inverse-exists">
    <title>When an inverse exists</title>

    <p>Not every function can be undone.</p>

    <p>The problem is duplication. If two different inputs give the same output, then the output does not remember which input produced it. The square function on all real numbers is the first example: <m>f(x)=x^2</m>. It sends both <m>2</m> and <m>-2</m> to <m>4</m>:</p>

    <md>f(2)=4, \qquad f(-2)=4.</md>

    <p>An inverse would have to answer: <m>f^{-1}(4)=?</m> Should the answer be <m>2</m> or <m>-2</m>? A function is allowed only one output for each input, so there is no inverse function for <m>x^2</m> on all of <m>\mathbb{R}</m>.</p>

    <definition xml:id="def-one-to-one">
      <title>One-to-one function</title>
      <statement>
        <p>A function <m>f</m> is <term>one-to-one</term> if different inputs always give different outputs. Equivalently,</p>
        <md>f(x_1)=f(x_2) \quad\Longrightarrow\quad x_1=x_2.</md>
      </statement>
    </definition>

    <principle xml:id="principle-inverse-criterion">
      <title>Inverse criterion</title>
      <statement>
        <p>A function has an inverse function exactly when it is one-to-one.</p>
      </statement>
    </principle>

    <paragraphs xml:id="par-horizontal-line-test">
      <title>The horizontal line test</title>
      <p>A graph represents a one-to-one function exactly when every horizontal line crosses the graph at most once.</p>
      <p>A horizontal line represents one output value. If it hits the graph twice, then two different inputs produced the same output. That prevents an inverse.</p>
    </paragraphs>

    <sidebyside widths="47% 47%">
      <figure xml:id="fig-square-not-one-to-one">
        <caption>Not one-to-one on <m>\mathbb{R}</m></caption>
        <image>
          <latex-image>
            \begin{tikzpicture}
              \begin{axis}[
                  width=\textwidth,
                  height=0.78\textwidth,
                  xmin=-2.6, xmax=2.6,
                  ymin=-0.5, ymax=6,
                  axis lines=middle,
                  xlabel={\(x\)},
                  ylabel={\(y\)},
                  xtick={-2,0,2},
                  ytick={0,4},
                  grid=both,
                  grid style={line width=.1pt, draw=gray!25},
              ]
              \addplot[thick, domain=-2.4:2.4, samples=100] {x^2};
              \addplot[dashed, domain=-2.5:2.5, samples=2] {4};
              \addplot[only marks,mark=*] coordinates {(-2,4) (2,4)};
              \node[anchor=south] at (axis cs:0,4) {\(y=4\)};
              \end{axis}
            \end{tikzpicture}
          </latex-image>
        </image>
      </figure>

      <figure xml:id="fig-square-restricted-inverse">
        <caption>Restricting domain to <m>[0,\infty)</m> gives inverse <m>\sqrt{x}</m></caption>
        <image>
          <latex-image>
            \begin{tikzpicture}
              \begin{axis}[
                  width=\textwidth,
                  height=0.78\textwidth,
                  xmin=-0.5, xmax=4.5,
                  ymin=-0.5, ymax=4.5,
                  axis lines=middle,
                  xlabel={\(x\)},
                  ylabel={\(y\)},
                  xtick={0,1,4},
                  ytick={0,1,4},
                  grid=both,
                  grid style={line width=.1pt, draw=gray!25},
                  legend style={at={(0.5,-0.22)},anchor=north,legend columns=2}
              ]
              \addplot[thick, domain=0:2.1, samples=80] {x^2};
              \addlegendentry{\(y=x^2,\ x\ge0\)}
              \addplot[thick, dashed, domain=0:4.2, samples=80] {sqrt(x)};
              \addlegendentry{\(y=\sqrt{x}\)}
              \addplot[dotted, domain=0:4.2, samples=2] {x};
              \end{axis}
            \end{tikzpicture}
          </latex-image>
        </image>
      </figure>
    </sidebyside>

    <paragraphs xml:id="par-restricting-domain">
      <title>Restricting a domain</title>
      <p>The function <m>f(x)=x^2</m> does not have an inverse on <m>\mathbb{R}</m>. But if we restrict its domain to <m>[0,\infty)</m>, then it becomes one-to-one. On that domain, <m>f^{-1}(x)=\sqrt{x}</m>.</p>
      <p>If instead we restrict the square function to <m>(-\infty,0]</m>, then the inverse is <m>f^{-1}(x)=-\sqrt{x}</m>.</p>
      <p>The same formula <m>x^2</m> can lead to different inverse functions depending on the domain.</p>
    </paragraphs>

    <principle xml:id="principle-monotonicity-inverse">
      <title>Monotonicity and Inverses</title>
      <statement>
        <p>If a function is strictly increasing on its domain, then it is one-to-one.</p>
        <p>If a function is strictly decreasing on its domain, then it is one-to-one.</p>
      </statement>
    </principle>

    <paragraphs xml:id="par-graphs-of-inverses">
      <title>Graphs of inverse functions</title>
      <p>If <m>y=f(x)</m>, then the inverse relation swaps input and output: <m>x=f^{-1}(y)</m>. On a graph, swapping <m>x</m> and <m>y</m> reflects points across the line <m>y=x</m>.</p>
      <p>Thus the graphs of <m>f</m> and <m>f^{-1}</m> are mirror images across <m>y=x</m>, when both are drawn using the same scale on both axes. The point <m>(a,b)</m> on the graph of <m>f</m> corresponds to the point <m>(b,a)</m> on the graph of <m>f^{-1}</m>.</p>
    </paragraphs>

    <p>Composition and inverses will matter throughout calculus. The chain rule will describe how a composition changes. The derivative of an inverse function will describe how undoing a function changes rates.</p>

    <p>But before calculus can use functions well, we need to know the families that appear most often. A formula is more than symbols: it has a shape, a natural domain, a growth pattern, and often a physical meaning.</p>
  </subsection>

</section>
