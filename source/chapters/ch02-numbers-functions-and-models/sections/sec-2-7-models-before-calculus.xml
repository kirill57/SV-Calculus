<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-models-before-calculus">
  <title>Models before calculus</title>

  <p>The catalog gave us familiar function families. A model is the next step: we choose one of those families because a situation asks for it.</p>

  <p>A model is not the same thing as reality. It is a mathematical story about reality. A good model respects units, captures the main pattern, and makes predictions we can test.</p>

  <definition xml:id="def-mathematical-model">
    <title>Mathematical model</title>
    <statement>
      <p>A <term>mathematical model</term> is a function, equation, graph, or table used to represent a real quantity.</p>
      <p>A model should be judged by four questions:</p>
      <ul>
        <li><p>What assumptions does it make?</p></li>
        <li><p>Do the units make sense?</p></li>
        <li><p>How well does it fit the information we have?</p></li>
        <li><p>Where should we not trust it?</p></li>
      </ul>
    </statement>
  </definition>

  <p>Calculus will later tell us how models change and how their effects accumulate. Before calculus begins, we should learn the main shapes that models can take.</p>

  <subsection xml:id="subsec-linear-growth-models">
    <title>Linear growth</title>

    <p>Suppose water is poured into a tank at a steady rate of <m>3</m> liters per minute. At the moment we start watching, the tank already contains <m>12</m> liters.</p>

    <p>After <m>t</m> minutes, the amount of water is <m>W(t)=12+3t</m>. The number <m>12</m> is the starting amount. The number <m>3</m> is the rate of change.</p>

    <table>
      <title>Water volume in tank over time</title>
      <tabular top="major" bottom="major" left="major" right="major">
        <row bottom="minor">
          <cell><m>t\text{ (minutes)}</m></cell>
          <cell><m>0</m></cell>
          <cell><m>1</m></cell>
          <cell><m>2</m></cell>
          <cell><m>3</m></cell>
          <cell><m>4</m></cell>
        </row>
        <row>
          <cell><m>W(t)\text{ (liters)}</m></cell>
          <cell><m>12</m></cell>
          <cell><m>15</m></cell>
          <cell><m>18</m></cell>
          <cell><m>21</m></cell>
          <cell><m>24</m></cell>
        </row>
      </tabular>
    </table>

    <p>Each time <m>t</m> increases by <m>1</m>, the output increases by <m>3</m>: <m>15-12=3</m>, <m>18-15=3</m>, <m>21-18=3</m>. This is the signature of a linear model: equal input changes produce equal output changes.</p>

    <principle xml:id="principle-linear-model">
      <title>Linear model</title>
      <statement>
        <p>A linear model has the form</p>
        <md>y=b+mx.</md>
        <p>The number <m>b</m> is the initial value (<m>y(0)=b</m>). The number <m>m</m> is the constant rate of change:</p>
        <md>m=\frac{\text{change in output}}{\text{change in input}}.</md>
      </statement>
    </principle>

    <p>The units of <m>m</m> are output units divided by input units. In the water example, <m>m=3\frac{\text{liters}}{\text{minute}}</m>, so <m>3\frac{\text{liters}}{\text{minute}}\cdot t\text{ minutes} = 3t\text{ liters}</m>. The units are not decoration. They check the formula.</p>

    <example xml:id="ex-phone-plan-model">
      <title>A phone plan</title>
      <statement>
        <p>A phone plan charges <m>25</m> dollars per month plus <m>0.05</m> dollars per text message. If <m>n</m> is the number of text messages, the monthly cost is</p>
        <md>C(n)=25+0.05n.</md>
        <p>The slope is <m>0.05\frac{\text{dollars}}{\text{message}}</m>. The intercept is <m>25\text{ dollars}</m>.</p>
        <p>The natural domain is <m>n=0,1,2,3,\dots</m>, because the number of text messages must be a whole number. The graph is a collection of points, not a solid line. We often draw the line anyway because it makes the pattern easy to see, but the model's meaning decides the real domain.</p>
      </statement>
    </example>

    <example xml:id="ex-candle-burning-model">
      <title>Finding a linear model from two points</title>
      <statement>
        <p>Suppose a candle has height <m>18</m> cm at time <m>t=0</m>, and height <m>14</m> cm after <m>2</m> hours. If we assume the candle burns at a constant rate, then its height is linear.</p>
        <p>The slope is</p>
        <md>m=\frac{14-18}{2-0} = \frac{-4}{2} = -2\text{ cm/hour}.</md>
        <p>Therefore <m>H(t)=18-2t</m>.</p>
        <p>This model predicts that the candle reaches height <m>0</m> when <m>18-2t=0</m>, so <m>t=9</m>. The model says the candle lasts <m>9</m> hours.</p>
        <p>But this prediction depends on the assumption that the burn rate stays constant. If the candle shape changes or the flame weakens, the model may fail before <m>9</m> hours.</p>
      </statement>
    </example>

    <remark xml:id="rmk-modeling-assumptions-lesson">
      <title>Modeling lesson</title>
      <p>A formula can be algebraically correct and still be physically wrong outside the range where its assumptions make sense.</p>
    </remark>
  </subsection>

  <subsection xml:id="subsec-power-law-models">
    <title>Power-law scaling</title>

    <p>Linear models are about constant additive change. Power-law models are about scaling.</p>

    <p>Imagine enlarging a square photograph. If every length is multiplied by <m>2</m>, then the area is multiplied by <m>4</m>.</p>

    <p>A <m>3</m>-inch by <m>3</m>-inch square has area <m>9\text{ square inches}</m>. A <m>6</m>-inch by <m>6</m>-inch square has area <m>36\text{ square inches}</m>. Doubling the side length quadruples the area: <m>36=4\cdot 9</m>. This is square growth.</p>

    <p>For a square of side length <m>s</m>, the area is <m>A(s)=s^2</m>. If we multiply the side length by <m>k</m>, then <m>A(ks)=(ks)^2=k^2 s^2=k^2 A(s)</m>. So scaling the input by <m>k</m> scales the output by <m>k^2</m>.</p>

    <principle xml:id="principle-power-law-model">
      <title>Power-law model</title>
      <statement>
        <p>A power-law model has the form</p>
        <md>y=Cx^p,</md>
        <p>where <m>C</m> and <m>p</m> are constants. It satisfies the scaling rule</p>
        <md>y(kx)=C(kx)^p=k^p Cx^p=k^p y(x).</md>
      </statement>
    </principle>

    <p>The exponent <m>p</m> tells us how strongly the output responds to scaling:</p>

    <table>
      <title>Response to doubling input in power-law models</title>
      <tabular top="major" bottom="major" left="major" right="major">
        <row bottom="minor">
          <cell><term>Model</term></cell>
          <cell><term>Doubling input multiplies output by</term></cell>
          <cell><term>Common meaning</term></cell>
        </row>
        <row>
          <cell><m>y=Cx</m></cell>
          <cell><m>2</m></cell>
          <cell>length-like growth</cell>
        </row>
        <row>
          <cell><m>y=Cx^2</m></cell>
          <cell><m>4</m></cell>
          <cell>area-like growth</cell>
        </row>
        <row>
          <cell><m>y=Cx^3</m></cell>
          <cell><m>8</m></cell>
          <cell>volume-like growth</cell>
        </row>
        <row>
          <cell><m>y=C\sqrt{x}=Cx^{1/2}</m></cell>
          <cell><m>\sqrt{2}</m></cell>
          <cell>root-type growth</cell>
        </row>
      </tabular>
    </table>

    <example xml:id="ex-similar-tanks-scaling">
      <title>Similar tanks</title>
      <statement>
        <p>Suppose two water tanks have the same shape, but one is twice as tall, twice as wide, and twice as deep as the other.</p>
        <p>Every length has been multiplied by <m>2</m>. Volume is three-dimensional, so the larger tank has <m>2^3=8</m> times the volume.</p>
        <p>If a tank of height <m>h</m> has volume <m>V(h)=Ch^3</m> for some constant <m>C</m>, then <m>V(2h)=C(2h)^3=8Ch^3=8V(h)</m>.</p>
        <p>This is why a small-looking change in length can make a large change in volume.</p>
      </statement>
    </example>

    <paragraphs xml:id="par-units-suggest-power">
      <title>Units suggest a power</title>
      <p>Suppose a quantity depends only on a length <m>r</m>, and the output has units of area. Since area units are length squared, a first guess is <m>A(r)=Cr^2</m>. For a circle, <m>C=\pi</m>, so <m>A(r)=\pi r^2</m>.</p>
      <p>If the output has units of volume, a first guess is <m>V(r)=Cr^3</m>. For a sphere, <m>C=\frac{4}{3}\pi</m>, so <m>V(r)=\frac{4}{3}\pi r^3</m>.</p>
      <p>Units do not prove the formula. But they often tell us what kind of formula to expect.</p>
    </paragraphs>
  </subsection>

  <subsection xml:id="subsec-exponential-models">
    <title>Exponential growth and decay</title>

    <p>Some quantities do not add the same amount in equal time steps. They multiply by the same factor.</p>

    <p>Suppose a bacterial culture grows by <m>50\%</m> every hour. If it starts with <m>100</m> bacteria, then after each hour the population is multiplied by <m>1.5</m>.</p>

    <table>
      <title>Bacterial population growing by 50% per hour</title>
      <tabular top="major" bottom="major" left="major" right="major">
        <row bottom="minor">
          <cell><m>t\text{ (hours)}</m></cell>
          <cell><m>0</m></cell>
          <cell><m>1</m></cell>
          <cell><m>2</m></cell>
          <cell><m>3</m></cell>
          <cell><m>4</m></cell>
        </row>
        <row>
          <cell><m>P(t)</m></cell>
          <cell><m>100</m></cell>
          <cell><m>150</m></cell>
          <cell><m>225</m></cell>
          <cell><m>337.5</m></cell>
          <cell><m>506.25</m></cell>
        </row>
      </tabular>
    </table>

    <p>The differences are not constant (<m>150-100=50</m>, <m>225-150=75</m>, <m>337.5-225=112.5</m>), but the ratios are constant: <m>\frac{150}{100}=1.5</m>, <m>\frac{225}{150}=1.5</m>, <m>\frac{337.5}{225}=1.5</m>. This is exponential growth: <m>P(t)=100(1.5)^t</m>.</p>

    <principle xml:id="principle-exponential-model">
      <title>Exponential model</title>
      <statement>
        <p>An exponential model has the form</p>
        <md>y=Aa^t,</md>
        <p>where <m>A\gt 0</m> and <m>a\gt 0</m>. The number <m>A</m> is the initial value. The number <m>a</m> is the growth factor per unit time.</p>
        <p>If <m>a\gt 1</m>, the model grows. If <m>0\lt a\lt 1</m>, the model decays.</p>
      </statement>
    </principle>

    <example xml:id="ex-decay-half-life-model">
      <title>Half-life decay model</title>
      <statement>
        <p>A substance has half-life <m>8</m> days. That means its amount is multiplied by <m>\frac{1}{2}</m> every <m>8</m> days.</p>
        <p>If the starting amount is <m>M_0</m>, then a model for the amount after <m>t</m> days is</p>
        <md>M(t)=M_0\left(\frac{1}{2}\right)^{t/8}.</md>
        <p>Check: <m>M(t+8)=M_0\left(\frac{1}{2}\right)^{(t+8)/8} = \frac{1}{2}M_0\left(\frac{1}{2}\right)^{t/8} = \frac{1}{2}M(t)</m>. Every <m>8</m> days, the amount is cut in half.</p>
      </statement>
    </example>

    <example xml:id="ex-population-doubling-time">
      <title>Doubling time model</title>
      <statement>
        <p>A population doubles every <m>5</m> years. If the initial population is <m>P_0</m>, then <m>P(t)=P_0\,2^{t/5}</m>.</p>
        <p>Indeed, <m>P(t+5)=P_0\,2^{(t+5)/5}=2P_0\,2^{t/5}=2P(t)</m>. The exponent <m>\frac{t}{5}</m> counts how many doubling periods have passed.</p>
      </statement>
    </example>

    <figure xml:id="fig-linear-versus-exponential-models">
      <caption>Linear growth adds equal amounts. Exponential growth multiplies by equal factors.</caption>
      <image>
        <latex-image>
          \begin{tikzpicture}
            \begin{axis}[
                width=0.74\textwidth,
                height=0.46\textwidth,
                xmin=0, xmax=5,
                ymin=0, ymax=35,
                axis lines=left,
                xlabel={\(t\)},
                ylabel={output},
                xtick={0,1,2,3,4,5},
                ytick={0,5,10,20,30},
                grid=both,
                grid style={line width=.1pt, draw=gray!25},
                legend style={at={(0.5,-0.18)},anchor=north,legend columns=2}
            ]
            \addplot[thick, domain=0:5, samples=2] {4+4*x};
            \addlegendentry{linear: \(4+4t\)}
            \addplot[thick, dashed, domain=0:5, samples=100] {4*1.5^x};
            \addlegendentry{exponential: \(4(1.5)^t\)}
            \end{axis}
          \end{tikzpicture}
        </latex-image>
      </image>
    </figure>

    <warning xml:id="warn-exponential-long-term">
      <title>Short-term vs long-term validity</title>
      <p>Exponential growth can be a good short-term model and a terrible long-term model.</p>
      <p>A bacterial population cannot grow forever in a closed dish. A rumor cannot spread to more people than exist. A bank account cannot earn the same interest rate through every possible economic condition.</p>
      <p>A model may be useful because it is locally accurate, even when it is not globally permanent. Calculus will often help us decide what <q>locally</q> means.</p>
    </warning>
  </subsection>

  <subsection xml:id="subsec-periodic-motion-models">
    <title>Periodic motion</title>

    <p>Some quantities repeat. A Ferris wheel turns. A tide rises and falls. A weight on a spring moves up and down. A point on a rotating tire returns to its starting height again and again. The basic models for repeating motion use sine and cosine.</p>

    <example xml:id="ex-ferris-wheel-height">
      <title>Height on a Ferris wheel</title>
      <statement>
        <p>Suppose a Ferris wheel has center <m>20</m> meters above the ground and radius <m>18</m> meters. One full rotation takes <m>30</m> seconds.</p>
        <p>If a rider starts at the midline and is moving upward at time <m>t=0</m>, then the height can be modeled by</p>
        <md>H(t)=20+18\sin\left(\frac{\pi t}{15}\right).</md>
        <p>The number <m>20</m> is the midline. The number <m>18</m> is the amplitude. The period is <m>30</m>, because</p>
        <md>\sin\left(\frac{\pi(t+30)}{15}\right) = \sin\left(\frac{\pi t}{15}+2\pi\right) = \sin\left(\frac{\pi t}{15}\right).</md>
        <p>So <m>H(t+30)=H(t)</m>. The height repeats every <m>30</m> seconds.</p>

        <figure xml:id="fig-ferris-wheel-model">
          <caption>A sinusoidal model records a repeating motion by its midline, amplitude, and period.</caption>
          <image>
            <latex-image>
              \begin{tikzpicture}
                \begin{axis}[
                    width=0.76\textwidth,
                    height=0.45\textwidth,
                    xmin=0, xmax=60,
                    ymin=0, ymax=40,
                    axis lines=left,
                    xlabel={\(t\) (seconds)},
                    ylabel={\(H(t)\) (meters)},
                    xtick={0,7.5,15,22.5,30,45,60},
                    xticklabels={\(0\),\(7.5\),\(15\),\(22.5\),\(30\),\(45\),\(60\)},
                    ytick={2,20,38},
                    yticklabels={\(2\),\(20\),\(38\)},
                    grid=both,
                    grid style={line width=.1pt, draw=gray!25},
                ]
                \addplot[thick, domain=0:60, samples=200] {20+18*sin(deg(pi*x/15))};
                \addplot[dashed, domain=0:60, samples=2] {20};
                \node[anchor=west] at (axis cs:38,21.5) {midline \(H=20\)};
                \node[anchor=south] at (axis cs:7.5,38) {top};
                \node[anchor=north] at (axis cs:22.5,2) {bottom};
                \end{axis}
              \end{tikzpicture}
            </latex-image>
          </image>
        </figure>
      </statement>
    </example>

    <definition xml:id="def-periodic-function">
      <title>Periodic function and period</title>
      <statement>
        <p>A function is <term>periodic</term> if there is a positive number <m>T</m> such that <m>f(t+T)=f(t)</m> for every allowed <m>t</m>.</p>
        <p>The smallest such positive <m>T</m>, when it exists, is called the <term>period</term>.</p>
      </statement>
    </definition>

    <p>For <m>A\sin(Bt)</m> and <m>A\cos(Bt)</m>, the amplitude is <m>|A|</m>, and the period is <m>\frac{2\pi}{B}</m> (<m>B\gt 0</m>).</p>

    <example xml:id="ex-periodic-cosine-amplitude-period">
      <title>Amplitude and period of cosine</title>
      <statement>
        <p>The function <m>y(t)=5\cos(4t)</m> has amplitude <m>5</m> and period <m>\frac{2\pi}{4}=\frac{\pi}{2}</m>. The output repeats whenever <m>t</m> increases by <m>\frac{\pi}{2}</m>.</p>
      </statement>
    </example>
  </subsection>

  <subsection xml:id="subsec-choosing-model-units-data">
    <title>Choosing a model from units and data</title>

    <p>A model is a choice. The choice should be guided by the situation.</p>

    <example xml:id="ex-linear-vs-exponential-data">
      <title>Linear or exponential data</title>
      <statement>
        <p>A small plant is measured once per week:</p>
        <table>
          <title>Plant height over weeks</title>
          <tabular top="major" bottom="major" left="major" right="major">
            <row bottom="minor">
              <cell><m>t\text{ (weeks)}</m></cell>
              <cell><m>0</m></cell>
              <cell><m>1</m></cell>
              <cell><m>2</m></cell>
              <cell><m>3</m></cell>
              <cell><m>4</m></cell>
            </row>
            <row>
              <cell><m>H(t)\text{ (cm)}</m></cell>
              <cell><m>10</m></cell>
              <cell><m>13</m></cell>
              <cell><m>16</m></cell>
              <cell><m>19</m></cell>
              <cell><m>22</m></cell>
            </row>
          </tabular>
        </table>
        <p>The differences are <m>3, 3, 3, 3</m>. A linear model is natural: <m>H(t)=10+3t</m>.</p>

        <p>Now compare a different table:</p>
        <table>
          <title>Bacterial population over hours</title>
          <tabular top="major" bottom="major" left="major" right="major">
            <row bottom="minor">
              <cell><m>t\text{ (hours)}</m></cell>
              <cell><m>0</m></cell>
              <cell><m>1</m></cell>
              <cell><m>2</m></cell>
              <cell><m>3</m></cell>
              <cell><m>4</m></cell>
            </row>
            <row>
              <cell><m>P(t)</m></cell>
              <cell><m>100</m></cell>
              <cell><m>150</m></cell>
              <cell><m>224</m></cell>
              <cell><m>337</m></cell>
              <cell><m>506</m></cell>
            </row>
          </tabular>
        </table>
        <p>The differences are <m>50, 74, 113, 169</m> (not constant), but the ratios are approximately <m>1.50, 1.49, 1.50, 1.50</m>. An exponential model is natural: <m>P(t)\approx 100(1.5)^t</m>.</p>
        <p>The symbol <m>\approx</m> matters. Data are often measured with error, and real processes rarely follow a model perfectly.</p>
      </statement>
    </example>

    <paragraphs xml:id="par-residuals-model-errors">
      <title>Residuals: the errors a model leaves behind</title>
      <p>Suppose data points are <m>(x_1,y_1), (x_2,y_2), \dots, (x_n,y_n)</m>, and a model predicts <m>y=f(x)</m>. At input <m>x_i</m>, the prediction error is <m>y_i-f(x_i)</m>. These errors are called <term>residuals</term>. A good model should leave small residuals without using a formula that is more complicated than the situation deserves.</p>
    </paragraphs>

    <remark xml:id="rmk-modeling-habit">
      <title>A modeling habit</title>
      <p>Do not ask only: <em>Can I find a formula through the data?</em></p>
      <p>Ask also:</p>
      <ul>
        <li><p>Does the formula explain the pattern?</p></li>
        <li><p>Are the units meaningful?</p></li>
        <li><p>Does the formula behave reasonably outside the data?</p></li>
      </ul>
    </remark>

    <example xml:id="ex-overfitting-model">
      <title>A model that fits too well</title>
      <statement>
        <p>Given five data points, it is often possible to find a polynomial of degree <m>4</m> that passes exactly through all of them. That does not mean the polynomial is the best model.</p>
        <p>A high-degree polynomial may wiggle wildly between data points or make absurd predictions just outside the measured interval. Exact fit is not the same as understanding.</p>
        <p>For calculus, the simplest useful model is often better than a complicated perfect fit.</p>
      </statement>
    </example>

    <p>Models give us functions to study. Technology gives us quick graphs of those functions. But a graph on a screen is not the function itself. It is a picture made with choices: a window, a scale, and a finite number of sampled points.</p>

    <p>Those choices can hide the truth.</p>
  </subsection>

</section>
