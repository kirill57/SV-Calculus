<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-2-7-models-before-calculus">
  <title>Models before calculus</title>

  <p>The catalog gave us familiar function families. A model is the next step: we choose one of those families because a situation asks for it.</p>

  <p>A model is not the same thing as reality. It is a mathematical story about reality. A good model respects units, captures the main pattern, and makes predictions we can test.</p>

  <definition xml:id="def-sec-2-7-models-before-calculus-78">
    <title>Definition</title>
    <statement>
      <p>A <em>mathematical model</em> is a function, equation, graph, or table used to represent a real quantity.</p>
      <p>A model should be judged by four questions:</p>
        <md>\text{What assumptions does it make?}</md>
        <md>\text{Do the units make sense?}</md>
        <md>\text{How well does it fit the information we have?}</md>
        <md>\text{Where should we not trust it?}</md>
    </statement>
  </definition>

  <p>Calculus will later tell us how models change and how their effects accumulate. Before calculus begins, we should learn the main shapes that models can take.</p>

  <subsection xml:id="subsec-linear-growth-models">
    <title>Linear growth</title>

  <p>Suppose water is poured into a tank at a steady rate of <m>3</m> liters per minute. At the moment we start watching, the tank already contains <m>12</m> liters.</p>

  <p>After <m>t</m> minutes, the amount of water is</p>

  <md>W(t)=12+3t.</md>

  <p>The number <m>12</m> is the starting amount. The number <m>3</m> is the rate of change.</p>

  <md>
    <mrow>\begin{array}{c|ccccc}
t\text{ (minutes)} \amp 0 \amp 1 \amp 2 \amp 3 \amp 4</mrow>
    <mrow>\hline
W(t)\text{ (liters)} \amp 12 \amp 15 \amp 18 \amp 21 \amp 24
\end{array}</mrow>
  </md>

  <p>Each time <m>t</m> increases by <m>1</m>, the output increases by <m>3</m>.</p>

  <md>15-12=3,\qquad 18-15=3,\qquad 21-18=3.</md>

  <p>This is the signature of a linear model: equal input changes produce equal output changes.</p>

  <note xml:id="note-sec-2-7-models-before-calculus-79">
    <title>Linear model</title>
      <p>A linear model has the form</p>
        <md>y=b+mx.</md>
      <p>The number <m>b</m> is the initial value, because</p>
        <md>y(0)=b.</md>
      <p>The number <m>m</m> is the constant rate of change:</p>
        <md>m=\frac{\text{change in output}}{\text{change in input}}.</md>
  </note>

  <p>The units of <m>m</m> are output units divided by input units. In the water example,</p>

  <md>m=3\frac{\text{liters}}{\text{minute}}.</md>

  <p>So</p>

  <md>3\frac{\text{liters}}{\text{minute}}\cdot t\text{ minutes}
=
3t\text{ liters}.</md>

  <p>The units are not decoration. They check the formula.</p>

  <example xml:id="ex-sec-2-7-models-before-calculus-1">
    <title>a phone plan</title>
    <statement>
  <p>A phone plan charges <m>25</m> dollars per month plus <m>0.05</m> dollars per text message. If <m>n</m> is the number of text messages, the monthly cost is</p>

  <md>C(n)=25+0.05n.</md>

  <p>The slope is</p>

  <md>0.05\frac{\text{dollars}}{\text{message}}.</md>

  <p>The intercept is</p>

  <md>25\text{ dollars}.</md>

  <p>The natural domain is</p>

  <md>n=0,1,2,3,\ldots,</md>

  <p>because the number of text messages must be a whole number. The graph is a collection of points, not a solid line. We often draw the line anyway because it makes the pattern easy to see, but the model’s meaning decides the real domain.</p>
    </statement>
  </example>

  <paragraphs>
    <title>Finding a linear model from two points</title>

  <p>Suppose a candle has height <m>18</m> cm at time <m>t=0</m>, and height <m>14</m> cm after <m>2</m> hours. If we assume the candle burns at a constant rate, then its height is linear.</p>

  <p>The slope is</p>

  <md>m=
\frac{14-18}{2-0}
=
\frac{-4}{2}
=
-2</md>

  <p>cm/hour. Therefore</p>

  <md>H(t)=18-2t.</md>

  <p>This model predicts that the candle reaches height <m>0</m> when</p>

  <md>18-2t=0.</md>

  <p>So</p>

  <md>t=9.</md>

  <p>The model says the candle lasts <m>9</m> hours.</p>

  <p>But this prediction depends on the assumption that the burn rate stays constant. If the candle shape changes or the flame weakens, the model may fail before <m>9</m> hours.</p>

  <note xml:id="note-sec-2-7-models-before-calculus-80">
    <title>Modeling lesson</title>
      <p>A formula can be algebraically correct and still be physically wrong outside the range where its assumptions make sense.</p>
  </note>
  </paragraphs>

  <exercise xml:id="checkpoint-2-7-model-is-a-story">
        <title>What four questions should a model survive?</title>
        <statement>
          <p>
            A model is a mathematical story about reality, not reality itself. In your own words, what four questions should we ask of a model, and why can a formula be algebraically correct yet still be physically wrong outside the range of its assumptions?
          </p>
        </statement>
        <hint>
          <p>
            Ask about assumptions, units, fit, and the region where the story should not be trusted. Algebra checks internal consistency; modeling also checks whether the situation still matches the story.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-power-law-models">
    <title>Power-law scaling</title>

  <p>Linear models are about constant additive change. Power-law models are about scaling.</p>

  <p>Imagine enlarging a square photograph. If every length is multiplied by <m>2</m>, then the area is multiplied by <m>4</m>.</p>

  <p>A <m>3</m>-inch by <m>3</m>-inch square has area</p>

  <md>9\text{ square inches}.</md>

  <p>A <m>6</m>-inch by <m>6</m>-inch square has area</p>

  <md>36\text{ square inches}.</md>

  <p>Doubling the side length quadruples the area:</p>

  <md>36=4\cdot 9.</md>

  <p>This is not linear growth. It is square growth.</p>

  <p>For a square of side length <m>s</m>, the area is</p>

  <md>A(s)=s^2.</md>

  <p>If we multiply the side length by <m>k</m>, then</p>

  <md>A(ks)=(ks)^2=k^2s^2=k^2A(s).</md>

  <p>So scaling the input by <m>k</m> scales the output by <m>k^2</m>.</p>

  <note xml:id="note-sec-2-7-models-before-calculus-81">
    <title>Power-law model</title>
      <p>A power-law model has the form</p>
        <md>y=Cx^p,</md>
      <p>where <m>C</m> and <m>p</m> are constants.</p>
      <p>It satisfies the scaling rule</p>
        <md>y(kx)=C(kx)^p=k^pCx^p=k^p y(x).</md>
  </note>

  <p>The exponent <m>p</m> tells us how strongly the output responds to scaling.</p>

  <md>
    <mrow>\begin{array}{c|c|c}
<strong>model</strong> \amp <strong>doubling input multiplies output by</strong> \amp <strong>common meaning</strong></mrow>
    <mrow>\hline
y=Cx \amp 2 \amp \text{length-like growth}</mrow>
    <mrow>y=Cx^2 \amp 4 \amp \text{area-like growth}</mrow>
    <mrow>y=Cx^3 \amp 8 \amp \text{volume-like growth}</mrow>
    <mrow>y=C\sqrt{x}=Cx^{1/2} \amp \sqrt2 \amp \text{root-type growth}
\end{array}</mrow>
  </md>


  <exercise xml:id="checkpoint-2-7-power-law-scaling">
        <title>What does a power-law model remember about scaling?</title>
        <statement>
          <p>
            Doubling the side of a square quadruples the area. In your own words, what scaling rule does a model <m>y=Cx^p</m> encode, and why is that a different story from constant additive change?
          </p>
        </statement>
        <hint>
          <p>
            Replacing <m>x</m> by <m>kx</m> multiplies the output by <m>k^p</m>. Linear change cares about adding a fixed amount; power-law change cares about stretching the input.
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-2-7-models-before-calculus-3">
    <title>similar tanks</title>
    <statement>
  <p>Suppose two water tanks have the same shape, but one is twice as tall, twice as wide, and twice as deep as the other.</p>

  <p>Every length has been multiplied by <m>2</m>. Volume is three-dimensional, so the larger tank has</p>

  <md>2^3=8</md>

  <p>times the volume.</p>

  <p>If a tank of height <m>h</m> has volume</p>

  <md>V(h)=Ch^3</md>

  <p>for some constant <m>C</m>, then</p>

  <md>V(2h)=C(2h)^3=8Ch^3=8V(h).</md>

  <p>This is why a small-looking change in length can make a large change in volume.</p>
    </statement>
  </example>

  <example xml:id="ex-sec-2-7-models-before-calculus-4">
    <title>units suggest a power</title>
    <statement>
  <p>Suppose a quantity depends only on a length <m>r</m>, and the output has units of area. Since area units are length squared, a first guess is</p>

  <md>A(r)=Cr^2.</md>

  <p>For a circle, the constant is</p>

  <md>C=\pi,</md>

  <p>so</p>

  <md>A(r)=\pi r^2.</md>

  <p>If the output has units of volume, a first guess is</p>

  <md>V(r)=Cr^3.</md>

  <p>For a sphere, the constant is</p>

  <md>C=\frac43\pi,</md>

  <p>so</p>

  <md>V(r)=\frac43\pi r^3.</md>

  <p>Units do not prove the formula. But they often tell us what kind of formula to expect.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-exponential-models">
    <title>Exponential growth and decay</title>

  <p>Some quantities do not add the same amount in equal time steps. They multiply by the same factor.</p>

  <p>Suppose a bacterial culture grows by <m>50\%</m> every hour. If it starts with <m>100</m> bacteria, then after each hour the population is multiplied by <m>1.5</m>.</p>

  <md>
    <mrow>\begin{array}{c|ccccc}
t\text{ (hours)} \amp 0 \amp 1 \amp 2 \amp 3 \amp 4</mrow>
    <mrow>\hline
P(t) \amp 100 \amp 150 \amp 225 \amp 337.5 \amp 506.25
\end{array}</mrow>
  </md>

  <p>The differences are not constant:</p>

  <md>150-100=50,</md>

  <md>225-150=75,</md>

  <md>337.5-225=112.5.</md>

  <p>But the ratios are constant:</p>

  <md>\frac{150}{100}=1.5,
\qquad
\frac{225}{150}=1.5,
\qquad
\frac{337.5}{225}=1.5.</md>

  <p>This is exponential growth.</p>

  <p>The model is</p>

  <md>P(t)=100(1.5)^t.</md>

  <note xml:id="note-sec-2-7-models-before-calculus-82">
    <title>Exponential model</title>
      <p>An exponential model has the form</p>
        <md>y=Aa^t,</md>
      <p>where</p>
        <md>A&gt;0,\qquad a&gt;0.</md>
      <p>The number <m>A</m> is the initial value. The number <m>a</m> is the growth factor per unit time.</p>
      <p>If <m>a&gt;1</m>, the model grows.</p>
      <p>If <m>0&lt;a&lt;1</m>, the model decays.</p>
  </note>

  <example xml:id="ex-sec-2-7-models-before-calculus-5">
    <title>half-life</title>
    <statement>
  <p>A substance has half-life <m>8</m> days. That means its amount is multiplied by <m>\frac12</m> every <m>8</m> days.</p>

  <p>If the starting amount is <m>M_0</m>, then a model for the amount after <m>t</m> days is</p>

  <md>M(t)=M_0\left(\frac12\right)^{t/8}.</md>

  <p>Check the half-life property:</p>

  <md>M(t+8)
=
M_0\left(\frac12\right)^{(t+8)/8}
=
M_0\left(\frac12\right)^{t/8+1}
=
\frac12 M_0\left(\frac12\right)^{t/8}
=
\frac12 M(t).</md>

  <p>Every <m>8</m> days, the amount is cut in half.</p>
    </statement>
  </example>

  <example xml:id="ex-sec-2-7-models-before-calculus-6">
    <title>doubling time</title>
    <statement>
  <p>A population doubles every <m>5</m> years. If the initial population is <m>P_0</m>, then</p>

  <md>P(t)=P_0\,2^{t/5}.</md>

  <p>Indeed,</p>

  <md>P(t+5)
=
P_0\,2^{(t+5)/5}
=
P_0\,2^{t/5+1}
=
2P(t).</md>

  <p>The exponent</p>

  <md>\frac{t}{5}</md>

  <p>counts how many doubling periods have passed.</p>

  <figure xml:id="fig-linear-versus-exponential-models">
    <caption>Linear growth adds equal amounts. Exponential growth multiplies by equal factors.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.74\textwidth,
    height=0.46\textwidth,
    xmin=0, xmax=5,
    ymin=0, ymax=35,
    axis lines=left,
    xlabel={\(t\)},
    ylabel={output},
    xtick={0,1,2,3,4,5},
    ytick={0,5,10,20,30},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
    legend style={at={(0.02,0.98)},anchor=north west,legend columns=1,font=\small}
]
\addplot[thick, domain=0:5, samples=2] {4+4*x};
\addlegendentry{linear: \(4+4t\)}
\addplot[thick, dashed, domain=0:5, samples=100] {4*1.5^x};
\addlegendentry{exponential: \(4(1.5)^t\)}
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>
    </statement>
  </example>

  <paragraphs>
    <title>A warning</title>

  <p>Exponential growth can be a good short-term model and a terrible long-term model.</p>

  <p>A bacterial population cannot grow forever in a closed dish. A rumor cannot spread to more people than exist. A bank account cannot earn the same interest rate through every possible economic condition.</p>

  <p>A model may be useful because it is locally accurate, even when it is not globally permanent. Calculus will often help us decide what “locally” means.</p>
  </paragraphs>

  <exercise xml:id="checkpoint-2-7-linear-versus-exponential">
        <title>How do you tell linear growth from exponential growth in data?</title>
        <statement>
          <p>
            A linear model adds the same amount in equal time steps. An exponential model multiplies by the same factor. In your own words, what should you look for in a table of values, and why can exponential growth be a good short-term model and a terrible long-term one?
          </p>
        </statement>
        <hint>
          <p>
            Constant differences suggest addition. Constant ratios suggest multiplication. A closed dish, a finite population, or a changing interest rate can break a multiplicative story even if the first few steps look exponential.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-periodic-motion-models">
    <title>Periodic motion</title>

  <p>Some quantities repeat.</p>

  <p>A Ferris wheel turns. A tide rises and falls. A weight on a spring moves up and down. A point on a rotating tire returns to its starting height again and again.</p>

  <p>The basic models for repeating motion use sine and cosine.</p>

  <example xml:id="ex-sec-2-7-models-before-calculus-8">
    <title>height on a Ferris wheel</title>
    <statement>
  <p>Suppose a Ferris wheel has center <m>20</m> meters above the ground and radius <m>18</m> meters. One full rotation takes <m>30</m> seconds.</p>

  <p>If a rider starts at the midline and is moving upward at time <m>t=0</m>, then the height can be modeled by</p>

  <md>H(t)=20+18\sin\left(\frac{\pi t}{15}\right).</md>

  <p>The number <m>20</m> is the midline. The number <m>18</m> is the amplitude. The period is <m>30</m>, because</p>

  <md>\sin\left(\frac{\pi(t+30)}{15}\right)
=
\sin\left(\frac{\pi t}{15}+2\pi\right)
=
\sin\left(\frac{\pi t}{15}\right).</md>

  <p>So</p>

  <md>H(t+30)=H(t).</md>

  <p>The height repeats every <m>30</m> seconds.</p>

  <figure xml:id="fig-ferris-wheel-model">
    <caption>A sinusoidal model records a repeating motion by its midline, amplitude, and period.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.76\textwidth,
    height=0.45\textwidth,
    xmin=0, xmax=60,
    ymin=0, ymax=40,
    axis lines=left,
    xlabel={\(t\) (seconds)},
    ylabel={\(H(t)\) (meters)},
    xtick={0,7.5,15,22.5,30,45,60},
    xticklabels={\(0\),\(7.5\),\(15\),\(22.5\),\(30\),\(45\),\(60\)},
    ytick={2,20,38},
    yticklabels={\(2\),\(20\),\(38\)},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=0:60, samples=200] {20+18*sin(deg(pi*x/15))};
\addplot[dashed, domain=0:60, samples=2] {20};
\node[fill=white, inner sep=1.5pt, anchor=south] at (axis cs:42,22.2) {midline \(H=20\)};
\node[anchor=south] at (axis cs:7.5,38) {top};
\node[anchor=south west] at (axis cs:23.8,3.8) {bottom};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <note xml:id="note-sec-2-7-models-before-calculus-83">
    <title>Periodic model</title>
      <p>A function is <em>periodic</em> if there is a positive number <m>T</m> such that</p>
        <md>f(t+T)=f(t)</md>
      <p>for every allowed <m>t</m>.</p>
      <p>The smallest such positive <m>T</m>, when it exists, is called the <em>period</em>.</p>
  </note>

  <p>For</p>

  <md>A\sin(Bt)</md>

  <p>and</p>

  <md>A\cos(Bt),</md>

  <p>the amplitude is</p>

  <md>|A|,</md>

  <p>and the period is</p>

  <md>\frac{2\pi}{B},
\qquad B&gt;0.</md>
    </statement>
  </example>

  <example xml:id="ex-sec-2-7-models-before-calculus-9">
    <title>Example</title>
    <statement>
  <p>The function</p>

  <md>y(t)=5\cos(4t)</md>

  <p>has amplitude</p>

  <md>5</md>

  <p>and period</p>

  <md>\frac{2\pi}{4}=\frac{\pi}{2}.</md>

  <p>The output repeats whenever <m>t</m> increases by <m>\frac{\pi}{2}</m>.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-choosing-model-units-data">
    <title>Choosing a model from units and data</title>

  <p>A model is a choice. The choice should be guided by the situation.</p>

  <table xml:id="tab-sec-2-7-models-before-calculus-2">
    <tabular halign="left">
      <row bottom="minor">
        <cell><strong>family</strong></cell>
        <cell><strong>typical form</strong></cell>
        <cell><strong>natural domain</strong></cell>
        <cell><strong>signature behavior</strong></cell>
      </row>
      <row bottom="minor">
        <cell>linear</cell>
        <cell><m>mx+b</m></cell>
        <cell><m>\mathbb R</m></cell>
        <cell>constant additive change</cell>
      </row>
      <row bottom="minor">
        <cell>power</cell>
        <cell><m>Cx^p</m></cell>
        <cell>depends on <m>p</m></cell>
        <cell>scaling by powers</cell>
      </row>
      <row bottom="minor">
        <cell>polynomial</cell>
        <cell><m>a_nx^n+\cdots+a_0</m></cell>
        <cell><m>\mathbb R</m></cell>
        <cell>smooth algebraic growth</cell>
      </row>
      <row bottom="minor">
        <cell>rational</cell>
        <cell><m>\dfrac{p(x)}{q(x)}</m></cell>
        <cell><m>q(x)\neq0</m></cell>
        <cell>ratios, holes, asymptotes</cell>
      </row>
      <row bottom="minor">
        <cell>algebraic</cell>
        <cell>roots and algebraic operations</cell>
        <cell>depends on roots and denominators</cell>
        <cell>geometric formulas</cell>
      </row>
      <row bottom="minor">
        <cell>trigonometric</cell>
        <cell><m>\sin x,\cos x,\tan x</m></cell>
        <cell>depends on function</cell>
        <cell>periodic motion</cell>
      </row>
      <row bottom="minor">
        <cell>exponential</cell>
        <cell><m>a^x</m></cell>
        <cell><m>\mathbb R</m></cell>
        <cell>constant multiplicative change</cell>
      </row>
      <row>
        <cell>logarithmic</cell>
        <cell><m>\log_a x</m></cell>
        <cell><m>(0,\infty)</m></cell>
        <cell>inverse of exponential growth</cell>
      </row>
    </tabular>
  </table>


  <exercise xml:id="checkpoint-2-7-residuals-not-exact-fit">
        <title>Why is an exact fit not the same as understanding?</title>
        <statement>
          <p>
            Residuals are the errors a model leaves behind, and a high-degree polynomial can pass through every data point. In your own words, why should we ask whether a formula explains a pattern rather than only whether it hits the measured points, and what can go wrong just outside the data?
          </p>
        </statement>
        <hint>
          <p>
            Exact interpolation can wiggle wildly between points or make absurd predictions nearby. A good model should leave small residuals without being more complicated than the situation deserves.
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-2-7-models-before-calculus-10">
    <title>linear or exponential?</title>
    <statement>
  <p>A small plant is measured once per week.</p>

  <md>
    <mrow>\begin{array}{c|ccccc}
t\text{ (weeks)} \amp 0 \amp 1 \amp 2 \amp 3 \amp 4</mrow>
    <mrow>\hline
H(t)\text{ (cm)} \amp 10 \amp 13 \amp 16 \amp 19 \amp 22
\end{array}</mrow>
  </md>

  <p>The differences are</p>

  <md>3,\quad 3,\quad 3,\quad 3.</md>

  <p>A linear model is natural:</p>

  <md>H(t)=10+3t.</md>

  <p>Now compare a different table:</p>

  <md>
    <mrow>\begin{array}{c|ccccc}
t\text{ (hours)} \amp 0 \amp 1 \amp 2 \amp 3 \amp 4</mrow>
    <mrow>\hline
P(t) \amp 100 \amp 150 \amp 224 \amp 337 \amp 506
\end{array}</mrow>
  </md>

  <p>The differences are</p>

  <md>50,\quad 74,\quad 113,\quad 169,</md>

  <p>not constant. But the ratios are approximately</p>

  <md>1.50,\quad 1.49,\quad 1.50,\quad 1.50.</md>

  <p>An exponential model is natural:</p>

  <md>P(t)\approx 100(1.5)^t.</md>

  <p>The symbol <m>\approx</m> matters. Data are often measured with error, and real processes rarely follow a model perfectly.</p>
    </statement>
  </example>

  <paragraphs>
    <title>Residuals: the errors a model leaves behind</title>

  <p>Suppose data points are</p>

  <md>(x_1,y_1),\quad (x_2,y_2),\quad \ldots,\quad (x_n,y_n),</md>

  <p>and a model predicts</p>

  <md>y=f(x).</md>

  <p>At input <m>x_i</m>, the prediction error is</p>

  <md>y_i-f(x_i).</md>

  <p>These errors are called <em>residuals</em>. A good model should leave small residuals without using a formula that is more complicated than the situation deserves.</p>

  <note xml:id="note-sec-2-7-models-before-calculus-84">
    <title>A modeling habit</title>
      <p>Do not ask only:</p>
        <md>\text{Can I find a formula through the data?}</md>
      <p>Ask also:</p>
        <md>\text{Does the formula explain the pattern?}</md>
        <md>\text{Are the units meaningful?}</md>
        <md>\text{Does the formula behave reasonably outside the data?}</md>
  </note>
  </paragraphs>

  <example xml:id="ex-sec-2-7-models-before-calculus-12">
    <title>a model that fits too well</title>
    <statement>
  <p>Given five data points, it is often possible to find a polynomial of degree <m>4</m> that passes exactly through all of them. That does not mean the polynomial is the best model.</p>

  <p>A high-degree polynomial may wiggle wildly between data points or make absurd predictions just outside the measured interval. Exact fit is not the same as understanding.</p>

  <p>For calculus, the simplest useful model is often better than a complicated perfect fit.</p>

  <p>Models give us functions to study. Technology gives us quick graphs of those functions. But a graph on a screen is not the function itself. It is a picture made with choices: a window, a scale, and a finite number of sampled points.</p>

  <p>Those choices can hide the truth.</p>
    </statement>
  </example>
  </subsection>



</section>
