<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-2-6-a-catalog-of-essential-functions">
  <title>A catalog of essential functions</title>

  <p>We can now build functions by formulas, graphs, tables, words, transformations, arithmetic, composition, and inverses. But calculus does not treat every function as a stranger.</p>

  <p>Some families appear again and again. Lines describe constant rates. Polynomials describe smooth algebraic change. Rational functions show ratios and asymptotes. Trigonometric functions describe periodic motion. Exponential functions describe constant percentage change. Logarithms undo exponentials.</p>

  <p>This catalog is not a list to memorize. It is a set of familiar shapes and stories.</p>

  <subsection xml:id="subsec-linear-functions-units">
    <title>Linear functions and units</title>

  <p>The distance function for constant velocity had the form</p>

  <md>s(t)=s_0+vt.</md>

  <p>This is a linear function.</p>

  <p>In general, a linear function has the form</p>

  <md>f(x)=mx+b.</md>

  <p>The number <m>m</m> is the slope. It measures output change per unit input change:</p>

  <md>m=\frac{\text{change in output}}{\text{change in input}}.</md>

  <p>The number <m>b</m> is the value at input <m>0</m>:</p>

  <md>f(0)=b.</md>


  <exercise xml:id="checkpoint-2-6-linear-signature">
        <title>What is the signature of a linear function?</title>
        <statement>
          <p>
            A linear function has the form <m>f(x)=mx+b</m>. In your own words, what does the constant average rate of change <m>m</m> remember, and why do the units of <m>m</m> already tell you how outputs change when inputs change?
          </p>
        </statement>
        <hint>
          <p>
            Equal input steps produce equal output steps. The slope’s units are output units divided by input units, which is exactly the instruction <q>change per unit change.</q>
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-2-6-a-catalog-of-essential-functions-1">
    <title>a cost model</title>
    <statement>
  <p>Suppose a taxi ride costs <m>4</m> dollars to begin and <m>2.50</m> dollars per mile. If <m>m</m> is the number of miles, then</p>

  <md>C(m)=4+2.50m.</md>

  <p>The slope is</p>

  <md>2.50\frac{\text{dollars}}{\text{mile}}.</md>

  <p>The intercept is</p>

  <md>4\text{ dollars}.</md>

  <p>The units make the formula readable:</p>

  <md>2.50\frac{\text{dollars}}{\text{mile}}\cdot m\text{ miles}
=
2.50m\text{ dollars}.</md>

  <p>A linear model says that equal changes in input produce equal changes in output.</p>
    </statement>
  </example>

  <example xml:id="ex-sec-2-6-a-catalog-of-essential-functions-2">
    <title>converting temperature</title>
    <statement>
  <p>The Celsius-to-Fahrenheit function is</p>

  <md>F(C)=\frac95 C+32.</md>

  <p>The slope is</p>

  <md>\frac95.</md>

  <p>This says that increasing the Celsius temperature by <m>1^\circ</m> increases the Fahrenheit temperature by</p>

  <md>\frac95^\circ.</md>

  <p>The intercept is <m>32</m>, because</p>

  <md>0^\circ\text{ C}=32^\circ\text{ F}.</md>

  <p>The graph is a line, but the meaning is a conversion between two scales.</p>

  <note xml:id="note-sec-2-6-a-catalog-of-essential-functions-112">
    <title>Linear signature</title>
      <p>A linear function has constant average rate of change:</p>
        <md>\frac{f(x_2)-f(x_1)}{x_2-x_1}=m</md>
      <p>for every <m>x_1\neq x_2</m>.</p>
  </note>

  <p>Later, the derivative of a linear function will simply be its slope.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-powers-polynomials">
    <title>Powers and polynomials</title>

  <p>Power functions have the form</p>

  <md>f(x)=x^p.</md>

  <p>The most familiar powers are</p>

  <md>x,\qquad x^2,\qquad x^3,\qquad x^4.</md>

  <p>They already appeared in the first chapter through finite differences. Squares grew with odd-number differences. Cubes grew faster.</p>

  <p>Power functions describe scaling.</p>

  <example xml:id="ex-sec-2-6-a-catalog-of-essential-functions-3">
    <title>area and volume</title>
    <statement>
  <p>The area of a circle of radius <m>r</m> is</p>

  <md>A(r)=\pi r^2.</md>

  <p>If the radius doubles, the area becomes</p>

  <md>A(2r)=\pi(2r)^2=4\pi r^2=4A(r).</md>

  <p>So doubling length multiplies area by <m>4</m>.</p>

  <p>The volume of a sphere of radius <m>r</m> is</p>

  <md>V(r)=\frac43\pi r^3.</md>

  <p>If the radius doubles, the volume becomes</p>

  <md>V(2r)=\frac43\pi(2r)^3=8V(r).</md>

  <p>So doubling length multiplies volume by <m>8</m>.</p>

  <p>This is the basic difference between square growth and cubic growth.</p>

  <note xml:id="note-sec-2-6-a-catalog-of-essential-functions-113">
    <title>Power-law scaling</title>
      <p>For</p>
        <md>f(x)=Cx^p,</md>
      <p>multiplying the input by <m>k</m> multiplies the output by <m>k^p</m>:</p>
        <md>f(kx)=C(kx)^p=k^pCx^p=k^p f(x).</md>
  </note>

  <p>A polynomial is a finite sum of power functions with nonnegative integer powers:</p>

  <md>p(x)=a_nx^n+a_{n-1}x^{n-1}+\cdots+a_1x+a_0.</md>

  <p>Here the coefficients</p>

  <md>a_0,a_1,\ldots,a_n</md>

  <p>are real numbers, and <m>a_n\neq 0</m>. The number <m>n</m> is the degree of the polynomial.</p>
    </statement>
  </example>

  <example xml:id="ex-sec-2-6-a-catalog-of-essential-functions-4">
    <title>Examples</title>
    <statement>
  <p>The function</p>

  <md>p(x)=3x^4-2x^2+7x-1</md>

  <p>is a polynomial of degree <m>4</m>.</p>

  <p>The function</p>

  <md>q(x)=5x^2-9</md>

  <p>is a polynomial of degree <m>2</m>.</p>

  <p>The function</p>

  <md>r(x)=8</md>

  <p>is a constant polynomial. Its degree is <m>0</m>, as long as the constant is not <m>0</m>.</p>

  <p>Polynomials are defined for all real inputs. Their domains are</p>

  <md>\mathbb R.</md>

  <p>Their graphs have no breaks, holes, or vertical asymptotes. They may turn, cross the axis, or flatten, but they do not suddenly jump.</p>
    </statement>
  </example>

  <paragraphs>
    <title>Leading behavior</title>

  <p>Far from the origin, the highest-power term usually dominates the shape. For</p>

  <md>p(x)=2x^5-10x^2+3,</md>

  <p>the term <m>2x^5</m> eventually becomes much larger in size than <m>-10x^2</m> and <m>3</m>. So for large positive or negative <m>x</m>, the graph behaves roughly like</p>

  <md>y=2x^5.</md>

  <p>This idea will become more precise when we study limits at infinity.</p>
  </paragraphs>
  </subsection>

  <subsection xml:id="subsec-rational-functions-asymptotic">
    <title>Rational functions and asymptotic behavior</title>

  <p>A rational function is a quotient of two polynomials:</p>

  <md>r(x)=\frac{p(x)}{q(x)}.</md>

  <p>The denominator cannot be zero. Thus the natural domain is</p>

  <md>\{x\in\mathbb R:q(x)\neq 0\}.</md>

  <example xml:id="ex-sec-2-6-a-catalog-of-essential-functions-6">
    <title>Example</title>
    <statement>
  <p>The function</p>

  <md>r(x)=\frac{x+1}{x^2-4}</md>

  <p>is undefined when</p>

  <md>x^2-4=0.</md>

  <p>Since</p>

  <md>x^2-4=(x-2)(x+2),</md>

  <p>the forbidden inputs are</p>

  <md>x=2
\qquad\text{and}\qquad
x=-2.</md>

  <p>So the natural domain is</p>

  <md>(-\infty,-2)\cup(-2,2)\cup(2,\infty).</md>

  <p>Rational functions often show behavior that polynomials do not: vertical asymptotes, horizontal asymptotes, and holes.</p>
    </statement>
  </example>

  <example xml:id="ex-sec-2-6-a-catalog-of-essential-functions-7">
    <title>average cost</title>
    <statement>
  <p>Suppose producing <m>x</m> items costs</p>

  <md>C(x)=100+5x</md>

  <p>dollars. The first <m>100</m> dollars is a fixed setup cost. The <m>5x</m> dollars is the variable cost.</p>

  <p>The average cost per item is</p>

  <md>A(x)=\frac{C(x)}{x}
=
\frac{100+5x}{x}
=
5+\frac{100}{x},
\qquad x&gt;0.</md>

  <p>As <m>x</m> grows, the term</p>

  <md>\frac{100}{x}</md>

  <p>gets smaller. The average cost approaches <m>5</m> dollars per item.</p>

  <p>It never equals <m>5</m>, but it gets closer and closer. The line</p>

  <md>y=5</md>

  <p>is a horizontal asymptote.</p>

  <figure xml:id="fig-average-cost-asymptote">
    <caption>The average cost approaches the variable cost per item as the fixed cost is spread over more items.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.72\textwidth,
    height=0.43\textwidth,
    xmin=0, xmax=50,
    ymin=0, ymax=35,
    axis lines=left,
    xlabel={\(x\) items},
    ylabel={\(A(x)\) dollars/item},
    xtick={0,10,20,30,40,50},
    ytick={0,5,10,20,30},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=3.5:50, samples=120] {5+100/x};
\addplot[dashed, domain=0:50, samples=2] {5};
\node[anchor=west] at (axis cs:24,11) {\(A(x)=5+\frac{100}{x}\)};
\node[anchor=north west] at (axis cs:28,3.0) {\(y=5\)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>
    </statement>
  </example>

  <paragraphs>
    <title>Vertical asymptotes</title>

  <p>The function</p>

  <md>f(x)=\frac{1}{x-2}</md>

  <p>is undefined at <m>x=2</m>. As <m>x</m> gets close to <m>2</m>, the denominator gets close to <m>0</m>, and the quotient grows very large in size.</p>

  <p>The vertical line</p>

  <md>x=2</md>

  <p>is a vertical asymptote.</p>

  <p>We will define this precisely with limits. For now, the important picture is this: a rational function can grow without bound near a forbidden input.</p>
  </paragraphs>

  <paragraphs>
    <title>A hole instead of an asymptote</title>

  <p>Not every forbidden input creates an asymptote. Consider</p>

  <md>h(x)=\frac{x^2-1}{x-1}.</md>

  <p>The input <m>x=1</m> is forbidden, but for <m>x\neq1</m>,</p>

  <md>h(x)=x+1.</md>

  <p>So the graph is the line <m>y=x+1</m> with a hole at the missing point</p>

  <md>(1,2).</md>

  <p>This is why domain restrictions matter. The formula simplifies, but the missing input remains missing.</p>
  </paragraphs>

  <exercise xml:id="checkpoint-2-6-hole-versus-asymptote">
        <title>Why is a missing input not always a vertical asymptote?</title>
        <statement>
          <p>
            A rational function is undefined where its denominator is zero, but <m>\frac{x^2-1}{x-1}</m> has a hole rather than an asymptote at <m>x=1</m>. In your own words, what nearby behavior distinguishes a hole from a vertical asymptote?
          </p>
        </statement>
        <hint>
          <p>
            After cancellation, nearby values may settle toward a finite height. Near an asymptote, nearby values grow without bound instead of settling.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-algebraic-functions">
    <title>Algebraic functions</title>

  <p>Algebraic functions are built from polynomials using a finite number of algebraic operations: addition, subtraction, multiplication, division, and roots.</p>

  <p>Examples include</p>

  <md>\sqrt{x},</md>

  <md>\sqrt{4-x^2},</md>

  <md>\frac{\sqrt{x+1}}{x-3},</md>

  <p>and</p>

  <md>\sqrt[3]{x^2+1}.</md>

  <p>These functions often come from geometry.</p>

  <example xml:id="ex-sec-2-6-a-catalog-of-essential-functions-10">
    <title>the upper semicircle</title>
    <statement>
  <p>The circle of radius <m>2</m> centered at the origin satisfies</p>

  <md>x^2+y^2=4.</md>

  <p>Solving for <m>y</m>, we get</p>

  <md>y=\pm\sqrt{4-x^2}.</md>

  <p>The upper half is</p>

  <md>f(x)=\sqrt{4-x^2}.</md>

  <p>The square root requires</p>

  <md>4-x^2\ge 0.</md>

  <p>Thus</p>

  <md>-2\le x\le 2.</md>

  <p>The natural domain is</p>

  <md>[-2,2].</md>

  <p>The range is</p>

  <md>[0,2].</md>
    </statement>
  </example>

  <example xml:id="ex-sec-2-6-a-catalog-of-essential-functions-11">
    <title>a root changes smoothness</title>
    <statement>
  <p>The function</p>

  <md>f(x)=\sqrt{x}</md>

  <p>has domain</p>

  <md>[0,\infty).</md>

  <p>Its graph starts at <m>(0,0)</m> and rises slowly after an initially steep beginning. Later, when we study derivatives, we will see that the tangent near <m>x=0</m> is very different from the tangent of a polynomial.</p>

  <p>So algebraic functions can be continuous and natural while still having behavior that needs care.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-trig-functions-radians">
    <title>Trigonometric functions in radians</title>

  <p>Motion can repeat. A wheel turns. A pendulum swings. A tide rises and falls. A point on a spring oscillates.</p>

  <p>The basic functions for periodic motion are sine and cosine.</p>

  <p>On the unit circle, an angle <m>\theta</m> determines a point</p>

  <md>(\cos\theta,\sin\theta).</md>

  <p>The cosine is the horizontal coordinate. The sine is the vertical coordinate.</p>

  <figure xml:id="fig-unit-circle-sin-cos">
    <caption>On the unit circle, cosine and sine are coordinates of the point reached by angle <m>\theta</m>.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}[scale=2.0, every node/.style={font=\small}]
  \draw[-&gt;] (-1.25,0) -- (1.35,0) node[right] {\(x\)};
  \draw[-&gt;] (0,-1.25) -- (0,1.35) node[above] {\(y\)};
  \draw[thick] (0,0) circle (1);

  \def\ang{40}
  \coordinate (P) at ({cos(\ang)},{sin(\ang)});
  \draw[very thick] (0,0) -- (P);
  \draw[dashed] (P) -- ({cos(\ang)},0);
  \draw[dashed] (P) -- (0,{sin(\ang)});
  \fill (P) circle (1.2pt);

  \draw[-&gt;] (0.28,0) arc (0:\ang:0.28);
  \node at (0.38,0.14) {\(\theta\)};
  \node[above right] at (P) {\((\cos\theta,\sin\theta)\)};
  \node[below] at ({cos(\ang)/2},0) {\(\cos\theta\)};
  \node[left] at (0,{sin(\ang)/2}) {\(\sin\theta\)};
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <p>The angle must be measured in radians for calculus.</p>

  <p>A radian is not an arbitrary unit. It is defined by arc length:</p>

  <md>\theta=\frac{\text{arc length}}{\text{radius}}.</md>

  <p>On the unit circle, the radius is <m>1</m>, so the radian measure of an angle equals the length of the arc it cuts off.</p>

  <p>A full turn has circumference</p>

  <md>2\pi,</md>

  <p>so a full turn is</p>

  <md>2\pi</md>

  <p>radians.</p>

  <p>A half turn is</p>

  <md>\pi</md>

  <p>radians.</p>

  <p>A quarter turn is</p>

  <md>\frac{\pi}{2}</md>

  <p>radians.</p>

  <note xml:id="note-sec-2-6-a-catalog-of-essential-functions-114">
    <title>Why radians matter</title>
      <p>The derivative formulas for sine and cosine take their clean form only when angles are measured in radians.</p>
      <p>Later we will use the limit</p>
        <md>\lim_{h\to0}\frac{\sin h}{h}=1.</md>
      <p>That statement is true with <m>h</m> in radians.</p>
  </note>

  <p>The sine and cosine functions have domain all real numbers:</p>

  <md>\mathbb R.</md>

  <p>Their ranges are</p>

  <md>[-1,1].</md>

  <p>They repeat every <m>2\pi</m>:</p>

  <md>\sin(x+2\pi)=\sin x,</md>

  <md>\cos(x+2\pi)=\cos x.</md>

  <p>The tangent function is</p>

  <md>\tan x=\frac{\sin x}{\cos x}.</md>

  <p>It is undefined where</p>

  <md>\cos x=0.</md>

  <p>Thus tangent has vertical asymptotes at</p>

  <md>x=\frac{\pi}{2}+k\pi,
\qquad k\in\mathbb Z.</md>

  <p>The other trigonometric functions are reciprocals:</p>

  <md>\sec x=\frac{1}{\cos x},
\qquad
\csc x=\frac{1}{\sin x},
\qquad
\cot x=\frac{\cos x}{\sin x}.</md>

  <p>Their domains must exclude the points where the denominators are zero.</p>


  <exercise xml:id="checkpoint-2-6-why-radians-matter">
        <title>Why does calculus measure angles in radians?</title>
        <statement>
          <p>
            On the unit circle, cosine and sine are coordinates of a point reached by an angle. In your own words, what makes a radian a geometric unit rather than an arbitrary scale, and why will that choice matter for later derivative formulas?
          </p>
        </statement>
        <hint>
          <p>
            A radian is arc length divided by radius. On the unit circle, the angle equals the arc it cuts off. The clean limit <m>\lim_{h\to0}\frac{\sin h}{h}=1</m> uses that geometric scale.
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-2-6-a-catalog-of-essential-functions-12">
    <title>periodic motion</title>
    <statement>
  <p>A point moving up and down with height</p>

  <md>y(t)=3\sin(2t)</md>

  <p>has amplitude <m>3</m>, because the output ranges from <m>-3</m> to <m>3</m>.</p>

  <p>The factor <m>2</m> inside the sine makes the motion repeat faster than <m>\sin t</m>. Since sine repeats when its input increases by <m>2\pi</m>, we need</p>

  <md>2t</md>

  <p>to increase by <m>2\pi</m>. Therefore <m>t</m> must increase by <m>\pi</m>. The period is</p>

  <md>\pi.</md>

  <p>The function</p>

  <md>y(t)=3\sin(2t)</md>

  <p>repeats every <m>\pi</m> units of time.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-exponential-functions">
    <title>Exponential functions</title>

  <p>Linear functions add the same amount for each equal step. Exponential functions multiply by the same factor for each equal step.</p>

  <p>A function of the form</p>

  <md>f(x)=a^x,</md>

  <p>where</p>

  <md>a&gt;0,\qquad a\neq1,</md>

  <p>is an exponential function.</p>

  <p>If <m>a&gt;1</m>, the function grows. If <m>0&lt;a&lt;1</m>, the function decays.</p>


  <exercise xml:id="checkpoint-2-6-exponential-versus-linear">
        <title>How is exponential change different from linear change?</title>
        <statement>
          <p>
            Linear functions add the same amount for each equal step. Exponential functions multiply by the same factor. In your own words, what is the signature of <m>f(x)=a^x</m>, and why can exponential growth never output zero or a negative number?
          </p>
        </statement>
        <hint>
          <p>
            For a fixed step <m>h</m>, the identity <m>a^{x+h}=a^x a^h</m> multiplies the old output by the same factor no matter where the step begins. Powers of a positive base stay positive.
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-2-6-a-catalog-of-essential-functions-13">
    <title>doubling</title>
    <statement>
  <p>Suppose a bacterial population doubles every <m>3</m> hours. If the initial population is <m>P_0</m>, then after <m>t</m> hours a natural model is</p>

  <md>P(t)=P_0\,2^{t/3}.</md>

  <p>Check the doubling property:</p>

  <md>P(t+3)
=
P_0\,2^{(t+3)/3}
=
P_0\,2^{t/3+1}
=
2P_0\,2^{t/3}
=
2P(t).</md>

  <p>Every time <m>t</m> increases by <m>3</m>, the output doubles.</p>

  <p>This is the signature of exponential growth: equal time steps produce equal multiplicative changes.</p>
    </statement>
  </example>

  <example xml:id="ex-sec-2-6-a-catalog-of-essential-functions-14">
    <title>half-life</title>
    <statement>
  <p>Suppose a radioactive substance loses half its mass every <m>8</m> days. If the initial mass is <m>M_0</m>, then</p>

  <md>M(t)=M_0\left(\frac12\right)^{t/8}.</md>

  <p>Then</p>

  <md>M(t+8)
=
M_0\left(\frac12\right)^{(t+8)/8}
=
\frac12 M_0\left(\frac12\right)^{t/8}
=
\frac12 M(t).</md>

  <p>Every <m>8</m> days, the mass is cut in half.</p>

  <note xml:id="note-sec-2-6-a-catalog-of-essential-functions-115">
    <title>Exponential signature</title>
      <p>For <m>f(x)=a^x</m>,</p>
        <md>f(x+h)=a^{x+h}=a^x a^h.</md>
      <p>For a fixed step <m>h</m>, the output is multiplied by the same factor <m>a^h</m>, no matter where the step begins.</p>
  </note>

  <p>The domain of <m>a^x</m> is</p>

  <md>\mathbb R.</md>

  <p>The range is</p>

  <md>(0,\infty).</md>

  <p>Exponential functions never output <m>0</m> or a negative number.</p>

  <p>One exponential function will become especially important in calculus:</p>

  <md>e^x.</md>

  <p>The number <m>e</m> is approximately</p>

  <md>2.71828.</md>

  <p>Its special role will not be assumed. We will earn it later: <m>e^x</m> is the exponential function whose rate of change equals itself.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-logarithms-inverse-functions">
    <title>Logarithms as inverse functions</title>

  <p>Since</p>

  <md>a^x</md>

  <p>is one-to-one for <m>a&gt;0</m>, <m>a\neq1</m>, it has an inverse. That inverse is the logarithm base <m>a</m>.</p>

  <definition xml:id="def-sec-2-6-a-catalog-of-essential-functions-116">
    <title>Definition</title>
    <statement>
      <p>For <m>a&gt;0</m>, <m>a\neq1</m>,</p>
        <md>\log_a x=y</md>
      <p>means</p>
        <md>a^y=x.</md>
      <p>In words, <m>\log_a x</m> is the exponent needed on <m>a</m> to get <m>x</m>.</p>
    </statement>
  </definition>

  <p>For example,</p>

  <md>\log_2 8=3</md>

  <p>because</p>

  <md>2^3=8.</md>

  <p>Also,</p>

  <md>\log_{10} 1000=3</md>

  <p>because</p>

  <md>10^3=1000.</md>

  <p>The logarithm has domain</p>

  <md>(0,\infty)</md>

  <p>and range</p>

  <md>\mathbb R.</md>

  <p>This is the reverse of the exponential function, whose domain is <m>\mathbb R</m> and range <m>(0,\infty)</m>.</p>

  <figure xml:id="fig-exp-log-inverses">
    <caption>Exponential and logarithm functions are inverses, so their graphs reflect across the line <m>y=x</m>.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.72\textwidth,
    height=0.50\textwidth,
    xmin=-3, xmax=5,
    ymin=-3, ymax=5,
    axis lines=middle,
    xlabel={\(x\)},
    ylabel={\(y\)},
    xtick={-2,0,1,2,4},
    ytick={-2,0,1,2,4},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
    legend style={at={(0.02,0.98)},anchor=north west,legend columns=1,font=\small}
]
\addplot[thick, domain=-3:1.55, samples=100] {exp(x)};
\addlegendentry{\(y=e^x\)}
\addplot[thick, dashed, domain=0.06:5, samples=100] {ln(x)};
\addlegendentry{\(y=\ln x\)}
\addplot[dotted, domain=-3:5, samples=2] {x};
\addlegendentry{\(y=x\)}
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <p>Because logarithms undo exponentials,</p>

  <md>\log_a(a^x)=x</md>

  <p>for every real <m>x</m>, and</p>

  <md>a^{\log_a x}=x</md>

  <p>for every <m>x&gt;0</m>.</p>

  <p>The exponential laws become logarithm laws. Since</p>

  <md>a^u a^v=a^{u+v},</md>

  <p>we get</p>

  <md>\log_a(xy)=\log_a x+\log_a y,
\qquad x&gt;0,\ y&gt;0.</md>

  <p>Since</p>

  <md>(a^u)^r=a^{ru},</md>

  <p>we get</p>

  <md>\log_a(x^r)=r\log_a x,
\qquad x&gt;0.</md>

  <example xml:id="ex-sec-2-6-a-catalog-of-essential-functions-15">
    <title>solving an exponential equation</title>
    <statement>
  <p>Suppose</p>

  <md>2^t=11.</md>

  <p>The solution is</p>

  <md>t=\log_2 11.</md>

  <p>This is already an exact answer. A decimal approximation can be found later, but the logarithm is the number itself.</p>
    </statement>
  </example>

  <paragraphs>
    <title>Natural logarithm</title>

  <p>The logarithm base <m>e</m> is called the natural logarithm:</p>

  <md>\ln x=\log_e x.</md>

  <p>It satisfies</p>

  <md>\ln(e^x)=x</md>

  <p>and</p>

  <md>e^{\ln x}=x,
\qquad x&gt;0.</md>

  <p>The word “natural” will make more sense after derivatives. For now, remember only that <m>e^x</m> and <m>\ln x</m> are inverse functions.</p>
  </paragraphs>

  <exercise xml:id="checkpoint-2-6-logarithm-as-inverse">
        <title>What question does a logarithm answer?</title>
        <statement>
          <p>
            The logarithm base <m>a</m> is the inverse of <m>a^x</m>. In your own words, what question does <m>\log_a x</m> answer, and why are the domain <m>(0,\infty)</m> and range <m>\mathbb R</m> exactly the reverse of the exponential function’s domain and range?
          </p>
        </statement>
        <hint>
          <p>
            <m>\log_a x</m> is the exponent needed on <m>a</m> to get <m>x</m>. An inverse swaps the allowed inputs with the produced outputs, so the graphs reflect across <m>y=x</m>.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-catalog-glance">
    <title>The catalog at a glance</title>

  <table xml:id="tab-sec-2-6-a-catalog-of-essential-functions-3">
    <tabular halign="left">
      <row bottom="minor">
        <cell><strong>family</strong></cell>
        <cell><strong>typical form</strong></cell>
        <cell><strong>natural domain</strong></cell>
        <cell><strong>signature behavior</strong></cell>
      </row>
      <row bottom="minor">
        <cell>linear</cell>
        <cell><m>mx+b</m></cell>
        <cell><m>\mathbb R</m></cell>
        <cell>constant additive change</cell>
      </row>
      <row bottom="minor">
        <cell>power</cell>
        <cell><m>Cx^p</m></cell>
        <cell>depends on <m>p</m></cell>
        <cell>scaling by powers</cell>
      </row>
      <row bottom="minor">
        <cell>polynomial</cell>
        <cell><m>a_nx^n+\cdots+a_0</m></cell>
        <cell><m>\mathbb R</m></cell>
        <cell>smooth algebraic growth</cell>
      </row>
      <row bottom="minor">
        <cell>rational</cell>
        <cell><m>\dfrac{p(x)}{q(x)}</m></cell>
        <cell><m>q(x)\neq0</m></cell>
        <cell>ratios, holes, asymptotes</cell>
      </row>
      <row bottom="minor">
        <cell>algebraic</cell>
        <cell>roots and algebraic operations</cell>
        <cell>depends on roots and denominators</cell>
        <cell>geometric formulas</cell>
      </row>
      <row bottom="minor">
        <cell>trigonometric</cell>
        <cell><m>\sin x,\cos x,\tan x</m></cell>
        <cell>depends on function</cell>
        <cell>periodic motion</cell>
      </row>
      <row bottom="minor">
        <cell>exponential</cell>
        <cell><m>a^x</m></cell>
        <cell><m>\mathbb R</m></cell>
        <cell>constant multiplicative change</cell>
      </row>
      <row>
        <cell>logarithmic</cell>
        <cell><m>\log_a x</m></cell>
        <cell><m>(0,\infty)</m></cell>
        <cell>inverse of exponential growth</cell>
      </row>
    </tabular>
  </table>

  <p>This table is not a substitute for understanding the shapes. It is a map. When a problem mentions constant rate, think linear. When it mentions repeated percentage change, think exponential. When it mentions periodic motion, think trigonometric. When it mentions undoing an exponential, think logarithm.</p>

  <p>A function family is a first guess, not a proof. The next step is modeling: choosing a function because units, data, and behavior suggest it. That will be our last preparation before limits turn “near” into a precise mathematical tool.</p>
  </subsection>



</section>
