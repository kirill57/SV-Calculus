<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-4-7-motion-revisited">
  <title>Motion revisited</title>

  <p>The derivative began as velocity.  Then it became slope, local linear approximation, and a function of position along a graph.  Now the original motion story returns, but with sharper language.</p>

  <p>A position function tells where an object is.  Its derivative tells how fast that position is changing.  The derivative of the derivative tells how fast the velocity is changing.</p>

  <subsection xml:id="subsec-position-velocity-acceleration">
    <title>Position, velocity, and acceleration</title>

  <p>Suppose a cart moves along a straight track.  Its position after <m>t</m> seconds is</p>

  <md>s(t)=t^2</md>

  <p>meters, measured from a chosen origin on the track.</p>

  <p>At <m>t=0</m>,</p>

  <md>s(0)=0.</md>

  <p>At <m>t=1</m>,</p>

  <md>s(1)=1.</md>

  <p>At <m>t=2</m>,</p>

  <md>s(2)=4.</md>

  <p>The cart is moving farther and farther from the origin, and it is doing so faster as time passes.</p>

  <p>The velocity is the derivative of position:</p>

  <md>v(t)=s'(t).</md>

  <p>Since</p>

  <md>s(t)=t^2,</md>

  <p>we have</p>

  <md>v(t)=2t.</md>

  <p>So</p>

  <md>v(1)=2\text{ meters/second},
\qquad
v(2)=4\text{ meters/second}.</md>

  <p>The velocity itself is changing.  The rate of change of velocity is called acceleration.</p>

  <p>Since</p>

  <md>v(t)=2t,</md>

  <p>we have</p>

  <md>a(t)=v'(t)=2.</md>

  <p>So the acceleration is constant:</p>

  <md>2\text{ meters/second}^2.</md>

  <note xml:id="note-sec-4-7-motion-revisited-103">
    <title>Position, velocity, and acceleration</title>
      <p>If <m>s(t)</m> is the position of an object moving along a line, then</p>
        <md>v(t)=s'(t)</md>
      <p>is its velocity, and</p>
        <md>a(t)=v'(t)=s''(t)</md>
      <p>is its acceleration.</p>
  </note>

  <p>The notation</p>

  <md>s''(t)</md>

  <p>means the derivative of the derivative.  In Leibniz notation,</p>

  <md>v(t)=\frac{ds}{dt},</md>

  <p>and</p>

  <md>a(t)=\frac{dv}{dt}=\frac{d^2s}{dt^2}.</md>

  <p>The notation</p>

  <md>\frac{d^2s}{dt^2}</md>

  <p>means “differentiate <m>s</m> twice with respect to <m>t</m>.”  It does not mean</p>

  <md>\left(\frac{ds}{dt}\right)^2.</md>

  <paragraphs>
    <title>Units</title>

  <p>If position is measured in meters and time in seconds, then</p>

  <md>s(t)\text{ has units of meters,}</md>

  <md>v(t)=s'(t)\text{ has units of meters/second,}</md>

  <p>and</p>

  <md>a(t)=v'(t)\text{ has units of meters/second}^2.</md>

  <p>The units explain the meaning.  Acceleration measures how many meters per second of velocity are gained or lost each second.</p>

  <md>
    <mrow>\begin{array}{c|c|c}
<strong>quantity</strong> \amp <strong>meaning</strong> \amp <strong>units</strong></mrow>
    <mrow>\hline
s(t) \amp \text{position} \amp \text{meters}</mrow>
    <mrow>
v(t)=s'(t) \amp \text{rate of change of position} \amp \text{meters/second}</mrow>
    <mrow>
a(t)=v'(t)=s''(t) \amp \text{rate of change of velocity} \amp \text{meters/second}^2
\end{array}</mrow>
  </md>
  </paragraphs>


  <exercise xml:id="checkpoint-4-7-velocity-and-acceleration">
        <title>Why are velocity and acceleration derivatives of position?</title>
        <statement>
          <p>
            If <m>s(t)</m> is position, then <m>v=s'</m> and <m>a=v'=s''</m>. In your own words, why do those two derivatives deserve the names velocity and acceleration, and why is <m>\dfrac{d^2s}{dt^2}</m> not the square of <m>\dfrac{ds}{dt}</m>?
          </p>
        </statement>
        <hint>
          <p>
            Velocity is the rate of change of position. Acceleration is the rate of change of velocity. The superscript <m>2</m> counts how many times we differentiate.
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-4-7-motion-revisited-2">
    <title>Example</title>
    <statement>
  <p>Let</p>

  <md>s(t)=10+4t-t^2</md>

  <p>be the position of a particle in meters after <m>t</m> seconds.</p>

  <p>Then</p>

  <md>v(t)=s'(t)=4-2t,</md>

  <p>and</p>

  <md>a(t)=v'(t)=-2.</md>

  <p>At <m>t=0</m>,</p>

  <md>v(0)=4,</md>

  <p>so the particle is moving in the positive direction.</p>

  <p>At <m>t=2</m>,</p>

  <md>v(2)=4-4=0.</md>

  <p>The particle is momentarily stopped.</p>

  <p>At <m>t=3</m>,</p>

  <md>v(3)=4-6=-2.</md>

  <p>The particle is now moving in the negative direction.</p>

  <p>The acceleration is</p>

  <md>-2\text{ meters/second}^2</md>

  <p>at every time.  This means the velocity is decreasing by <m>2</m> meters/second each second.</p>

  <figure xml:id="fig-position-velocity-acceleration">
    <caption>Position, velocity, and acceleration are linked by derivatives.  The position has a maximum when the velocity changes sign from positive to negative.</caption>
            <sidebyside widths="32% 32% 32%" valign="bottom">
    <image xml:id="fig-position-velocity-acceleration-panel-1">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=\textwidth,
    height=0.82\textwidth,
    xmin=0, xmax=4,
    ymin=8, ymax=15,
    axis lines=left,
    xlabel={\(t\)},
    ylabel={\(s(t)\)},
    xtick={0,2,4},
    ytick={10,14},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=0:4, samples=120] {10+4*x-x^2};
\addplot[only marks, mark=*] coordinates {(2,14)};
\node[anchor=south] at (axis cs:2,14.1) {\(v=0\)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
    <image xml:id="fig-position-velocity-acceleration-panel-2">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=\textwidth,
    height=0.82\textwidth,
    xmin=0, xmax=4,
    ymin=-5, ymax=5,
    axis lines=middle,
    xlabel={\(t\)},
    ylabel={\(v(t)\)},
    xtick={0,2,4},
    ytick={-4,0,4},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[very thick, domain=0:4, samples=2] {4-2*x};
\addplot[only marks, mark=*] coordinates {(2,0)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
    <image xml:id="fig-position-velocity-acceleration-panel-3">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=\textwidth,
    height=0.82\textwidth,
    xmin=0, xmax=4,
    ymin=-4, ymax=2,
    axis lines=middle,
    xlabel={\(t\)},
    ylabel={\(a(t)\)},
    xtick={0,2,4},
    ytick={-2,0},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[very thick, domain=0:4, samples=2] {-2};
\node[anchor=west] at (axis cs:0.5,-2.35) {\(a(t)=-2\)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
    </sidebyside>
  </figure>

  <p>The point <m>t=2</m> is special.  The position graph has a horizontal tangent there.  The velocity is zero there.  The particle changes direction there.</p>

  <p>This is the motion version of a graph idea:</p>

  <md>\text{a horizontal tangent means derivative }0.</md>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-speed-versus-velocity">
    <title>Speed versus velocity</title>

  <p>Velocity has a sign.  Speed does not.</p>

  <p>If the positive direction is to the right, then</p>

  <md>v(t)&gt;0</md>

  <p>means the object is moving right, while</p>

  <md>v(t)&lt;0</md>

  <p>means the object is moving left.</p>

  <p>Speed asks only how fast the object is moving, not which direction it is moving.  For motion along a line,</p>

  <md>\text{speed}=|v(t)|.</md>


  <exercise xml:id="checkpoint-4-7-speed-versus-velocity">
        <title>Why is speed the absolute value of velocity?</title>
        <statement>
          <p>
            Velocity has a sign; speed does not. In your own words, why can two different velocities share the same speed, and why can average velocity be zero on an interval where the average speed is not?
          </p>
        </statement>
        <hint>
          <p>
            Speed asks how fast, not which way. Displacement can cancel; distance traveled cannot.
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-4-7-motion-revisited-3">
    <title>Example</title>
    <statement>
  <p>Let</p>

  <md>s(t)=t^2-4t,
\qquad 0\le t\le4.</md>

  <p>Then</p>

  <md>v(t)=s'(t)=2t-4.</md>

  <p>At <m>t=1</m>,</p>

  <md>v(1)=-2,</md>

  <p>so the particle is moving in the negative direction with speed</p>

  <md>|v(1)|=2.</md>

  <p>At <m>t=3</m>,</p>

  <md>v(3)=2,</md>

  <p>so the particle is moving in the positive direction with speed</p>

  <md>|v(3)|=2.</md>

  <p>The velocities are different:</p>

  <md>-2
\qquad\text{and}\qquad
2,</md>

  <p>but the speeds are the same:</p>

  <md>2
\qquad\text{and}\qquad
2.</md>

  <p>At <m>t=2</m>,</p>

  <md>v(2)=0.</md>

  <p>The particle turns around.</p>

  <p>The position values are</p>

  <md>s(0)=0,
\qquad
s(2)=-4,
\qquad
s(4)=0.</md>

  <p>So from <m>t=0</m> to <m>t=4</m>, the displacement is</p>

  <md>s(4)-s(0)=0.</md>

  <p>The average velocity is therefore</p>

  <md>\frac{s(4)-s(0)}{4-0}=0.</md>

  <p>But the particle did not stand still.  It moved from <m>0</m> to <m>-4</m>, and then back from <m>-4</m> to <m>0</m>.  The total distance traveled is</p>

  <md>4+4=8.</md>

  <p>So the average speed over the interval is</p>

  <md>\frac{8}{4}=2.</md>

  <md>
\text{average velocity}
=
\frac{\text{displacement}}{\text{time}},
\qquad
\text{average speed}
=
\frac{\text{distance traveled}}{\text{time}}.
</md>

  <p>These are not the same when the object changes direction.</p>

  <figure xml:id="fig-speed-vs-velocity-return">
    <caption>The particle returns to its starting position, so its displacement is <m>0</m>, but it has traveled a total distance of <m>8</m>.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.74\textwidth,
    height=0.46\textwidth,
    xmin=0, xmax=4.3,
    ymin=-4.8, ymax=1.2,
    axis lines=middle,
    xlabel={\(t\)},
    ylabel={\(s(t)\)},
    xtick={0,1,2,3,4},
    ytick={-4,0},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=0:4, samples=120] {x^2-4*x};
\addplot[only marks, mark=*] coordinates {(0,0) (2,-4) (4,0)};
\node[fill=white, inner sep=1pt, anchor=south west] at (axis cs:0.28,0.35) {\(s(0)=0\)};
\node[anchor=north] at (axis cs:2,-4.1) {\(s(2)=-4\)};
\node[fill=white, inner sep=1pt, anchor=south] at (axis cs:3.55,0.35) {\(s(4)=0\)};
\node[anchor=west] at (axis cs:0.7,-2.4) {moving negative};
\node[anchor=west] at (axis cs:2.65,-2.4) {moving positive};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <p>There is a small warning hidden here.  Since speed is</p>

  <md>|v(t)|,</md>

  <p>the speed graph can have a corner when <m>v(t)</m> passes through <m>0</m>.  In this example,</p>

  <md>\text{speed}=|2t-4|.</md>

  <p>It has a sharp corner at <m>t=2</m>.  The velocity is a smooth line, but the speed is not smooth at the turning instant.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-falling-under-constant-acceleration">
    <title>Falling under constant acceleration</title>

  <p>Near the surface of the earth, if air resistance is ignored, falling objects have nearly constant acceleration.</p>

  <p>If we measure height upward, then gravity pulls in the negative direction.  In feet, the acceleration is approximately</p>

  <md>-32\text{ ft/s}^2.</md>

  <p>In meters, it is approximately</p>

  <md>-9.8\text{ m/s}^2.</md>

  <p>The sign is not a property of gravity alone.  It also depends on our choice of positive direction.  If upward is positive, gravity is negative.  If downward is positive, gravity is positive.</p>

  <paragraphs>
    <title>The standard height model in feet</title>

  <p>Suppose height is measured in feet and time in seconds.  If upward is positive and acceleration is</p>

  <md>-32\text{ ft/s}^2,</md>

  <p>then a useful height model is</p>

  <md>y(t)=y_0+v_0t-16t^2.</md>

  <p>Here:</p>

  <md>y_0=\text{initial height},</md>

  <p>and</p>

  <md>v_0=\text{initial velocity}.</md>

  <p>Differentiate:</p>

  <md>v(t)=y'(t)=v_0-32t.</md>

  <p>Differentiate again:</p>

  <md>a(t)=v'(t)=-32.</md>

  <p>So the formula has exactly the desired acceleration.</p>

  <p>In meters, the corresponding model is</p>

  <md>y(t)=y_0+v_0t-4.9t^2,</md>

  <p>with</p>

  <md>v(t)=v_0-9.8t</md>

  <p>and</p>

  <md>a(t)=-9.8.</md>

  <note xml:id="note-sec-4-7-motion-revisited-104">
    <title>Constant acceleration model</title>
      <p>If acceleration is constant and equal to <m>a</m>, then</p>
        <md>s(t)=s_0+v_0t+\frac12at^2</md>
      <p>has</p>
        <md>v(t)=s'(t)=v_0+at</md>
      <p>and</p>
        <md>a(t)=s''(t)=a.</md>
      <p>At this point we can verify the formula by differentiating.  Later, integration will explain how to build it from the acceleration.</p>
  </note>
  </paragraphs>


  <exercise xml:id="checkpoint-4-7-zero-velocity-not-zero-acceleration">
        <title>Why can velocity be zero while acceleration is not?</title>
        <statement>
          <p>
            At the highest point of a throw, velocity is <m>0</m> but acceleration is still the gravitational constant. In your own words, why does that not contradict the meaning of a derivative, and why is it a mistake to say gravity turns off at the top?
          </p>
        </statement>
        <hint>
          <p>
            A value of a function can be zero while the function is still changing. Acceleration is the rate of change of velocity, not the velocity itself.
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-4-7-motion-revisited-5">
    <title>a ball thrown upward</title>
    <statement>
  <p>A ball is thrown upward from the ground with initial velocity</p>

  <md>64\text{ ft/s}.</md>

  <p>Ignoring air resistance, its height is</p>

  <md>y(t)=64t-16t^2.</md>

  <p>The velocity is</p>

  <md>v(t)=y'(t)=64-32t.</md>

  <p>The acceleration is</p>

  <md>a(t)=v'(t)=-32.</md>

  <p>The ball reaches its highest point when its velocity is <m>0</m>:</p>

  <md>64-32t=0.</md>

  <p>Thus</p>

  <md>t=2.</md>

  <p>At that time the height is</p>

  <md>y(2)=64(2)-16(2)^2=128-64=64.</md>

  <p>So the maximum height is</p>

  <md>64\text{ ft}.</md>

  <p>The ball returns to the ground when</p>

  <md>y(t)=0.</md>

  <p>Solve:</p>

  <md>64t-16t^2=0.</md>

  <p>Factor:</p>

  <md>16t(4-t)=0.</md>

  <p>Thus</p>

  <md>t=0
\qquad\text{or}\qquad
t=4.</md>

  <p>The time <m>t=0</m> is the launch time.  The ball lands at</p>

  <md>t=4\text{ seconds}.</md>

  <p>At landing,</p>

  <md>v(4)=64-32(4)=-64.</md>

  <p>So the velocity is</p>

  <md>-64\text{ ft/s}.</md>

  <p>The speed is</p>

  <md>64\text{ ft/s}.</md>

  <p>The negative sign says the ball is moving downward.</p>

  <figure xml:id="fig-thrown-upward">
    <caption>At the highest point, the velocity is <m>0</m>, but the acceleration is still <m>-32\text{ ft/s}^2</m>.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.76\textwidth,
    height=0.48\textwidth,
    xmin=0, xmax=4.4,
    ymin=-5, ymax=72,
    axis lines=left,
    xlabel={\(t\) (seconds)},
    ylabel={\(y(t)\) (feet)},
    xtick={0,2,4},
    ytick={0,64},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=0:4, samples=140] {64*x-16*x^2};
\addplot[only marks, mark=*] coordinates {(0,0) (2,64) (4,0)};
\draw[dashed] (axis cs:2,0) -- (axis cs:2,64);
\node[anchor=south] at (axis cs:2,64.5) {highest point};
\node[anchor=west] at (axis cs:0.45,25) {rising: \(v&gt;0\)};
\node[anchor=west] at (axis cs:2.5,25) {falling: \(v&lt;0\)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <p>This example corrects a common misunderstanding:</p>

  <md>
\text{At the top, velocity is }0,\text{ but acceleration is not }0.
</md>

  <p>Gravity does not turn off at the highest point.  The velocity is momentarily zero, but it is still changing.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-units-signs-motion">
    <title>Units and signs in motion problems</title>

  <p>The hardest part of many motion problems is not differentiating.  It is deciding what the signs mean.</p>

  <p>A position is measured relative to an origin.  A velocity is measured relative to a positive direction.  Acceleration tells whether velocity is increasing or decreasing in that chosen direction.</p>

  <md>
    <mrow>\begin{array}{c|l}
<strong>statement</strong> \amp <strong>meaning</strong></mrow>
    <mrow>\hline
s(t)&gt;0 \amp \text{the object is on the positive side of the origin}</mrow>
    <mrow>
s(t)&lt;0 \amp \text{the object is on the negative side of the origin}</mrow>
    <mrow>
v(t)&gt;0 \amp \text{the object is moving in the positive direction}</mrow>
    <mrow>
v(t)&lt;0 \amp \text{the object is moving in the negative direction}</mrow>
    <mrow>
a(t)&gt;0 \amp \text{the velocity is increasing}</mrow>
    <mrow>
a(t)&lt;0 \amp \text{the velocity is decreasing}
\end{array}</mrow>
  </md>

  <p>Acceleration does not directly tell which way the object is moving.  Velocity does that.</p>

  <p>Acceleration tells how velocity is changing.</p>

  <paragraphs>
    <title>Speeding up and slowing down</title>

  <p>Because speed is</p>

  <md>|v(t)|,</md>

  <p>an object speeds up when the magnitude of velocity increases.</p>

  <p>For motion along a line:</p>

  <md>
\text{if \(v\) and \(a\) have the same sign, the object is speeding up,}
</md>

  <p>and</p>

  <md>
\text{if \(v\) and \(a\) have opposite signs, the object is slowing down.}
</md>

  <md>
    <mrow>\begin{array}{c|c|c}
<strong>velocity</strong> \amp <strong>acceleration</strong> \amp <strong>speed</strong></mrow>
    <mrow>\hline
v&gt;0 \amp a&gt;0 \amp \text{increasing}</mrow>
    <mrow>
v&gt;0 \amp a&lt;0 \amp \text{decreasing}</mrow>
    <mrow>
v&lt;0 \amp a&gt;0 \amp \text{decreasing}</mrow>
    <mrow>
v&lt;0 \amp a&lt;0 \amp \text{increasing}
\end{array}</mrow>
  </md>

  <p>For the ball</p>

  <md>y(t)=64t-16t^2,</md>

  <p>we have</p>

  <md>v(t)=64-32t,
\qquad
a(t)=-32.</md>

  <p>For <m>0&lt;t&lt;2</m>,</p>

  <md>v(t)&gt;0</md>

  <p>and</p>

  <md>a(t)&lt;0.</md>

  <p>The signs are opposite, so the ball is slowing down as it rises.</p>

  <p>For <m>2&lt;t&lt;4</m>,</p>

  <md>v(t)&lt;0</md>

  <p>and</p>

  <md>a(t)&lt;0.</md>

  <p>The signs are the same, so the ball is speeding up as it falls.</p>
  </paragraphs>

  <paragraphs>
    <title>Same physical motion, different sign convention</title>

  <p>Suppose a stone is dropped from a <m>100</m>-meter tower.</p>

  <p>If height above the ground is the position, and upward is positive, then</p>

  <md>y(t)=100-4.9t^2.</md>

  <p>The velocity is</p>

  <md>v(t)=-9.8t,</md>

  <p>and the acceleration is</p>

  <md>a(t)=-9.8.</md>

  <p>Both velocity and acceleration are negative after the stone is released, so the stone is speeding up downward.</p>

  <p>If instead we measure downward distance from the release point, then the same motion is described by</p>

  <md>r(t)=4.9t^2.</md>

  <p>Then</p>

  <md>r'(t)=9.8t,</md>

  <p>and</p>

  <md>r''(t)=9.8.</md>

  <p>Now velocity and acceleration are positive because the positive direction has been chosen downward.</p>

  <p>The physics did not change.  The coordinate choice changed.</p>

  <note xml:id="note-sec-4-7-motion-revisited-105">
    <title>Sign rule for motion</title>
      <p>Before interpreting signs, choose:</p>
        <md>\text{origin}
\qquad\text{and}\qquad
\text{positive direction}.</md>
      <p>Only then do signs have meaning.</p>
  </note>
  </paragraphs>

  <paragraphs>
    <title>A small checklist for motion problems</title>

  <p>For a motion problem on a line:</p>

  <md>
    <mrow>\begin{array}{ll}
1. \amp \text{Choose an origin and a positive direction.}</mrow>
    <mrow>2. \amp \text{Write the position function with units.}</mrow>
    <mrow>3. \amp \text{Differentiate once for velocity.}</mrow>
    <mrow>4. \amp \text{Differentiate twice for acceleration.}</mrow>
    <mrow>5. \amp \text{Use \(|v(t)|\) when the question asks for speed.}
\end{array}</mrow>
  </md>

  <p>The derivative has now returned to its first meaning: instantaneous motion.  But even motion can hide traps.  A graph may look continuous and still have no velocity at a sharp turn.  A derivative may exist and still behave unexpectedly.  Before moving to a larger library of differentiation rules, we pause to collect these warning examples.</p>
  </paragraphs>

  <exercise xml:id="checkpoint-4-7-signs-and-speeding-up">
        <title>Why must origin and positive direction be chosen before signs mean anything?</title>
        <statement>
          <p>
            The same falling stone can have negative or positive acceleration depending on whether upward or downward is called positive. In your own words, why does that not change the physics, and how do the signs of <m>v</m> and <m>a</m> together tell whether the object is speeding up?
          </p>
        </statement>
        <hint>
          <p>
            Signs are relative to a chosen axis. Speed increases when velocity and acceleration point the same way along that axis.
          </p>
        </hint>
      </exercise>

  </subsection>



</section>
