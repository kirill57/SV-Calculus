<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-first-derivative-formulas">
  <title>First derivative formulas</title>

  <p>Rather than evaluating the difference quotient limit from scratch for every function, we establish foundational operational rules: constant functions, power rules, constant multiples, and sums, which together allow immediate differentiation of all polynomials.</p>

  <subsection xml:id="subsec-constant-identity-functions">
    <title>Constant and identity functions</title>

    <theorem xml:id="thm-constant-identity-derivatives">
      <title>Derivatives of constant and identity functions</title>
      <statement>
        <p>For any real constant <m>c</m>:</p>
        <md>\frac{d}{dx}(c) = 0 \quad \text{and} \quad \frac{d}{dx}(x) = 1.</md>
      </statement>
      <proof>
        <p>For <m>f(x)=c</m>, <m>\lim_{h\to 0}\frac{c-c}{h} = \lim_{h\to 0} 0 = 0</m>. For <m>f(x)=x</m>, <m>\lim_{h\to 0}\frac{(x+h)-x}{h} = \lim_{h\to 0}\frac{h}{h} = 1</m>.</p>
      </proof>
    </theorem>
  </subsection>

  <subsection xml:id="subsec-power-functions-positive-integers">
    <title>Power functions with positive integer powers</title>

    <theorem xml:id="thm-power-rule-positive-integers">
      <title>Power rule for positive integers</title>
      <statement>
        <p>If <m>n</m> is a positive integer, then</p>
        <md>\frac{d}{dx}(x^n) = n x^{n-1}.</md>
      </statement>
      <proof>
        <p>Using the algebraic identity <m>A^n - B^n = (A-B)\sum_{k=0}^{n-1} A^{n-1-k}B^k</m> with <m>A=x+h</m> and <m>B=x</m>:</p>
        <md>\frac{(x+h)^n - x^n}{h} = (x+h)^{n-1} + (x+h)^{n-2}x + \cdots + x^{n-1}.</md>
        <p>As <m>h\to 0</m>, each of the <m>n</m> terms approaches <m>x^{n-1}</m>, yielding <m>n x^{n-1}</m>.</p>
      </proof>
    </theorem>
  </subsection>

  <subsection xml:id="subsec-constant-multiple-sum-rules-first">
    <title>Constant multiples and sums</title>

    <theorem xml:id="thm-constant-multiple-sum-rules">
      <title>Linearity of differentiation</title>
      <statement>
        <p>If <m>f</m> and <m>g</m> are differentiable and <m>c</m> is a constant, then</p>
        <md>\frac{d}{dx}\bigl(c f(x)\bigr) = c f'(x),</md>
        <md>\frac{d}{dx}\bigl(f(x) + g(x)\bigr) = f'(x) + g'(x),</md>
        <md>\frac{d}{dx}\bigl(f(x) - g(x)\bigr) = f'(x) - g'(x).</md>
      </statement>
    </theorem>
  </subsection>

  <subsection xml:id="subsec-polynomial-rule">
    <title>The polynomial rule</title>

    <p>Combining the power rule with linearity, any polynomial <m>p(x) = \sum_{k=0}^n a_k x^k</m> differentiates term-by-term:</p>
    <md>p'(x) = \sum_{k=1}^n k a_k x^{k-1} = n a_n x^{n-1} + (n-1)a_{n-1}x^{n-2} + \cdots + 2a_2 x + a_1.</md>

    <example xml:id="ex-polynomial-tangent-line">
      <title>Tangent line to a polynomial</title>
      <statement>
        <p>For <m>p(x)=x^4-3x^2+2</m> at <m>x=1</m>: <m>p(1)=0</m> and <m>p'(x)=4x^3-6x</m>, so <m>p'(1)=-2</m>. The tangent line equation is <m>y = -2x+2</m>.</p>

        <figure xml:id="fig-polynomial-tangent-first-formulas">
          <caption>Tangent line to <m>p(x)=x^4-3x^2+2</m> at <m>(1,0)</m>.</caption>
          <image>
            <latex-image>
              \begin{tikzpicture}
                \begin{axis}[
                    width=0.72\textwidth,
                    height=0.48\textwidth,
                    xmin=-2.1, xmax=2.1,
                    ymin=-1.6, ymax=5.2,
                    axis lines=middle,
                    xlabel={\(x\)},
                    ylabel={\(y\)},
                    xtick={-2,-1,0,1,2},
                    ytick={-1,0,1,2,4},
                    grid=both,
                    grid style={line width=.1pt, draw=gray!25},
                ]
                \addplot[thick, domain=-2:2, samples=180] {x^4-3*x^2+2};
                \addplot[very thick, domain=-0.4:1.8, samples=2] {-2*x+2};
                \addplot[only marks, mark=*] coordinates {(1,0)};
                \node[anchor=west] at (axis cs:1.05,0.15) {\((1,0)\)};
                \node[anchor=west] at (axis cs:0.25,2.65) {\(p(x)=x^4-3x^2+2\)};
                \node[anchor=west] at (axis cs:0.8,-0.9) {tangent slope \(-2\)};
                \end{axis}
              \end{tikzpicture}
            </latex-image>
          </image>
        </figure>
      </statement>
    </example>
  </subsection>

  <subsection xml:id="subsec-first-derivative-formulas-summary">
    <title>Summary of elementary derivative rules</title>

    <table>
      <title>Basic derivative rules</title>
      <tabular top="major" bottom="major" left="major" right="major">
        <row bottom="minor">
          <cell><term>Rule</term></cell>
          <cell><term>Formula</term></cell>
        </row>
        <row>
          <cell>Constant rule</cell>
          <cell><m>\frac{d}{dx}(c) = 0</m></cell>
        </row>
        <row>
          <cell>Power rule</cell>
          <cell><m>\frac{d}{dx}(x^n) = n x^{n-1}</m></cell>
        </row>
        <row>
          <cell>Constant multiple rule</cell>
          <cell><m>\frac{d}{dx}(c f(x)) = c f'(x)</m></cell>
        </row>
        <row>
          <cell>Sum / difference rule</cell>
          <cell><m>\frac{d}{dx}(f(x) \pm g(x)) = f'(x) \pm g'(x)</m></cell>
        </row>
      </tabular>
    </table>
  </subsection>

</section>
