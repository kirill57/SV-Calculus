<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-3-5-continuity">
  <title>Continuity</title>

  <p>The precise definition of a limit gave us a way to control nearby values.  It said:</p>

  <md>x\text{ close to }a
\quad \Longrightarrow \quad
f(x)\text{ close to }L.</md>

  <p>But a limit still does not have to care about the value at the point.  A function may have a hole, a wrong value, or no value at all, and still have a limit nearby.</p>

  <p>Continuity is the calm case.  It is the case where the nearby behavior and the actual value agree.</p>

  <subsection xml:id="subsec-continuity-at-point">
    <title>Continuity at a point</title>

  <p>Recall the function</p>

  <md>
    <mrow>F(x)=
\begin{cases}
2x+1, \amp x\neq1,</mrow>
    <mrow>-1, \amp x=1.
\end{cases}</mrow>
  </md>

  <p>Near <m>x=1</m>, but not at <m>x=1</m>, the function follows the line</p>

  <md>y=2x+1.</md>

  <p>So</p>

  <md>\lim_{x\to1}F(x)=3.</md>

  <p>But</p>

  <md>F(1)=-1.</md>

  <p>The limit exists, but it does not equal the actual value.  The graph has the nearby behavior of the line <m>y=2x+1</m>, except that the point at <m>x=1</m> has been placed somewhere else.</p>

  <p>This function is not continuous at <m>x=1</m>.</p>

  <note xml:id="note-sec-3-5-continuity-68">
    <title>Continuity at a point</title>
      <p>A function <m>f</m> is <em>continuous at <m>a</m></em> if</p>
        <md>\lim_{x\to a}f(x)=f(a).</md>
      <p>Equivalently, three things must happen:</p>
        <md>
    <mrow>\begin{array}{ll}
1. \amp f(a)\text{ is defined},</mrow>
    <mrow>2. \amp \displaystyle \lim_{x\to a}f(x)\text{ exists},</mrow>
    <mrow>3. \amp \displaystyle \lim_{x\to a}f(x)=f(a).
\end{array}</mrow>
  </md>
  </note>

  <p>The third condition is the heart of the definition.  Nearby values must approach the actual value.</p>

  <md>
\text{Continuity means: nearby inputs give nearby outputs, including at the point itself.}
</md>

  <example xml:id="ex-sec-3-5-continuity-1">
    <title>a polynomial is continuous at a point</title>
    <statement>
  <p>Let</p>

  <md>f(x)=x^2.</md>

  <p>At <m>x=2</m>,</p>

  <md>f(2)=4.</md>

  <p>Also,</p>

  <md>\lim_{x\to2}x^2=4.</md>

  <p>Therefore</p>

  <md>\lim_{x\to2}f(x)=f(2),</md>

  <p>so <m>f</m> is continuous at <m>2</m>.</p>

  <p>In fact, the same reasoning works at every point.  If <m>a</m> is any real number, then</p>

  <md>\lim_{x\to a}x^2=a^2=f(a).</md>

  <p>So <m>x^2</m> is continuous everywhere.</p>
    </statement>
  </example>

  <paragraphs>
    <title>The <m>\varepsilon</m>-<m>\delta</m> version</title>

  <p>The limit definition gives a precise version of continuity.</p>

  <p>A function <m>f</m> is continuous at <m>a</m> if, for every <m>\varepsilon&gt;0</m>, there is a <m>\delta&gt;0</m> such that whenever <m>x</m> is in the domain of <m>f</m> and</p>

  <md>|x-a|&lt;\delta,</md>

  <p>we have</p>

  <md>|f(x)-f(a)|&lt;\varepsilon.</md>

  <p>There is no need to exclude <m>x=a</m> here.  If <m>x=a</m>, then</p>

  <md>|f(x)-f(a)|=|f(a)-f(a)|=0,</md>

  <p>which is already less than every positive <m>\varepsilon</m>.</p>

  <p>So the limit definition uses a punctured interval:</p>

  <md>0&lt;|x-a|&lt;\delta,</md>

  <p>while continuity uses the full interval:</p>

  <md>|x-a|&lt;\delta.</md>
  </paragraphs>

  <exercise xml:id="checkpoint-3-5-continuity-three-conditions">
        <title>What three things must happen for continuity at a point?</title>
        <statement>
          <p>
            Continuity at <m>a</m> means <m>\lim_{x\to a}f(x)=f(a)</m>. In your own words, why does that one equation hide three requirements, and what goes wrong if the nearby limit exists but the actual value is missing or wrong?
          </p>
        </statement>
        <hint>
          <p>
            The output at <m>a</m> must exist, the nearby limit must exist, and those two numbers must agree. A hole or a relocated point breaks the last two in different ways.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-holes-continuous-extensions">
    <title>Holes and continuous extensions</title>

  <p>Consider again</p>

  <md>g(x)=\frac{x^2-1}{x-1},
\qquad x\neq1.</md>

  <p>For <m>x\neq1</m>,</p>

  <md>g(x)=x+1.</md>

  <p>Thus</p>

  <md>\lim_{x\to1}g(x)=2.</md>

  <p>But <m>g(1)</m> is not defined, because the original formula gives division by <m>0</m>.  So <m>g</m> is not continuous at <m>1</m>.</p>

  <p>The failure is small.  The graph only has a missing point.  We can repair it by defining a new function</p>

  <md>
    <mrow>\widetilde g(x)=
\begin{cases}
\dfrac{x^2-1}{x-1}, \amp x\neq1,</mrow>
    <mrow>
2, \amp x=1.
\end{cases}</mrow>
  </md>

  <p>Now</p>

  <md>\lim_{x\to1}\widetilde g(x)=2</md>

  <p>and</p>

  <md>\widetilde g(1)=2.</md>

  <p>Therefore <m>\widetilde g</m> is continuous at <m>1</m>.</p>

  <figure xml:id="fig-removable-continuity">
    <caption>The function <m>\dfrac{x^2-1}{x-1}</m> has the graph of <m>y=x+1</m>, except for a missing point at <m>x=1</m>.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.68\textwidth,
    height=0.45\textwidth,
    xmin=-1.1, xmax=3,
    ymin=-0.3, ymax=4.3,
    axis lines=left,
    xlabel={\(x\)},
    ylabel={\(y\)},
    xtick={1},
    ytick={2},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=-1:3, samples=2] {x+1};
\addplot[only marks, mark=o, mark size=3pt, thick] coordinates {(1,2)};
\node[anchor=west] at (axis cs:0.95,1.8) {missing point};
\node[anchor=west] at (axis cs:1.55,3.45) {\(y=x+1\)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <p>A discontinuity like this is called <em>removable</em>.  The limit tells us exactly what value to insert.</p>

  <note xml:id="note-sec-3-5-continuity-69">
    <title>Removable discontinuity</title>
      <p>A function has a removable discontinuity at <m>a</m> if the limit</p>
        <md>\lim_{x\to a}f(x)</md>
      <p>exists, but the function is not continuous at <m>a</m> because <m>f(a)</m> is missing or has the wrong value.</p>
  </note>

  <p>The word “removable” does not mean the original function was continuous.  It means we can make it continuous by changing the value at one point.</p>

  <exercise xml:id="checkpoint-3-5-removable-extension">
        <title>When can a hole be repaired?</title>
        <statement>
          <p>
            The function <m>\frac{x^2-1}{x-1}</m> is not continuous at <m>x=1</m> because the value is missing, yet defining the missing height to be <m>2</m> produces a continuous extension. In your own words, what makes a discontinuity removable, and why can a jump or a vertical asymptote not be repaired by filling in one point?
          </p>
        </statement>
        <hint>
          <p>
            Repair is possible only when nearby values already agree on a finite target. A jump has two targets. An infinite discontinuity has no finite target at all.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-continuity-on-interval">
    <title>Continuity on an interval</title>

  <p>A function is continuous on an interval if it is continuous at every point of that interval.</p>

  <p>For an open interval <m>(a,b)</m>, this means continuity at each point</p>

  <md>a&lt;x&lt;b.</md>

  <p>For a closed interval <m>[a,b]</m>, the endpoints need one-sided versions:</p>

  <md>\lim_{x\to a^+}f(x)=f(a)</md>

  <p>at the left endpoint, and</p>

  <md>\lim_{x\to b^-}f(x)=f(b)</md>

  <p>at the right endpoint.</p>

  <note xml:id="note-sec-3-5-continuity-70">
    <title>Continuity on a closed interval</title>
      <p>A function <m>f</m> is continuous on <m>[a,b]</m> if</p>
        <md>f\text{ is continuous at every point of }(a,b),</md>
        <md>\lim_{x\to a^+}f(x)=f(a),</md>
      <p>and</p>
        <md>\lim_{x\to b^-}f(x)=f(b).</md>
  </note>

  <p>The one-sided conditions are natural.  At the left endpoint <m>a</m>, points of the interval approach <m>a</m> only from the right.  At the right endpoint <m>b</m>, points of the interval approach <m>b</m> only from the left.</p>


  <exercise xml:id="checkpoint-3-5-closed-interval-one-sided">
        <title>Why do endpoints of a closed interval use one-sided continuity?</title>
        <statement>
          <p>
            Continuity on <m>[a,b]</m> asks for ordinary continuity inside <m>(a,b)</m>, but only a right-hand match at <m>a</m> and a left-hand match at <m>b</m>. In your own words, why are those one-sided conditions the natural ones at the ends of a closed interval?
          </p>
        </statement>
        <hint>
          <p>
            At the left endpoint, allowed inputs of the interval approach only from the right. At the right endpoint, they approach only from the left. There is no other side inside the interval.
          </p>
        </hint>
      </exercise>

  <example xml:id="ex-sec-3-5-continuity-3">
    <title>a piecewise function that is continuous</title>
    <statement>
  <p>Define</p>

  <md>
    <mrow>H(x)=
\begin{cases}
x^2, \amp x&lt;1,</mrow>
    <mrow>2x-1, \amp x\ge1.
\end{cases}</mrow>
  </md>

  <p>Away from <m>x=1</m>, each piece is a polynomial, so each piece is continuous.  The only point to check is <m>x=1</m>.</p>

  <p>From the left,</p>

  <md>\lim_{x\to1^-}H(x)=\lim_{x\to1^-}x^2=1.</md>

  <p>From the right,</p>

  <md>\lim_{x\to1^+}H(x)=\lim_{x\to1^+}(2x-1)=1.</md>

  <p>Also,</p>

  <md>H(1)=2(1)-1=1.</md>

  <p>Thus</p>

  <md>\lim_{x\to1}H(x)=H(1),</md>

  <p>so <m>H</m> is continuous at <m>1</m>.  Therefore <m>H</m> is continuous everywhere.</p>
    </statement>
  </example>

  <example xml:id="ex-sec-3-5-continuity-4">
    <title>a piecewise function with a jump</title>
    <statement>
  <p>Define</p>

  <md>
    <mrow>J(x)=
\begin{cases}
x^2, \amp x&lt;1,</mrow>
    <mrow>2x, \amp x\ge1.
\end{cases}</mrow>
  </md>

  <p>Again, the only point to check is <m>x=1</m>.</p>

  <p>From the left,</p>

  <md>\lim_{x\to1^-}J(x)=1.</md>

  <p>From the right,</p>

  <md>\lim_{x\to1^+}J(x)=2.</md>

  <p>The one-sided limits disagree, so</p>

  <md>\lim_{x\to1}J(x)</md>

  <p>does not exist.  Therefore <m>J</m> is not continuous at <m>1</m>.</p>

  <p>The formulas on the two sides are both harmless.  The problem is that they do not meet.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-algebra-continuous-functions">
    <title>Algebra of continuous functions</title>

  <p>Continuity behaves well under the usual algebraic operations.  This is one reason continuous functions are easy to build.</p>

  <p>Suppose <m>f</m> and <m>g</m> are continuous at <m>a</m>.  Then</p>

  <md>f(x)\to f(a)
\qquad\text{and}\qquad
g(x)\to g(a)</md>

  <p>as <m>x\to a</m>.  The limit laws give:</p>

  <md>f(x)+g(x)\to f(a)+g(a),</md>

  <md>f(x)-g(x)\to f(a)-g(a),</md>

  <md>f(x)g(x)\to f(a)g(a),</md>

  <p>and, if <m>g(a)\neq0</m>,</p>

  <md>\frac{f(x)}{g(x)}\to \frac{f(a)}{g(a)}.</md>

  <p>So the new function is continuous at <m>a</m>.</p>

  <note xml:id="note-sec-3-5-continuity-71">
    <title>Algebra of continuous functions</title>
      <p>If <m>f</m> and <m>g</m> are continuous at <m>a</m>, then the following functions are continuous at <m>a</m>:</p>
        <md>f+g,\qquad f-g,\qquad cf,\qquad fg,</md>
      <p>where <m>c</m> is any constant.</p>
      <p>If <m>g(a)\neq0</m>, then</p>
        <md>\frac{f}{g}</md>
      <p>is also continuous at <m>a</m>.</p>
  </note>

  <p>The condition <m>g(a)\neq0</m> matters.  A quotient cannot be continuous at a point where its denominator is <m>0</m>, because the quotient is not even defined there.</p>

  <example xml:id="ex-sec-3-5-continuity-5">
    <title>a rational function</title>
    <statement>
  <p>Let</p>

  <md>R(x)=\frac{x^2+3x+1}{x^2+1}.</md>

  <p>The numerator and denominator are polynomials, hence continuous everywhere.  The denominator satisfies</p>

  <md>x^2+1&gt;0</md>

  <p>for every real <m>x</m>.  Therefore <m>R</m> is continuous everywhere.</p>

  <p>So, for example,</p>

  <md>\lim_{x\to2}\frac{x^2+3x+1}{x^2+1}
=
\frac{2^2+3(2)+1}{2^2+1}
=
\frac{11}{5}.</md>

  <p>Continuity is the reason direct substitution works here.</p>
    </statement>
  </example>

  <paragraphs>
    <title>Basic continuous functions</title>

  <p>The following functions are continuous at every point in their domains:</p>

  <md>\text{polynomials},</md>

  <md>\text{rational functions where the denominator is not zero},</md>

  <md>\sin x,\quad \cos x,\quad \tan x\text{ where defined},</md>

  <md>e^x,\quad a^x\text{ for }a&gt;0,</md>

  <md>\ln x\text{ for }x&gt;0,</md>

  <p>and root functions where they are defined.</p>

  <p>This fact does not make limits disappear.  It gives us a shortcut when no hole, jump, or asymptote is present.</p>
  </paragraphs>

  <exercise xml:id="checkpoint-3-5-algebra-and-composition">
        <title>Why is continuity easy to build from old continuous pieces?</title>
        <statement>
          <p>
            Sums, products, and (when the denominator is nonzero) quotients of continuous functions are continuous, and so is a composition <m>f\circ g</m> when <m>g</m> is continuous at <m>a</m> and <m>f</m> is continuous at <m>g(a)</m>. In your own words, why do these combination rules follow from limit laws, and why does a zero denominator still block continuity?
          </p>
        </statement>
        <hint>
          <p>
            Continuity says nearby values approach the actual values. Limit laws then pass that approach to sums, products, and compositions. A quotient is not even defined where the denominator is zero, so it cannot be continuous there.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-continuity-compositions">
    <title>Continuity of compositions</title>

  <p>Algebra builds new functions by adding, multiplying, and dividing.  Composition builds new functions by putting one function inside another.</p>

  <p>Suppose</p>

  <md>g(x)\to g(a)</md>

  <p>as <m>x\to a</m>, and suppose <m>f</m> is continuous at <m>g(a)</m>.  Then</p>

  <md>f(g(x))\to f(g(a)).</md>

  <p>That is exactly the continuity of <m>f\circ g</m> at <m>a</m>.</p>

  <note xml:id="note-sec-3-5-continuity-72">
    <title>Continuity of compositions</title>
      <p>If <m>g</m> is continuous at <m>a</m>, and <m>f</m> is continuous at <m>g(a)</m>, then</p>
        <md>f\circ g</md>
      <p>is continuous at <m>a</m>.  In symbols,</p>
        <md>\lim_{x\to a}f(g(x))=f(g(a)).</md>
  </note>

  <p>The order of the statement matters.  First <m>g(x)</m> must approach <m>g(a)</m>.  Then <m>f</m> must preserve that approach near the input <m>g(a)</m>.</p>

  <example xml:id="ex-sec-3-5-continuity-7">
    <title>Example</title>
    <statement>
  <p>The function</p>

  <md>q(x)=\sqrt{1+x^2}</md>

  <p>is continuous for every real <m>x</m>.  The inside function</p>

  <md>g(x)=1+x^2</md>

  <p>is continuous everywhere.  Its values are always at least <m>1</m>, so the square root is defined.  Since the square root function is continuous on <m>[0,\infty)</m>, the composition is continuous everywhere.</p>

  <p>Therefore</p>

  <md>\lim_{x\to3}\sqrt{1+x^2}
=
\sqrt{1+3^2}
=
\sqrt{10}.</md>
    </statement>
  </example>

  <example xml:id="ex-sec-3-5-continuity-8">
    <title>Example</title>
    <statement>
  <p>The function</p>

  <md>r(x)=\sin(x^2)</md>

  <p>is continuous everywhere because <m>x^2</m> is continuous everywhere and sine is continuous everywhere.  Thus</p>

  <md>\lim_{x\to a}\sin(x^2)=\sin(a^2).</md>

  <p>This kind of substitution is legal because continuity protects it.</p>
    </statement>
  </example>
  </subsection>

  <subsection xml:id="subsec-three-common-discontinuities">
    <title>Three common discontinuities</title>

  <p>Continuity can fail in several ways.  The three most common early failures are removable discontinuities, jump discontinuities, and infinite discontinuities.</p>

  <md>
    <mrow>\begin{array}{c|c|c}
<strong>type</strong> \amp <strong>what happens</strong> \amp <strong>example</strong></mrow>
    <mrow>\hline
\text{removable} \amp
\text{limit exists, but value is missing or wrong}
\amp
\displaystyle \frac{x^2-1}{x-1}\text{ at }x=1</mrow>
    <mrow>
\text{jump} \amp
\text{left and right limits disagree}
\amp
\displaystyle \frac{|x|}{x}\text{ at }x=0</mrow>
    <mrow>
\text{infinite} \amp
\text{values grow without bound}
\amp
\displaystyle \frac{1}{(x-2)^2}\text{ at }x=2
\end{array}</mrow>
  </md>

  <figure xml:id="fig-three-discontinuities">
    <caption>Three common ways continuity can fail: a hole, a jump, or a vertical asymptote.</caption>
            <sidebyside widths="32% 32% 32%" valign="bottom">
    <image xml:id="fig-three-discontinuities-panel-1">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=\textwidth,
    height=0.88\textwidth,
    xmin=-0.5, xmax=2.5,
    ymin=0, ymax=4,
    axis lines=middle,
    xtick={1},
    ytick={2},
    title={removable},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=-0.2:2.3, samples=2] {x+1};
\addplot[only marks, mark=o, mark size=2.8pt, thick] coordinates {(1,2)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
    <image xml:id="fig-three-discontinuities-panel-2">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=\textwidth,
    height=0.88\textwidth,
    xmin=-2, xmax=2,
    ymin=-1.6, ymax=1.6,
    axis lines=middle,
    xtick={0},
    ytick={-1,1},
    title={jump},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=-2:-0.05, samples=2] {-1};
\addplot[thick, domain=0.05:2, samples=2] {1};
\addplot[only marks, mark=o, mark size=2.8pt, thick] coordinates {(0,-1) (0,1)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
    <image xml:id="fig-three-discontinuities-panel-3">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=\textwidth,
    height=0.88\textwidth,
    xmin=0.2, xmax=3.8,
    ymin=0, ymax=8,
    axis lines=middle,
    xtick={2},
    ytick={4},
    title={infinite},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=0.55:1.65, samples=120] {1/(x-2)^2};
\addplot[thick, domain=2.35:3.45, samples=120] {1/(x-2)^2};
\draw[dashed, thick] (axis cs:2,0) -- (axis cs:2,8);
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
    </sidebyside>
  </figure>

  <p>The names are not just vocabulary.  They tell us what kind of repair, if any, is possible.</p>

  <p>A removable discontinuity can be fixed by changing one value.</p>

  <p>A jump cannot be fixed by changing one value, because the two sides approach different targets.</p>

  <p>An infinite discontinuity cannot be fixed by changing one value, because nearby values do not stay near any finite number.</p>
  </subsection>

  <subsection xml:id="subsec-continuity-checklist">
    <title>A continuity checklist</title>

  <p>When checking continuity at <m>a</m>, ask:</p>

  <table xml:id="tab-continuity-checklist">
    <tabular>
      <row bottom="minor">
        <cell><strong>question</strong></cell>
        <cell><strong>meaning</strong></cell>
      </row>
      <row>
        <cell>Is <m>f(a)</m> defined?</cell>
        <cell>If not, the function is not continuous at <m>a</m>.</cell>
      </row>
      <row>
        <cell>Does <m>\lim_{x\to a}f(x)</m> exist?</cell>
        <cell>If the two sides disagree or the values do not settle, the function is not continuous at <m>a</m>.</cell>
      </row>
      <row>
        <cell>Does the limit equal <m>f(a)</m>?</cell>
        <cell>If not, the nearby behavior and the actual value disagree.</cell>
      </row>
    </tabular>
  </table>

  <p>For familiar formulas, continuity often lets us substitute directly.  For piecewise functions, endpoints between pieces must be checked.  For quotients, denominators must be watched.  For roots and logarithms, the domain must be respected.</p>

  <p>Continuity is still a local idea: it describes what happens near one point.  But it has surprising global consequences.  A continuous graph cannot jump over a height, and on a closed finite interval it cannot avoid having a highest and lowest point.</p>

  <p>Those two facts are the next payoff.</p>
  </subsection>



</section>
