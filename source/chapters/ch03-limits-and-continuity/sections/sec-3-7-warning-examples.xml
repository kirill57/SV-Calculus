<?xml version="1.0" encoding="UTF-8"?>
<!-- CONVERTED — VISUALLY VERIFIED -->
<section xml:id="sec-3-7-warning-examples">
  <title>Warning examples</title>

  <p>Continuity gave us two promises.  The Intermediate Value Theorem says that a continuous graph cannot skip a height.  The Extreme Value Theorem says that a continuous function on a closed finite interval reaches a highest and lowest value.</p>

  <p>Those promises are strong because their hypotheses are strong.  If we remove a hypothesis, the conclusion may fail.</p>

  <p>This section gathers the warnings in one place.  Each example is small, but each one protects a theorem from being used illegally.</p>

  <subsection xml:id="subsec-no-limit-at-jump-warning">
    <title>A function with no limit at a jump</title>

  <p>A two-sided limit needs one target.  A jump gives two targets.</p>

  <p>Define</p>

  <md>
    <mrow>J(x)=
\begin{cases}
0, \amp x&lt;0,</mrow>
    <mrow>1, \amp x\ge 0.
\end{cases}</mrow>
  </md>

  <p>As <m>x\to0^-</m>, the values of <m>J(x)</m> are always <m>0</m>.  So</p>

  <md>\lim_{x\to0^-}J(x)=0.</md>

  <p>As <m>x\to0^+</m>, the values of <m>J(x)</m> are always <m>1</m>.  So</p>

  <md>\lim_{x\to0^+}J(x)=1.</md>

  <p>The one-sided limits disagree.  Therefore</p>

  <md>\lim_{x\to0}J(x)</md>

  <p>does not exist.</p>

  <figure xml:id="fig-jump-warning-no-limit">
    <caption>A jump creates two different one-sided limits, so the two-sided limit fails.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.68\textwidth,
    height=0.42\textwidth,
    xmin=-1.4, xmax=1.4,
    ymin=-0.3, ymax=1.35,
    axis lines=middle,
    xlabel={\(x\)},
    ylabel={\(J(x)\)},
    xtick={-1,0,1},
    ytick={0,1},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=-1.35:-0.04, samples=2] {0};
\addplot[thick, domain=0:1.35, samples=2] {1};
\addplot[only marks, mark=o, mark size=3pt, thick] coordinates {(0,0)};
\addplot[only marks, mark=*, mark size=2.5pt] coordinates {(0,1)};
\node[anchor=south east] at (axis cs:-0.15,0.12) {left target \(0\)};
\node[anchor=north west] at (axis cs:0.22,0.88) {right target \(1\)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <p>Changing the value at <m>x=0</m> cannot repair the two-sided limit.  The problem is not the point itself.  The problem is that the nearby values on the two sides head toward different numbers.</p>

  <md>\text{A two-sided limit is about agreement from both sides.}</md>

  <exercise xml:id="checkpoint-3-7-jump-two-targets">
        <title>Why can changing the value at a jump never repair a two-sided limit?</title>
        <statement>
          <p>
            At a jump, the left-hand and right-hand limits disagree. In your own words, why is the problem not the point itself, and why does a two-sided limit demand agreement from both sides?
          </p>
        </statement>
        <hint>
          <p>
            Nearby values on the two sides are already heading toward different numbers. Relabeling the single center point cannot make those two neighborhoods share a target.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-infinite-not-finite-warning">
    <title>A limit that is infinite, not finite</title>

  <p>A different kind of failure happens when the values do not settle near any finite number.</p>

  <p>Consider</p>

  <md>f(x)=\frac{1}{x^2},
\qquad x\neq0.</md>

  <p>As <m>x</m> gets close to <m>0</m>, the denominator <m>x^2</m> gets close to <m>0</m>, but stays positive.  The quotient grows larger and larger.</p>

  <md>
    <mrow>\begin{array}{c|cccccc}
x \amp -0.1 \amp -0.01 \amp -0.001 \amp 0.001 \amp 0.01 \amp 0.1</mrow>
    <mrow>\hline
\dfrac1{x^2} \amp 100 \amp 10{,}000 \amp 1{,}000{,}000
\amp 1{,}000{,}000 \amp 10{,}000 \amp 100
\end{array}</mrow>
  </md>

  <p>We write</p>

  <md>\lim_{x\to0}\frac1{x^2}=\infty.</md>

  <p>This notation does not mean that the function has a finite limit.  It means the values grow beyond every fixed positive bound.</p>

  <p>There is no real number <m>L</m> such that</p>

  <md>\lim_{x\to0}\frac1{x^2}=L.</md>

  <figure xml:id="fig-infinite-limit-warning">
    <caption>The values of <m>1/x^2</m> grow without bound near <m>0</m>.  This is infinite behavior, not a finite limit.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.70\textwidth,
    height=0.46\textwidth,
    xmin=-1.1, xmax=1.1,
    ymin=0, ymax=30,
    axis lines=middle,
    xlabel={\(x\)},
    ylabel={\(y\)},
    xtick={-1,0,1},
    ytick={10,20,30},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=-1:-0.19, samples=120] {1/(x^2)};
\addplot[thick, domain=0.19:1, samples=120] {1/(x^2)};
\draw[dashed, thick] (axis cs:0,0) -- (axis cs:0,30);
\node[anchor=west] at (axis cs:0.12,12) {\(x=0\)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <p>A removable discontinuity can be repaired by filling in one value.  Infinite divergent behavior cannot.  No choice of <m>f(0)</m> can keep the nearby values close to a finite number.</p>

  <md>\infty\text{ describes unbounded behavior.  It is not a real-number target.}</md>

  <exercise xml:id="checkpoint-3-7-infinity-not-a-finite-target">
        <title>Why is <m>\infty</m> not a real-number target?</title>
        <statement>
          <p>
            The values of <m>\frac1{x^2}</m> grow beyond every fixed positive bound as <m>x\to0</m>. In your own words, why is that infinite behavior not a finite limit, and why can no choice of a value at the missing point keep the nearby outputs close to a real number <m>L</m>?
          </p>
        </statement>
        <hint>
          <p>
            A finite limit requires outputs to settle inside some <m>\varepsilon</m>-window around a real target. Unbounded growth refuses every such window. Filling one point cannot cage an exploding neighborhood.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-ivt-fails-without-continuity-warning">
    <title>The Intermediate Value Theorem fails without continuity</title>

  <p>The Intermediate Value Theorem said:</p>

  <md>\text{continuous graph}+\text{endpoint values}
\quad\Longrightarrow\quad
\text{all intermediate values are hit.}</md>

  <p>The word <em>continuous</em> cannot be removed.</p>

  <p>Define</p>

  <md>
    <mrow>S(x)=
\begin{cases}
0, \amp x&lt;0,</mrow>
    <mrow>2, \amp x\ge0.
\end{cases}</mrow>
  </md>

  <p>On the interval <m>[-1,1]</m>,</p>

  <md>S(-1)=0
\qquad\text{and}\qquad
S(1)=2.</md>

  <p>The number <m>1</m> lies between <m>0</m> and <m>2</m>.  If we tried to use the Intermediate Value Theorem, we might expect some <m>c\in[-1,1]</m> such that</p>

  <md>S(c)=1.</md>

  <p>But no such <m>c</m> exists.  The function takes only the values <m>0</m> and <m>2</m>.  It jumps over <m>1</m>.</p>

  <figure xml:id="fig-ivt-warning-fails">
    <caption>Without continuity, a function can jump over an intermediate value.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.68\textwidth,
    height=0.42\textwidth,
    xmin=-1.4, xmax=1.4,
    ymin=-0.4, ymax=2.4,
    axis lines=middle,
    xlabel={\(x\)},
    ylabel={\(S(x)\)},
    xtick={-1,0,1},
    ytick={0,1,2},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=-1.35:-0.04, samples=2] {0};
\addplot[thick, domain=0:1.35, samples=2] {2};
\addplot[only marks, mark=o, mark size=3pt, thick] coordinates {(0,0)};
\addplot[only marks, mark=*, mark size=2.5pt] coordinates {(0,2)};
\draw[dashed] (axis cs:-1.35,1) -- (axis cs:1.35,1);
\node[anchor=south west, fill=white, inner sep=1pt] at (axis cs:0.18,1.12) {skipped value \(1\)};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <p>The theorem fails because <m>S</m> is not continuous at <m>0</m>.</p>

  <md>\text{The Intermediate Value Theorem is a no-jump theorem.}</md>

  <exercise xml:id="checkpoint-3-7-ivt-no-jump">
        <title>What illegal use of the Intermediate Value Theorem do the warnings protect against?</title>
        <statement>
          <p>
            The Intermediate Value Theorem is a no-jump theorem: continuous graph plus endpoint values imply that intermediate heights are hit. In your own words, why does dropping continuity let a function skip a height, and what should you check before using the theorem to prove that an equation has a solution?
          </p>
        </statement>
        <hint>
          <p>
            A theorem is a machine with conditions. If the graph can jump, an intermediate value is no longer guaranteed. Check continuity on the closed interval first.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-evt-fails-open-interval-warning">
    <title>The Extreme Value Theorem fails on an open interval</title>

  <p>The Extreme Value Theorem said:</p>

  <md>\text{continuous on }[a,b]
\quad\Longrightarrow\quad
\text{absolute maximum and minimum are reached.}</md>

  <p>The closed interval <m>[a,b]</m> includes its endpoints.  If the endpoints are missing, the conclusion can fail.</p>

  <p>Consider</p>

  <md>f(x)=x</md>

  <p>on the open interval</p>

  <md>(0,1).</md>

  <p>The function is continuous at every point of its domain.  Its values get close to <m>1</m>, but never equal <m>1</m>, because <m>x=1</m> is not in the interval.</p>

  <p>So <m>f</m> has no absolute maximum on <m>(0,1)</m>.</p>

  <p>The values also get close to <m>0</m>, but never equal <m>0</m>, because <m>x=0</m> is not in the interval.</p>

  <p>So <m>f</m> has no absolute minimum on <m>(0,1)</m>.</p>

  <figure xml:id="fig-evt-warning-open-interval">
    <caption>On <m>(0,1)</m>, the function <m>f(x)=x</m> approaches its lowest and highest possible values but never reaches them.</caption>
    <image width="75%">
      <latex-image>
\begin{tikzpicture}
\begin{axis}[
    width=0.68\textwidth,
    height=0.42\textwidth,
    xmin=-0.15, xmax=1.15,
    ymin=-0.15, ymax=1.15,
    axis lines=middle,
    xlabel={\(x\)},
    ylabel={\(f(x)\)},
    xtick={0,1},
    ytick={0,1},
    grid=both,
    grid style={line width=.1pt, draw=gray!25},
]
\addplot[thick, domain=0.02:0.98, samples=2] {x};
\addplot[only marks, mark=o, mark size=3pt, thick] coordinates {(0,0) (1,1)};
\node[anchor=south east] at (axis cs:0.95,0.95) {not reached};
\node[anchor=north west] at (axis cs:0.05,0.05) {not reached};
\end{axis}
\end{tikzpicture}
      </latex-image>
    </image>
  </figure>

  <p>The problem is not the formula.  The formula <m>f(x)=x</m> is perfectly continuous.  The problem is the domain.  The endpoints are missing.</p>

  <md>\text{For the Extreme Value Theorem, endpoints matter.}</md>

  <exercise xml:id="checkpoint-3-7-evt-hypotheses">
        <title>Which Extreme Value Theorem hypotheses can quietly fail?</title>
        <statement>
          <p>
            On an open interval, <m>f(x)=x</m> approaches its apparent highest and lowest values without reaching them. On <m>[0,\infty)</m>, the same formula has no largest value. In your own words, why do both <q>closed</q> and <q>bounded</q> matter, and what does it mean to say that a theorem’s output is no longer guaranteed when a condition is missing?
          </p>
        </statement>
        <hint>
          <p>
            Endpoints can be the place where a max or min is attained. An interval that stretches forever can let values escape. The examples are reminders of what the theorem actually says, not exceptions to it.
          </p>
        </hint>
      </exercise>

  </subsection>

  <subsection xml:id="subsec-evt-fails-unbounded-interval-warning">
    <title>The Extreme Value Theorem fails on an unbounded interval</title>

  <p>A closed interval is not enough if it stretches forever.</p>

  <p>Consider</p>

  <md>f(x)=x</md>

  <p>on</p>

  <md>[0,\infty).</md>

  <p>The function is continuous on its domain.  The interval includes its left endpoint <m>0</m>, so <m>f</m> has an absolute minimum:</p>

  <md>f(0)=0.</md>

  <p>But <m>f</m> has no absolute maximum.  Given any value <m>f(x)=x</m>, we can choose a larger input, such as <m>x+1</m>, and get</p>

  <md>f(x+1)=x+1&gt;f(x).</md>

  <p>So no largest value exists.</p>

  <p>A physical version is also common.  Recall the cooling model</p>

  <md>T(t)=70+\frac{90}{t+1},
\qquad t\ge0.</md>

  <p>The interval <m>[0,\infty)</m> is unbounded.  The temperature starts at</p>

  <md>T(0)=160,</md>

  <p>and approaches <m>70</m> as <m>t\to\infty</m>.  It never reaches <m>70</m> for any finite <m>t</m>, because</p>

  <md>\frac{90}{t+1}&gt;0.</md>

  <p>So this continuous function has an absolute maximum on <m>[0,\infty)</m>, but no absolute minimum.</p>

  <md>\text{A function can approach a best value forever without reaching it.}</md>

  <p>The word “bounded” in the Extreme Value Theorem prevents this escape to infinity.</p>
  </subsection>

  <subsection xml:id="subsec-map-of-warnings">
    <title>A map of the warnings</title>

  <p>The examples above are not exceptions to the theorems.  They are reminders of what the theorems actually say.</p>

  <table xml:id="tab-sec-3-7-warning-examples-4">
    <tabular halign="left">
      <row bottom="minor">
        <cell><strong>claim we want</strong></cell>
        <cell><strong>missing hypothesis</strong></cell>
        <cell><strong>what breaks</strong></cell>
      </row>
      <row bottom="minor">
        <cell><m>\lim_{x\to a}f(x)</m> exists</cell>
        <cell>left and right sides agree</cell>
        <cell>jump gives two targets</cell>
      </row>
      <row bottom="minor">
        <cell>
finite limit exists</cell>
        <cell>values stay bounded near <m>a</m></cell>
        <cell>vertical asymptote</cell>
      </row>
      <row bottom="minor">
        <cell>
IVT conclusion</cell>
        <cell>continuity</cell>
        <cell>intermediate value can be skipped</cell>
      </row>
      <row bottom="minor">
        <cell>
EVT conclusion</cell>
        <cell>closed interval</cell>
        <cell>endpoint value may not be reached</cell>
      </row>
      <row>
        <cell>
EVT conclusion</cell>
        <cell>bounded interval</cell>
        <cell>largest or smallest value may escape to infinity</cell>
      </row>
    </tabular>
  </table>

  <p>A theorem is a machine with conditions.  If a condition is missing, the output is no longer guaranteed.</p>

  <p>Limits and continuity have now given us a careful language for functions of a real variable <m>x</m>.  But some limiting processes arrive as lists:</p>

  <md>1,\quad \frac12,\quad \frac13,\quad \frac14,\quad \ldots</md>

  <p>or</p>

  <md>1.4,\quad 1.41,\quad 1.414,\quad 1.4142,\quad \ldots</md>

  <p>The input is no longer every nearby real number.  The input is the whole-number step <m>n</m>.  That leads to sequences.</p>
  </subsection>



</section>
